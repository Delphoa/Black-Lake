# ProtoKV Streaming Video Understanding: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “ProtoKV: Streaming Video Understanding under Delayed Query with Summary-State Memory”

**Source paper:** Le Tu Ngoc Minh, Jinyeong Lim, Dongsu Han, “ProtoKV: Streaming Video Understanding under Delayed Query with Summary-State Memory,” arXiv:2606.26762v1.[^source-paper]

**Artifact prepared:** 2026-07-16  
**Local source reviewed:** complete PDF (20 pages) and matching full-paper HTML (69995 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **ProtoKV: Streaming Video Understanding under Delayed Query with Summary-State Memory** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Streaming video understanding (SVU) must answer queries that arrive asynchronously while visual tokens stream continuously under strict GPU-memory and query-time latency budgets. A key challenge is delayed query: decisive cues may appear briefly, yet many subsequent updates occur before the query arrives, increasing the risk that those cues are evicted or diluted under bounded memory.
- Source passage 3: Streaming video understanding (SVU) systems must reason over continuous video signals in real-time (Zhang et al. , 2025 ; Niu et al.
- Source passage 4: A central difficulty in SVU is delayed query, where decisive cues (e.g., a momentary state change) appear briefly and long before a query is issued (Yang et al. , 2023 ; Grauman et al.
- Source passage 5: Bounded-memory designs typically occupy two extremes: sliding-window baselines maintain constant memory by retaining only recent content but inevitably lose evidence once it expires (Xiao et al. , 2024 ; Beltagy et al.
- Source passage 6: Recent work advances this operating point via online KV-cache compression under a hard memory cap for SVU (Kim et al. , 2025 ) .
- Source passage 7: We propose ProtoKV, a constant-footprint SVU memory mechanism in this regime that shifts far-history representation away from token retention via a two-tier online state: (i) a short near window storing exact KV for recent frames, and (ii) a fixed-capacity far memory summarizing older history into object-centric prototypes with lightweight residual statistics. ProtoKV does not retain token-level KV for the distant past; instead, it leverages pseudo-tokens and mass-aware weighting to allow standard attention to access far-history evidence under a constant query-time footprint.
- Source passage 8: Contributions. (1) We introduce a far-memory mechanism for SVU that summarizes distant history into a fixed-capacity, spatiotemporally consistent prototype bank augmented with residual statistics.
- Source passage 9: Streaming video understanding (SVU) requires maintaining an online state to process continuous video streams and answer arbitrary queries without replaying past frames (Chen et al. , 2024 ; Chatterjee et al.
- Source passage 10: This pressure is amplified in video due to the high token arrival rate, quickly producing large token histories even with aggressive sampling. As a result, SVU systems must maintain an online state that supports immediate answering at query time, but whose memory cost does not scale with stream length.

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 470 extractable mathematical objects, 55 substantive headings, and 21 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: t_{e}
- Equation or mathematical object 2: t_{q}
- Equation or mathematical object 3: q
- Equation or mathematical object 4: t_{e}
- Equation or mathematical object 5: t_{q}
- Equation or mathematical object 6: \Delta_{q}=t_{q}-t_{e}
- Equation or mathematical object 7: \Delta_{q}
- Equation or mathematical object 8: t_{e}
- Equation or mathematical object 9: t_{0}
- Equation or mathematical object 10: t_{q}=t_{0}+\Delta
- Equation or mathematical object 11: \Delta_{q}
- Equation or mathematical object 12: \Delta
- Equation or mathematical object 13: \rightarrow
- Equation or mathematical object 14: \Delta
- Equation or mathematical object 15: \Delta
- Equation or mathematical object 16: W
- Equation or mathematical object 17: K_{\max}
- Equation or mathematical object 18: S
- Equation or mathematical object 19: L_{context}=W+K_{\max}\cdot S
- Equation or mathematical object 20: B_{\text{near}}

### 2.2 Method and information flow

- Method evidence 1: Streaming video understanding (SVU) must answer queries that arrive asynchronously while visual tokens stream continuously under strict GPU-memory and query-time latency budgets. A key challenge is delayed query: decisive cues may appear briefly, yet many subsequent updates occur before the query arrives, increasing the risk that those cues are evicted or diluted under bounded memory. We propose ProtoKV, a...
- Method evidence 2: Recent work advances this operating point via online KV-cache compression under a hard memory cap for SVU (Kim et al. , 2025 ) . While this class of methods yields predictable query-time cost, online retention decisions are brittle when answers depend on rare but decisive evidence across long delays. More broadly, tying far-history to specific token instances means that once a token is dropped, its associated...
- Method evidence 3: We propose ProtoKV, a constant-footprint SVU memory mechanism in this regime that shifts far-history representation away from token retention via a two-tier online state: (i) a short near window storing exact KV for recent frames, and (ii) a fixed-capacity far memory summarizing older history into object-centric prototypes with lightweight residual statistics. ProtoKV does not retain token-level KV for the distant...
- Method evidence 4: Contributions. (1) We introduce a far-memory mechanism for SVU that summarizes distant history into a fixed-capacity, spatiotemporally consistent prototype bank augmented with residual statistics. (2) We propose pseudo-token synthesis with mass-aware weighting, enabling standard attention to exploit far-history evidence under a bounded query-time footprint. (3) We introduce a delayed-query protocol with...
- Method evidence 5: While some works utilize end-to-end trained compression (Chatterjee et al. , 2025 ) , we focus on training-free, drop-in mechanisms that are easier to deploy across models and backbones. Prior approaches span a spectrum of trade-offs under these constraints. Sliding-window KV caching (SWA) guarantees constant memory by retaining only the most recent window, yielding predictable cost but losing evidence once it exits...
- Method evidence 6: Baselines. We restrict our comparisons to methods that match our deployment constraints: (i) bounded KV memory, (ii) training-free evaluation (no additional fine-tuning), and (iii) query-agnostic online updates, where the memory state is continuously maintained as frames arrive and queries are answered immediately at tq without query-conditioned retrieval or re-encoding. Under these criteria, we include two...
- Method evidence 7: Budget Matching and Efficiency Measurement. We compare methods under a common query-time context budget by controlling the number of visual tokens that each method allows the model to attend to at query time. We then report the resulting measured peak GPU memory and latency metrics to verify that the compared settings fall within the intended budget regime. Query-time latency is reported separately in Fig. 4(b) .
- Method evidence 8: Fig. 4 summarizes the system-level budget sensitivity of ProtoKV under memory constraints. Fig. 4(a) reports accuracy as we vary the memory budget, showing that ProtoKV consistently delivers the strongest accuracy among the budgeted baselines, with larger budgets translating into more reliable improvements. Fig. 4(b) compares query-time TTFT (time from query arrival to the first generated token) under a matched...
- Method evidence 9: Comparison to SVU offloading baseline. Table 4 compares ProtoKV with ReKV (Di et al. , 2025 ) , a representative offloading/retrieval-based SVU baseline, to contextualize the latency–accuracy trade-off against an external-memory approach under matched peak GPU memory. While achieving comparable accuracy on RVS-Ego, ProtoKV improves query-time responsiveness, reducing both query-time TTFT and end-to-end latency by...
- Method evidence 10: SVU with bounded memory. A common streaming baseline is sliding-window KV caching, which guarantees constant memory but can discard decisive cues when queries arrive after long and unpredictable delays. InfiniPot-V (Kim et al. , 2025 ) extends this regime with online token-retention/compression under a fixed cap, mitigating outright eviction yet still representing far history as a subset of token instances — a...
- Method evidence 11: KV Cache Compression in Large Language Models. KV cache management in LLMs primarily relies on sliding windows (Xiao et al. , 2024 ) or importance-based eviction (Li et al. , 2024b ; Zhang et al. , 2023 ) . While effective for discrete linguistic tokens, these token-selection strategies are often too brittle for video streams. As demonstrated in our experiments, such selection-only approaches fail to preserve sparse...
- Method evidence 12: In this work, we address streaming video understanding under strict memory budgets, where decisive evidence can appear briefly but must remain usable after many subsequent updates before a query arrives. We propose ProtoKV, a constant-footprint KV memory that keeps an exact near-window cache while representing far history as an object-centric prototype bank with lightweight residual statistics. At query time,...
- Method evidence 13: All baselines are evaluated under the same benchmark protocol, video preprocessing pipeline, backbone, and total memory budget | M | |M| ; the memory mechanism is the only intended source of difference between methods.
- Method evidence 14: In both setups the raw context is then compressed to the budget | M | |M| by each bounded-memory method, so the effective compression factor scales with both stream length up to t q t_{q} and the chosen | M | |M| . The LLaVA-OV setting represents the more aggressive compression regime in our experiments, while the Qwen2.5-VL setting operates at a fixed raw ceiling regardless of stream length.

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: ProtoKV: Streaming Video Understanding under Delayed Query with Summary-State Memory
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: 2 Background and Problem Setup
- Source section 5: 2.1 KV Growth as an Operational Constraint in SVU
- Source section 6: 2.2 Bounded-Memory SVU and Prior Approaches
- Source section 7: 2.3 Query Delay and Post-Evidence Update Pressure
- Source section 8: 3 ProtoKV
- Source section 9: 3.1 State representation and prototype bank
- Source section 10: 3.2 Online update: eviction, assignment, and prototype maintenance
- Source section 11: 3.3 Residual Statistics and Pseudo-Token Interface
- Source section 12: 4 Experiments
- Source section 13: 4.1 Experimental Setup
- Source section 14: 4.2 Main Benchmark - SVU
- Source section 15: 4.3 Query-Delay Robustness
- Source section 16: 4.4 Ablation Study
- Source section 17: 4.5 Auxiliary Evaluation on Offline Video Understanding
- Source section 18: 4.6 Task-Type Analysis
- Source section 19: 4.7 System Validation
- Source section 20: 5 Related Work
- Source section 21: 6 Conclusion
- Source section 22: Acknowledgement
- Source section 23: Impact Statement
- Source section 24: References
- Source section 25: Appendix A ProtoKV Method Details
- Source section 26: A.1 Streaming Memory State and Residual Representation
- Source section 27: Residual statistics.
- Source section 28: PQ codebook initialization.
- Source section 29: A.2 Streaming Update Procedure
- Source section 30: A.3 Query-Time Pseudo-Token Synthesis
- Source section 31: Bounded attention context.
- Source section 32: Appendix B Configuration and Hyperparameters
- Source section 33: B.1 Default Hyperparameter Configuration
- Source section 34: Scaling rule for the memory budget.
- Source section 35: Additional implementation constants.
- Source section 36: B.2 Hyperparameter Sensitivity
- Source section 37: Budget allocation.
- Source section 38: Update and maintenance coefficients.
- Source section 39: Appendix C Experimental Setting Details
- Source section 40: C.1 Dataset Selection for Query-Delay Sweep
- Source section 41: Delayed-query protocol and validity constraint.
- Source section 42: Retrospective query types per benchmark.
- Source section 43: Included vs. excluded examples.
- Source section 44: C.2 Implementation Setup
- Source section 45: Baseline configurations.
- Source section 46: Raw sequence lengths.
- Source section 47: C.3 Online Update Overhead
- Source section 48: Setup.
- Source section 49: Where the update time goes.
- Source section 50: Why this does not affect query-time TTFT.
- Source section 51: Appendix D Additional Experiments
- Source section 52: D.1 RoPE Position Anchor Comparison
- Source section 53: D.2 MLVU Task-Type Breakdown
- Source section 54: D.3 Dynamic Pseudo-Token Allocation
- Source section 55: Appendix E Qualitative Case Studies

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: Streaming video understanding (SVU) must answer queries that arrive asynchronously while visual tokens stream continuously under strict GPU-memory and query-time latency budgets. A key challenge is delayed query: decisive cues may appear briefly, yet many subsequent updates occur before the query arrives, increasing the risk that those cues are evicted or diluted under bounded memory. We propose ProtoKV, a...
- Evaluation evidence 2: Streaming video understanding (SVU) systems must reason over continuous video signals in real-time (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) . Despite progress on offline benchmarks, real-world deployment requires handling continuous frames and asynchronous queries under a fixed GPU memory budget (Chatterjee et al. , 2025 ) . In this setting, the model cannot assume access to the full clip nor...
- Evaluation evidence 3: We propose ProtoKV, a constant-footprint SVU memory mechanism in this regime that shifts far-history representation away from token retention via a two-tier online state: (i) a short near window storing exact KV for recent frames, and (ii) a fixed-capacity far memory summarizing older history into object-centric prototypes with lightweight residual statistics. ProtoKV does not retain token-level KV for the distant...
- Evaluation evidence 4: This pressure is amplified in video due to the high token arrival rate, quickly producing large token histories even with aggressive sampling. As a result, SVU systems must maintain an online state that supports immediate answering at query time, but whose memory cost does not scale with stream length. While simple heuristics like window truncation or token downsampling can stabilize memory (Xiao et al. , 2024 ;...
- Evaluation evidence 5: While some works utilize end-to-end trained compression (Chatterjee et al. , 2025 ) , we focus on training-free, drop-in mechanisms that are easier to deploy across models and backbones. Prior approaches span a spectrum of trade-offs under these constraints. Sliding-window KV caching (SWA) guarantees constant memory by retaining only the most recent window, yielding predictable cost but losing evidence once it exits...
- Evaluation evidence 6: In practice, SVU benchmarks (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) typically do not provide per-query evidence timestamps t e t_{e} . Thus, our delayed-query evaluation uses the dataset-defined query timestamp t 0 t_{0} as a reference and shifts the query to t q = t 0 + Δ t_{q}=t_{0}+\Delta (used as an empirical proxy for Δ q \Delta_{q} ), while restricting to retrospective query types with...
- Evaluation evidence 7: Pseudo tokens. Residual statistics are useful only if they can be consumed at query time without changing the model. We expose far memory as a fixed set of pseudo tokens: synthetic KV-cache entries that are directly consumable by standard attention. Injecting a small set of auxiliary key/value vectors as a lightweight interface to attention has been explored in parameter-efficient adaptation (Li and Liang, 2021 ) ....
- Evaluation evidence 8: Benchmarks. We evaluate on four SVU benchmarks (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) that provide a dataset-defined query timestamp for each example and task-type annotations that allow principled grouping of queries. We follow the official evaluation split for each dataset. These annotations enable controlled delayed-query sweeps and rule-based filtering of query types without manual...
- Evaluation evidence 9: Delayed-Query Protocol and Valid-Query Selection. Ideally, one would evaluate query delay relative to the last-seen evidence time t e t_{e} by setting t q = t e + Δ e t_{q}=t_{e}+\Delta_{e} . Since the evaluated SVU benchmarks do not provide t e t_{e} per query, we instead use the dataset-defined query timestamp t 0 t_{0} as a reference and sweep delayed queries by setting t q = t 0 + Δ t_{q}=t_{0}+\Delta .
- Evaluation evidence 10: Baselines. We restrict our comparisons to methods that match our deployment constraints: (i) bounded KV memory, (ii) training-free evaluation (no additional fine-tuning), and (iii) query-agnostic online updates, where the memory state is continuously maintained as frames arrive and queries are answered immediately at tq without query-conditioned retrieval or re-encoding. Under these criteria, we include two...
- Evaluation evidence 11: Budget Matching and Efficiency Measurement. We compare methods under a common query-time context budget by controlling the number of visual tokens that each method allows the model to attend to at query time. We then report the resulting measured peak GPU memory and latency metrics to verify that the compared settings fall within the intended budget regime. Query-time latency is reported separately in Fig. 4(b) .
- Evaluation evidence 12: We first evaluate ProtoKV on standard SVU benchmarks under matched peak GPU memory; query-time latency is validated separately in Fig. 4(b) under the same budgeted setting. Table 1 reports main benchmark accuracy and scores for two backbones and compares ProtoKV against SWA and a token-retention baseline.
- Evaluation evidence 13: ProtoKV achieves the best overall performance across the RVS benchmarks on both backbones, consistently outperforming SWA and the token-retention baseline. ProtoKV likewise yields higher accuracy on OVO-Bench and StreamingBench under the same KV cache memory budget. Overall, these results indicate that ProtoKV’s summary-state far memory makes more effective use of limited memory than window truncation or...
- Evaluation evidence 14: Fig. 3 reports accuracy as a function of query delay Δ \Delta . Across all settings, performance decreases as Δ \Delta grows, but the degradation profile differs sharply by memory mechanism. SWA exhibits an abrupt drop once the delay pushes the relevant evidence beyond the near window, reflecting that evidence is irrecoverably lost after truncation. InfiniPot-V degrades more gradually, yet still shows a consistent...
- Evaluation evidence 15: At the largest evaluated delays, across both backbones, ProtoKV yields up to +20.4pp over SWA and +12.5pp over token retention on RVS-Movie, with comparable long-delay improvements on RVS-Ego and OVO-Bench. This behavior supports our central interpretation of Δ \Delta as post-evidence update pressure, not merely “older evidence”: larger Δ \Delta implies the decisive cue must survive more overwrite/eviction/merge...
- Evaluation evidence 16: Table 2 summarizes component ablations under matched peak GPU memory. ProtoKV shows a relatively mild drop as query delay increases, while the ablations reveal that this robustness is not due to any single trick but to the overall design. In particular, removing the far-memory summary leads to a pronounced failure at long delays, whereas keeping only the far prototypes preserves delayed-query behavior but sacrifices...
- Evaluation evidence 17: To complement our SVU evaluation, we additionally evaluate ProtoKV on offline video understanding benchmarks (Fu et al. , 2025 ; Zhou et al. , 2025 ) under the same memory budget, using Qwen2-VL-7B (Wang et al. , 2024 ) to align with prior OVU baselines’ reported settings. We compare ProtoKV against representative OVU baselines including DyCoke (Tao et al. , 2025 ) . As shown in Table 3 , ProtoKV improves...
- Evaluation evidence 18: Fig. 4 summarizes the system-level budget sensitivity of ProtoKV under memory constraints. Fig. 4(a) reports accuracy as we vary the memory budget, showing that ProtoKV consistently delivers the strongest accuracy among the budgeted baselines, with larger budgets translating into more reliable improvements. Fig. 4(b) compares query-time TTFT (time from query arrival to the first generated token) under a matched...
- Evaluation evidence 19: Comparison to SVU offloading baseline. Table 4 compares ProtoKV with ReKV (Di et al. , 2025 ) , a representative offloading/retrieval-based SVU baseline, to contextualize the latency–accuracy trade-off against an external-memory approach under matched peak GPU memory. While achieving comparable accuracy on RVS-Ego, ProtoKV improves query-time responsiveness, reducing both query-time TTFT and end-to-end latency by...
- Evaluation evidence 20: SVU with bounded memory. A common streaming baseline is sliding-window KV caching, which guarantees constant memory but can discard decisive cues when queries arrive after long and unpredictable delays. InfiniPot-V (Kim et al. , 2025 ) extends this regime with online token-retention/compression under a fixed cap, mitigating outright eviction yet still representing far history as a subset of token instances — a...
- Evaluation evidence 21: Offline Video Understanding and Long-Context VLMs. Long-context VLMs typically compress video tokens in offline settings where the full sequence is accessible. LongVU (Shen et al. , 2025 ) employs a training-based adaptive compression module, while DyCoke (Tao et al. , 2025 ) offers a training-free alternative via importance-based pruning. However, these offline-centric designs are not optimized for the continuous...
- Evaluation evidence 22: In this work, we address streaming video understanding under strict memory budgets, where decisive evidence can appear briefly but must remain usable after many subsequent updates before a query arrives. We propose ProtoKV, a constant-footprint KV memory that keeps an exact near-window cache while representing far history as an object-centric prototype bank with lightweight residual statistics. At query time,...
- Evaluation evidence 23: We appreciate anonymous reviewers for providing constructive feedback and suggestions. This work was supported by Samsung Electronics, Institute for Information & communications Technology Planning & Evaluation (IITP) grant funded by the Korea government (MSIT)(RS-2019-II190075,Artificial Intelligence Graduate School Support Program (KAIST)), and National Research Foundation of Korea (NRF) grant funded by the Korea...
- Evaluation evidence 24: At query time, ProtoKV exposes each prototype as S S pseudo-KV tokens decoded from R k R_{k} , concatenates them with the near-window KV, and feeds the result to standard attention. We treat each PQ histogram as a factorized categorical distribution over code tuples. The smoothed per-subspace probability for key residuals is
- Evaluation evidence 25: Table 5 lists the default hyperparameter values used in all main experiments unless otherwise stated. The same defaults are applied across all six benchmarks in this paper, without per-dataset retuning. The only quantity varied across evaluation settings is the external memory budget | M | |M| , which determines W W and K max K_{\max} according to the scaling rule described below.
- Evaluation evidence 26: A few additional constants appear in our implementation but are not critical to the main results: the warm-up window T warm T_{\mathrm{warm}} used to collect residuals for PQ codebook initialization ( Appendix A ) and the smoothing constant ϵ \epsilon used in the PQ probability estimate ( Equation 3 ). Empirical performance is not sensitive to small perturbations of these constants within typical ranges; exact...
- Evaluation evidence 27: ProtoKV exposes hyperparameters in three groups: (i) budget allocation , which divides the query-time token budget | M | = W + K max ⋅ S |M|=W+K_{\max}\cdot S between the near window and the far prototype bank; (ii) assignment and refresh ( λ sp \lambda_{\mathrm{sp}} , λ idle \lambda_{\mathrm{idle}} , T idle T_{\mathrm{idle}} ), which control prototype continuity and staleness behavior; and (iii) update and...
- Evaluation evidence 28: Under a fixed total budget | M | = 24 ​ k |M|=24\text{k} , we vary the near–far split by adjusting W W versus K max ⋅ S K_{\max}\cdot S while holding | M | = W + K max ⋅ S |M|=W+K_{\max}\cdot S constant. We also vary the per-prototype pseudo-token count S S with K max ⋅ S K_{\max}\cdot S fixed. Results on Qwen2.5-VL-7B and RVS-Ego are reported in Table 6 . Performance is similar across balanced splits, with only the...
- Evaluation evidence 29: We additionally perturb each key update and maintenance coefficient by 0.5 × 0.5\times and 2 × 2\times its default value on both RVS-Ego and RVS-Movie, keeping all other settings at default. Table 7 reports the resulting accuracy changes relative to the default. Across all swept coefficients, the changes remain modest, staying within 0.9 0.9 points of the default on both datasets. This indicates that ProtoKV is not...
- Evaluation evidence 30: Ideally, t 0 t_{0} would be the last-seen timestamp of decisive evidence for each query; the SVU benchmarks evaluated here do not annotate this quantity, so we follow standard practice and use the dataset-provided query timestamp as a proxy for t 0 t_{0} . For a video of length L L , a delayed query is valid only if t 0 + Δ ≤ L t_{0}+\Delta\leq L . To ensure fair comparisons across different Δ \Delta values, we...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1 : Query delay in streaming video understanding. When the delay exceeds the near window, answering requires information preserved in far memory.
- Visual/table evidence 2: Figure 2 : ProtoKV maintains an exact near-window KV buffer for recent tokens and, as the video stream continues, continuously aggregates evicted history into a fixed-capacity, object-centric prototype bank via online assignment. At query time, it synthesizes a bounded number of pseudo-KV tokens per prototype and concatenates them with the near-window tokens, yielding a fixed-length sequence that can be consumed by...
- Visual/table evidence 3: Table 1 : Main benchmark performance on SVU benchmarks. We use token budget | M | |M| = 24k tokens for RVS-Ego and RVS-Movie, and | M | |M| = 4k tokens for OVO-Bench and StreamingBench. For StreamingBench, we evaluate only Real-time visual understanding queries. Score columns are on a 0-5 scale.
- Visual/table evidence 4: Figure 3 : Query-delay robustness under matched token budgets in the online SVU setting. We apply a delayed-query sweep by setting t q = t 0 + Δ t_{q}=t_{0}+\Delta (minutes) and retain only retrospective query types whose answers are invariant to shifting t q t_{q} . For fair comparisons across Δ \Delta , we evaluate on the intersection subset that is valid for all delay values. We use token budget | M | |M| = 24k...
- Visual/table evidence 5: Table 2 : Component ablations under matched peak GPU memory ( | M | |M| = 24k, model: LLaVA-OV-7B, dataset: RVS-Ego). Evaluated on the same intersection subset used in Figure 3 .
- Visual/table evidence 6: Table 3 : Performance on offline video understanding benchmark (OVU) under fixed memory budgets. * denotes the numbers from official paper. We use Qwen2-VL-7B in this table for alignment with prior OVU baselines.
- Visual/table evidence 7: (a) Memory budget
- Visual/table evidence 8: (b) Query-time latency
- Visual/table evidence 9: Figure 4 : Budget sensitivity and query-time TTFT under memory constraints. (a) Accuracy versus memory budget. (b) Query-time TTFT (query arrival → first token) under a matched memory budget. DyCoke is shown as a query-conditioned reference.
- Visual/table evidence 10: Table 4 : Comparison to an SVU offloading/retrieval baseline on RVS-Ego under matched peak GPU memory with LLaVA-OV-7B. TTFT (query arrival → \rightarrow first token) and E2E latency (query arrival → \rightarrow completion) are measured.
- Visual/table evidence 11: Algorithm 1 ProtoKV Streaming Update
- Visual/table evidence 12: Algorithm 2 ProtoKV Prototype Maintenance
- Visual/table evidence 13: Algorithm 3 DecodeTopSResidualModes
- Visual/table evidence 14: Algorithm 4 ProtoKV Query-Time Pseudo-Token Decoding and Attention
- Visual/table evidence 15: Table 5 : Default hyperparameter configuration used in all main experiments. The same values are used across all six benchmarks; only | M | |M| varies across evaluation settings, which scales W W and K max K_{\max} as described in the text.
- Visual/table evidence 16: Table 6 : Sensitivity to budget allocation under fixed | M | = 24 ​ k |M|=24\text{k} on Qwen2.5-VL-7B / RVS-Ego. Accuracy is reported at query delays Δ = 0 \Delta=0 and Δ = 30 \Delta=30 minutes. Top: varying the near-window vs. far-memory split under a fixed total budget. Bottom: varying the pseudo-token count S S per prototype with K max ⋅ S K_{\max}\cdot S held fixed.
- Visual/table evidence 17: Table 7 : Sensitivity to update and maintenance coefficients on RVS-Ego and RVS-Movie under the default budget and Qwen2.5-VL-7B. Entries are accuracy changes (points) relative to the default setting at Δ = 0 \Delta=0 . Default accuracy at Δ = 0 \Delta=0 is 61.0 61.0 on RVS-Ego and 53.3 53.3 on RVS-Movie.
- Visual/table evidence 18: Table 8 : Examples of included and excluded queries for the query-delay sweep, paired by benchmark to highlight the contrast. Included queries are retained because their ground truth is determined by past video content and is invariant to a query-time shift; excluded queries reference the current moment and would change ground truth under the shift.
- Visual/table evidence 19: Table 9 : Accuracy under three RoPE position-anchor choices for τ k \tau_{k} on Qwen2.5-VL-7B / RVS-Ego under the default memory budget. All three variants share the same per-prototype anchor across the S S pseudo-tokens; they differ only in which absorbed position the anchor tracks.
- Visual/table evidence 20: Table 10 : ProtoKV’s per-task accuracy on MLVU under matched memory budget ( | M | = 6 ​ k |M|=6\text{k} ) and Qwen2-VL-7B. Task abbreviations: Order = Action Order, Count = Action Count, AR = Anomaly Recognition, Needle = Needle QA, Ego = Ego Reasoning, TR = Topic Reasoning. “Avg.” is the official MLVU M-Avg score across the seven tasks.
- Visual/table evidence 21: Table 11 : Variance-aware pseudo-token allocation versus the fixed default ( S = 8 S=8 per prototype) under matched total budget S tot = K max ⋅ S S_{\mathrm{tot}}=K_{\max}\cdot S on Qwen2.5-VL-7B / RVS-Ego.

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: Streaming video understanding (SVU) must answer queries that arrive asynchronously while visual tokens stream continuously under strict GPU-memory and query-time latency budgets. A key challenge is delayed query: decisive cues may appear briefly, yet many subsequent updates occur before the query arrives, increasing the risk that those cues are evicted or diluted under bounded memory. We propose ProtoKV, a...
- Evaluation evidence 2: Streaming video understanding (SVU) systems must reason over continuous video signals in real-time (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) . Despite progress on offline benchmarks, real-world deployment requires handling continuous frames and asynchronous queries under a fixed GPU memory budget (Chatterjee et al. , 2025 ) . In this setting, the model cannot assume access to the full clip nor...
- Evaluation evidence 3: We propose ProtoKV, a constant-footprint SVU memory mechanism in this regime that shifts far-history representation away from token retention via a two-tier online state: (i) a short near window storing exact KV for recent frames, and (ii) a fixed-capacity far memory summarizing older history into object-centric prototypes with lightweight residual statistics. ProtoKV does not retain token-level KV for the distant...
- Evaluation evidence 4: This pressure is amplified in video due to the high token arrival rate, quickly producing large token histories even with aggressive sampling. As a result, SVU systems must maintain an online state that supports immediate answering at query time, but whose memory cost does not scale with stream length. While simple heuristics like window truncation or token downsampling can stabilize memory (Xiao et al. , 2024 ;...
- Evaluation evidence 5: While some works utilize end-to-end trained compression (Chatterjee et al. , 2025 ) , we focus on training-free, drop-in mechanisms that are easier to deploy across models and backbones. Prior approaches span a spectrum of trade-offs under these constraints. Sliding-window KV caching (SWA) guarantees constant memory by retaining only the most recent window, yielding predictable cost but losing evidence once it exits...
- Evaluation evidence 6: In practice, SVU benchmarks (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) typically do not provide per-query evidence timestamps t e t_{e} . Thus, our delayed-query evaluation uses the dataset-defined query timestamp t 0 t_{0} as a reference and shifts the query to t q = t 0 + Δ t_{q}=t_{0}+\Delta (used as an empirical proxy for Δ q \Delta_{q} ), while restricting to retrospective query types with...
- Evaluation evidence 7: Pseudo tokens. Residual statistics are useful only if they can be consumed at query time without changing the model. We expose far memory as a fixed set of pseudo tokens: synthetic KV-cache entries that are directly consumable by standard attention. Injecting a small set of auxiliary key/value vectors as a lightweight interface to attention has been explored in parameter-efficient adaptation (Li and Liang, 2021 ) ....
- Evaluation evidence 8: Benchmarks. We evaluate on four SVU benchmarks (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) that provide a dataset-defined query timestamp for each example and task-type annotations that allow principled grouping of queries. We follow the official evaluation split for each dataset. These annotations enable controlled delayed-query sweeps and rule-based filtering of query types without manual...
- Evaluation evidence 9: Delayed-Query Protocol and Valid-Query Selection. Ideally, one would evaluate query delay relative to the last-seen evidence time t e t_{e} by setting t q = t e + Δ e t_{q}=t_{e}+\Delta_{e} . Since the evaluated SVU benchmarks do not provide t e t_{e} per query, we instead use the dataset-defined query timestamp t 0 t_{0} as a reference and sweep delayed queries by setting t q = t 0 + Δ t_{q}=t_{0}+\Delta .
- Evaluation evidence 10: Baselines. We restrict our comparisons to methods that match our deployment constraints: (i) bounded KV memory, (ii) training-free evaluation (no additional fine-tuning), and (iii) query-agnostic online updates, where the memory state is continuously maintained as frames arrive and queries are answered immediately at tq without query-conditioned retrieval or re-encoding. Under these criteria, we include two...
- Evaluation evidence 11: Budget Matching and Efficiency Measurement. We compare methods under a common query-time context budget by controlling the number of visual tokens that each method allows the model to attend to at query time. We then report the resulting measured peak GPU memory and latency metrics to verify that the compared settings fall within the intended budget regime. Query-time latency is reported separately in Fig. 4(b) .
- Evaluation evidence 12: We first evaluate ProtoKV on standard SVU benchmarks under matched peak GPU memory; query-time latency is validated separately in Fig. 4(b) under the same budgeted setting. Table 1 reports main benchmark accuracy and scores for two backbones and compares ProtoKV against SWA and a token-retention baseline.
- Evaluation evidence 13: ProtoKV achieves the best overall performance across the RVS benchmarks on both backbones, consistently outperforming SWA and the token-retention baseline. ProtoKV likewise yields higher accuracy on OVO-Bench and StreamingBench under the same KV cache memory budget. Overall, these results indicate that ProtoKV’s summary-state far memory makes more effective use of limited memory than window truncation or...
- Evaluation evidence 14: Fig. 3 reports accuracy as a function of query delay Δ \Delta . Across all settings, performance decreases as Δ \Delta grows, but the degradation profile differs sharply by memory mechanism. SWA exhibits an abrupt drop once the delay pushes the relevant evidence beyond the near window, reflecting that evidence is irrecoverably lost after truncation. InfiniPot-V degrades more gradually, yet still shows a consistent...
- Evaluation evidence 15: At the largest evaluated delays, across both backbones, ProtoKV yields up to +20.4pp over SWA and +12.5pp over token retention on RVS-Movie, with comparable long-delay improvements on RVS-Ego and OVO-Bench. This behavior supports our central interpretation of Δ \Delta as post-evidence update pressure, not merely “older evidence”: larger Δ \Delta implies the decisive cue must survive more overwrite/eviction/merge...
- Evaluation evidence 16: Table 2 summarizes component ablations under matched peak GPU memory. ProtoKV shows a relatively mild drop as query delay increases, while the ablations reveal that this robustness is not due to any single trick but to the overall design. In particular, removing the far-memory summary leads to a pronounced failure at long delays, whereas keeping only the far prototypes preserves delayed-query behavior but sacrifices...
- Evaluation evidence 17: To complement our SVU evaluation, we additionally evaluate ProtoKV on offline video understanding benchmarks (Fu et al. , 2025 ; Zhou et al. , 2025 ) under the same memory budget, using Qwen2-VL-7B (Wang et al. , 2024 ) to align with prior OVU baselines’ reported settings. We compare ProtoKV against representative OVU baselines including DyCoke (Tao et al. , 2025 ) . As shown in Table 3 , ProtoKV improves...
- Evaluation evidence 18: Fig. 4 summarizes the system-level budget sensitivity of ProtoKV under memory constraints. Fig. 4(a) reports accuracy as we vary the memory budget, showing that ProtoKV consistently delivers the strongest accuracy among the budgeted baselines, with larger budgets translating into more reliable improvements. Fig. 4(b) compares query-time TTFT (time from query arrival to the first generated token) under a matched...
- Evaluation evidence 19: Comparison to SVU offloading baseline. Table 4 compares ProtoKV with ReKV (Di et al. , 2025 ) , a representative offloading/retrieval-based SVU baseline, to contextualize the latency–accuracy trade-off against an external-memory approach under matched peak GPU memory. While achieving comparable accuracy on RVS-Ego, ProtoKV improves query-time responsiveness, reducing both query-time TTFT and end-to-end latency by...
- Evaluation evidence 20: SVU with bounded memory. A common streaming baseline is sliding-window KV caching, which guarantees constant memory but can discard decisive cues when queries arrive after long and unpredictable delays. InfiniPot-V (Kim et al. , 2025 ) extends this regime with online token-retention/compression under a fixed cap, mitigating outright eviction yet still representing far history as a subset of token instances — a...
- Evaluation evidence 21: Offline Video Understanding and Long-Context VLMs. Long-context VLMs typically compress video tokens in offline settings where the full sequence is accessible. LongVU (Shen et al. , 2025 ) employs a training-based adaptive compression module, while DyCoke (Tao et al. , 2025 ) offers a training-free alternative via importance-based pruning. However, these offline-centric designs are not optimized for the continuous...
- Evaluation evidence 22: In this work, we address streaming video understanding under strict memory budgets, where decisive evidence can appear briefly but must remain usable after many subsequent updates before a query arrives. We propose ProtoKV, a constant-footprint KV memory that keeps an exact near-window cache while representing far history as an object-centric prototype bank with lightweight residual statistics. At query time,...
- Evaluation evidence 23: We appreciate anonymous reviewers for providing constructive feedback and suggestions. This work was supported by Samsung Electronics, Institute for Information & communications Technology Planning & Evaluation (IITP) grant funded by the Korea government (MSIT)(RS-2019-II190075,Artificial Intelligence Graduate School Support Program (KAIST)), and National Research Foundation of Korea (NRF) grant funded by the Korea...
- Evaluation evidence 24: At query time, ProtoKV exposes each prototype as S S pseudo-KV tokens decoded from R k R_{k} , concatenates them with the near-window KV, and feeds the result to standard attention. We treat each PQ histogram as a factorized categorical distribution over code tuples. The smoothed per-subspace probability for key residuals is
- Evaluation evidence 25: Table 5 lists the default hyperparameter values used in all main experiments unless otherwise stated. The same defaults are applied across all six benchmarks in this paper, without per-dataset retuning. The only quantity varied across evaluation settings is the external memory budget | M | |M| , which determines W W and K max K_{\max} according to the scaling rule described below.
- Evaluation evidence 26: A few additional constants appear in our implementation but are not critical to the main results: the warm-up window T warm T_{\mathrm{warm}} used to collect residuals for PQ codebook initialization ( Appendix A ) and the smoothing constant ϵ \epsilon used in the PQ probability estimate ( Equation 3 ). Empirical performance is not sensitive to small perturbations of these constants within typical ranges; exact...
- Evaluation evidence 27: ProtoKV exposes hyperparameters in three groups: (i) budget allocation , which divides the query-time token budget | M | = W + K max ⋅ S |M|=W+K_{\max}\cdot S between the near window and the far prototype bank; (ii) assignment and refresh ( λ sp \lambda_{\mathrm{sp}} , λ idle \lambda_{\mathrm{idle}} , T idle T_{\mathrm{idle}} ), which control prototype continuity and staleness behavior; and (iii) update and...
- Evaluation evidence 28: Under a fixed total budget | M | = 24 ​ k |M|=24\text{k} , we vary the near–far split by adjusting W W versus K max ⋅ S K_{\max}\cdot S while holding | M | = W + K max ⋅ S |M|=W+K_{\max}\cdot S constant. We also vary the per-prototype pseudo-token count S S with K max ⋅ S K_{\max}\cdot S fixed. Results on Qwen2.5-VL-7B and RVS-Ego are reported in Table 6 . Performance is similar across balanced splits, with only the...
- Evaluation evidence 29: We additionally perturb each key update and maintenance coefficient by 0.5 × 0.5\times and 2 × 2\times its default value on both RVS-Ego and RVS-Movie, keeping all other settings at default. Table 7 reports the resulting accuracy changes relative to the default. Across all swept coefficients, the changes remain modest, staying within 0.9 0.9 points of the default on both datasets. This indicates that ProtoKV is not...
- Evaluation evidence 30: Ideally, t 0 t_{0} would be the last-seen timestamp of decisive evidence for each query; the SVU benchmarks evaluated here do not annotate this quantity, so we follow standard practice and use the dataset-provided query timestamp as a proxy for t 0 t_{0} . For a video of length L L , a delayed query is valid only if t 0 + Δ ≤ L t_{0}+\Delta\leq L . To ensure fair comparisons across different Δ \Delta values, we...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: 4.4 Ablation Study
- Ablation or analysis section 2: 4.6 Task-Type Analysis
- Ablation or analysis section 3: B.2 Hyperparameter Sensitivity
- Ablation or analysis section 4: D.1 RoPE Position Anchor Comparison

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

### 8.1 Primary external artifacts checked

- [Official ProtoKV repository](https://github.com/kaist-ina/ProtoKV) — Repository for the delayed-query streaming-video paper, distinguished from the unrelated text ProtoKV work.

### 8.2 Associated Black Lake records

- No same-paper DEP-A or DEP-E record and no directly corresponding Black-Lake-Data DEP was verified after checks by arXiv ID, DOI, normalized title, method, dataset, and benchmark.

## 9. Research notes and critical considerations

- Qualification evidence 1: Streaming video understanding (SVU) must answer queries that arrive asynchronously while visual tokens stream continuously under strict GPU-memory and query-time latency budgets. A key challenge is delayed query: decisive cues may appear briefly, yet many subsequent updates occur before the query arrives, increasing the risk that those cues are evicted or diluted under bounded memory. We propose ProtoKV, a...
- Qualification evidence 2: Streaming video understanding (SVU) systems must reason over continuous video signals in real-time (Zhang et al. , 2025 ; Niu et al. , 2025 ; Lin et al. , 2024 ) . Despite progress on offline benchmarks, real-world deployment requires handling continuous frames and asynchronous queries under a fixed GPU memory budget (Chatterjee et al. , 2025 ) . In this setting, the model cannot assume access to the full clip nor...
- Qualification evidence 3: In contrast to offline video understanding, where the full sequence is accessible and models can compress or revisit tokens with complete context (Zhou et al. , 2025 ; Fu et al. , 2025 ) , SVU must process frames online without knowing the stream horizon in advance. In practice, this makes it unrealistic to “buffer the whole video” before answering. We therefore focus on bounded-memory SVU under three...
- Qualification evidence 4: Importantly, Δ \Delta is not merely a time gap; it serves as a proxy for the amount of post-evidence update pressure accumulated before answering. During this interval, the system must repeatedly (i) encode incoming frames and (ii) update its bounded internal state. Because these updates are query-agnostic, the system must commit to maintenance decisions before knowing which cues will later be queried, and thus...
- Qualification evidence 5: A common strategy for bounded memory is token retention , which preserves far history by keeping selected token-level KV instances (Zhang et al. , 2023 ; Li et al. , 2024b ; Kim et al. , 2025 ) . However, under large Δ \Delta , token retention faces intrinsic challenges. First, retained tokens must compete for a fixed capacity over a long stream; early retention choices can become suboptimal as new content arrives,...
- Qualification evidence 6: We present ProtoKV , a constant-footprint KV memory for streaming video LLMs under arbitrary query arrivals. The central challenge is post-evidence update pressure : after decisive evidence appears, the system must absorb many subsequent updates before the query arrives, and token-instance retention can become increasingly brittle under a fixed budget as this delay grows.
- Qualification evidence 7: Residual statistics. For a token i i assigned to prototype k k , we define residuals: r i K = K i − c k , r i V = V i − p k r_{i}^{K}=K_{i}-c_{k},\quad r_{i}^{V}=V_{i}-p_{k} . The key point is not to reconstruct individual tokens, but to retain intra-prototype diversity that a single center cannot represent. We implement this with Product Quantization (Jegou et al. , 2010 ) -based streaming histogram statistics: it...
- Qualification evidence 8: Table 2 summarizes component ablations under matched peak GPU memory. ProtoKV shows a relatively mild drop as query delay increases, while the ablations reveal that this robustness is not due to any single trick but to the overall design. In particular, removing the far-memory summary leads to a pronounced failure at long delays, whereas keeping only the far prototypes preserves delayed-query behavior but sacrifices...
- Qualification evidence 9: SVU with bounded memory. A common streaming baseline is sliding-window KV caching, which guarantees constant memory but can discard decisive cues when queries arrive after long and unpredictable delays. InfiniPot-V (Kim et al. , 2025 ) extends this regime with online token-retention/compression under a fixed cap, mitigating outright eviction yet still representing far history as a subset of token instances — a...
- Qualification evidence 10: In this work, we address streaming video understanding under strict memory budgets, where decisive evidence can appear briefly but must remain usable after many subsequent updates before a query arrives. We propose ProtoKV, a constant-footprint KV memory that keeps an exact near-window cache while representing far history as an object-centric prototype bank with lightweight residual statistics. At query time,...
- Qualification evidence 11: The main paper retains the fixed- S S design because it instantiates ProtoKV’s summary-state interface with the smallest set of moving parts: a uniform per-prototype representation, no extra allocation policy, and a query-time context length that is determined purely by the static configuration ( W , K max , S ) (W,K_{\max},S) . The variance-aware variant above is therefore best read as evidence that the allocation...
- Qualification evidence 12: Case 3 (failure, Action Order): order errors among visually similar events.
- Qualification evidence 13: Case 4 (failure, Action Count): repeated visually similar events.

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.[^methodology]

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.26762v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: ProtoKV: Streaming Video Understanding under Delayed Query with Summary-State Memory
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: 2 Background and Problem Setup
- Source section 5: 2.1 KV Growth as an Operational Constraint in SVU
- Source section 6: 2.2 Bounded-Memory SVU and Prior Approaches
- Source section 7: 2.3 Query Delay and Post-Evidence Update Pressure
- Source section 8: 3 ProtoKV
- Source section 9: 3.1 State representation and prototype bank
- Source section 10: 3.2 Online update: eviction, assignment, and prototype maintenance
- Source section 11: 3.3 Residual Statistics and Pseudo-Token Interface
- Source section 12: 4 Experiments
- Source section 13: 4.1 Experimental Setup
- Source section 14: 4.2 Main Benchmark - SVU
- Source section 15: 4.3 Query-Delay Robustness
- Source section 16: 4.4 Ablation Study
- Source section 17: 4.5 Auxiliary Evaluation on Offline Video Understanding
- Source section 18: 4.6 Task-Type Analysis
- Source section 19: 4.7 System Validation
- Source section 20: 5 Related Work
- Source section 21: 6 Conclusion
- Source section 22: Acknowledgement
- Source section 23: Impact Statement
- Source section 24: References
- Source section 25: Appendix A ProtoKV Method Details
- Source section 26: A.1 Streaming Memory State and Residual Representation
- Source section 27: Residual statistics.
- Source section 28: PQ codebook initialization.
- Source section 29: A.2 Streaming Update Procedure
- Source section 30: A.3 Query-Time Pseudo-Token Synthesis
- Source section 31: Bounded attention context.
- Source section 32: Appendix B Configuration and Hyperparameters
- Source section 33: B.1 Default Hyperparameter Configuration
- Source section 34: Scaling rule for the memory budget.
- Source section 35: Additional implementation constants.
- Source section 36: B.2 Hyperparameter Sensitivity
- Source section 37: Budget allocation.
- Source section 38: Update and maintenance coefficients.
- Source section 39: Appendix C Experimental Setting Details
- Source section 40: C.1 Dataset Selection for Query-Delay Sweep
- Source section 41: Delayed-query protocol and validity constraint.
- Source section 42: Retrospective query types per benchmark.
- Source section 43: Included vs. excluded examples.
- Source section 44: C.2 Implementation Setup
- Source section 45: Baseline configurations.
- Source section 46: Raw sequence lengths.
- Source section 47: C.3 Online Update Overhead
- Source section 48: Setup.
- Source section 49: Where the update time goes.
- Source section 50: Why this does not affect query-time TTFT.
- Source section 51: Appendix D Additional Experiments
- Source section 52: D.1 RoPE Position Anchor Comparison
- Source section 53: D.2 MLVU Task-Type Breakdown
- Source section 54: D.3 Dynamic Pseudo-Token Allocation
- Source section 55: Appendix E Qualitative Case Studies

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1 : Query delay in streaming video understanding. When the delay exceeds the near window, answering requires information preserved in far memory.
- Visual/table evidence 2: Figure 2 : ProtoKV maintains an exact near-window KV buffer for recent tokens and, as the video stream continues, continuously aggregates evicted history into a fixed-capacity, object-centric prototype bank via online assignment. At query time, it synthesizes a bounded number of pseudo-KV tokens per prototype and concatenates them with the near-window tokens, yielding a fixed-length sequence that can be consumed by...
- Visual/table evidence 3: Table 1 : Main benchmark performance on SVU benchmarks. We use token budget | M | |M| = 24k tokens for RVS-Ego and RVS-Movie, and | M | |M| = 4k tokens for OVO-Bench and StreamingBench. For StreamingBench, we evaluate only Real-time visual understanding queries. Score columns are on a 0-5 scale.
- Visual/table evidence 4: Figure 3 : Query-delay robustness under matched token budgets in the online SVU setting. We apply a delayed-query sweep by setting t q = t 0 + Δ t_{q}=t_{0}+\Delta (minutes) and retain only retrospective query types whose answers are invariant to shifting t q t_{q} . For fair comparisons across Δ \Delta , we evaluate on the intersection subset that is valid for all delay values. We use token budget | M | |M| = 24k...
- Visual/table evidence 5: Table 2 : Component ablations under matched peak GPU memory ( | M | |M| = 24k, model: LLaVA-OV-7B, dataset: RVS-Ego). Evaluated on the same intersection subset used in Figure 3 .
- Visual/table evidence 6: Table 3 : Performance on offline video understanding benchmark (OVU) under fixed memory budgets. * denotes the numbers from official paper. We use Qwen2-VL-7B in this table for alignment with prior OVU baselines.
- Visual/table evidence 7: (a) Memory budget
- Visual/table evidence 8: (b) Query-time latency
- Visual/table evidence 9: Figure 4 : Budget sensitivity and query-time TTFT under memory constraints. (a) Accuracy versus memory budget. (b) Query-time TTFT (query arrival → first token) under a matched memory budget. DyCoke is shown as a query-conditioned reference.
- Visual/table evidence 10: Table 4 : Comparison to an SVU offloading/retrieval baseline on RVS-Ego under matched peak GPU memory with LLaVA-OV-7B. TTFT (query arrival → \rightarrow first token) and E2E latency (query arrival → \rightarrow completion) are measured.
- Visual/table evidence 11: Algorithm 1 ProtoKV Streaming Update
- Visual/table evidence 12: Algorithm 2 ProtoKV Prototype Maintenance
- Visual/table evidence 13: Algorithm 3 DecodeTopSResidualModes
- Visual/table evidence 14: Algorithm 4 ProtoKV Query-Time Pseudo-Token Decoding and Attention
- Visual/table evidence 15: Table 5 : Default hyperparameter configuration used in all main experiments. The same values are used across all six benchmarks; only | M | |M| varies across evaluation settings, which scales W W and K max K_{\max} as described in the text.
- Visual/table evidence 16: Table 6 : Sensitivity to budget allocation under fixed | M | = 24 ​ k |M|=24\text{k} on Qwen2.5-VL-7B / RVS-Ego. Accuracy is reported at query delays Δ = 0 \Delta=0 and Δ = 30 \Delta=30 minutes. Top: varying the near-window vs. far-memory split under a fixed total budget. Bottom: varying the pseudo-token count S S per prototype with K max ⋅ S K_{\max}\cdot S held fixed.
- Visual/table evidence 17: Table 7 : Sensitivity to update and maintenance coefficients on RVS-Ego and RVS-Movie under the default budget and Qwen2.5-VL-7B. Entries are accuracy changes (points) relative to the default setting at Δ = 0 \Delta=0 . Default accuracy at Δ = 0 \Delta=0 is 61.0 61.0 on RVS-Ego and 53.3 53.3 on RVS-Movie.
- Visual/table evidence 18: Table 8 : Examples of included and excluded queries for the query-delay sweep, paired by benchmark to highlight the contrast. Included queries are retained because their ground truth is determined by past video content and is invariant to a query-time shift; excluded queries reference the current moment and would change ground truth under the shift.
- Visual/table evidence 19: Table 9 : Accuracy under three RoPE position-anchor choices for τ k \tau_{k} on Qwen2.5-VL-7B / RVS-Ego under the default memory budget. All three variants share the same per-prototype anchor across the S S pseudo-tokens; they differ only in which absorbed position the anchor tracks.
- Visual/table evidence 20: Table 10 : ProtoKV’s per-task accuracy on MLVU under matched memory budget ( | M | = 6 ​ k |M|=6\text{k} ) and Qwen2-VL-7B. Task abbreviations: Order = Action Order, Count = Action Count, AR = Anomaly Recognition, Needle = Needle QA, Ego = Ego Reasoning, TR = Topic Reasoning. “Avg.” is the official MLVU M-Avg score across the seven tasks.
- Visual/table evidence 21: Table 11 : Variance-aware pseudo-token allocation versus the fixed default ( S = 8 S=8 per prototype) under matched total budget S tot = K max ⋅ S S_{\mathrm{tot}}=K_{\max}\cdot S on Qwen2.5-VL-7B / RVS-Ego.

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2606.26762v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2606.26762v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2606.26762v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2606.26762v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2606.26762
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
