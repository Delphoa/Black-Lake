# Nautilus Compass Black box: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “Nautilus Compass: Black-box Persona Drift Detection for Production LLM Agents”

**Source paper:** Wang, Chunxiao, “Nautilus Compass: Black-box Persona Drift Detection for Production LLM Agents,” arXiv:2605.09863v1.[^source-paper]

**Artifact prepared:** 2026-07-29  
**Local source reviewed:** complete PDF (19 pages) and matching full-paper HTML (58008 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **Nautilus Compass: Black-box Persona Drift Detection for Production LLM Agents** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Production LLM coding agents drift over long sessions: they forget user-specified constraints, slip into mistakes the user already flagged, and confabulate prior agreements. White-box approaches such as persona vectors require model weights and so cannot be applied to closed APIs (Claude, GPT-4) that most users actually interact with.
- Source passage 3: We present Nautilus Compass , a black-box persona drift detector and agent memory layer for production coding agents. The method operates entirely at the prompt-text layer: cosine similarity between user prompts and behavioral anchor texts, aggregated by a weighted top- k k mean using BGE-m3 embeddings.
- Source passage 4: On a held-out test set built from real Claude Code session traces and labeled by an independent LLM judge, Compass reaches ROC AUC 0.83 for drift detection. The embedded retrieval pipeline scores 56.6% on LongMemEval-S v0.8 and 44.4% on EverMemBench-Dynamic (n=500), topping the four published EverMemBench Table 4 baselines.
- Source passage 5: Code, anchors, frozen test data, and audit-log tooling are MIT-licensed at github.com/chunxiaoxx/nautilus-compass .
- Source passage 6: Modern coding agents based on Large Language Models (LLMs) such as Claude Code, Cursor, and Continue.dev sustain dialogue sessions spanning hundreds to thousands of turns. Within such sessions, a recurring failure mode is persona drift : the assistant gradually forgets user-specified constraints (“always verify before claiming success”), slips into bad habits the user explicitly flagged earlier (“don’t fabricate prior agreements”), or confabulates having reached agreements that were never made.
- Source passage 7: (a) Memory plugins like mem0 (Mem0 Team, 2025 ) , Letta (Packer et al. , 2023 ) , and Zep (Rasmussen et al.
- Source passage 8: We present Nautilus Compass , a black-box persona drift detection system that runs entirely in user-space hooks of an LLM coding agent. The reference implementation ships as a Claude Code plugin and as a generic MCP 2024-11-05 server (compatible with Cursor, Cline, Hermes, OpenClaw, and any other MCP client), with a CLI and a REST endpoint backed by the same BGE-m3 daemon—a single embedder service amortized across every entry point.
- Source passage 9: Our central finding is that the design of the anchor set, not the choice of embedder or aggregation function, is the dominant factor determining detection accuracy. We document a four-step iteration that improves ROC AUC from 0.51 (random) to 0.92 on a 100-prompt synthetic test set (Figure 1 ), and report negative results (e.g., that substituting a cross-encoder reranker for the bi-encoder cosine yields no measurable improvement at 36 × 36\times the latency).
- Source passage 10: All code, anchor sets, evaluation scripts, and benchmark data are released under MIT (anchors under CC0) at https://github.com/chunxiaoxx/nautilus-compass . The four-step ablation, the head-to-head comparison with mem0 on LongMemEval-S, and the negative cross-encoder result are all reproducible from the repository in under one hour on a CPU-only machine.

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 185 extractable mathematical objects, 78 substantive headings, and 15 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: k
- Equation or mathematical object 2: \times
- Equation or mathematical object 3: k
- Equation or mathematical object 4: 36\times
- Equation or mathematical object 5: 0.5056
- Equation or mathematical object 6: 0.9232
- Equation or mathematical object 7: p<0.05
- Equation or mathematical object 8: n=120
- Equation or mathematical object 9: \times
- Equation or mathematical object 10: k
- Equation or mathematical object 11: k
- Equation or mathematical object 12: k
- Equation or mathematical object 13: k
- Equation or mathematical object 14: k
- Equation or mathematical object 15: k=3
- Equation or mathematical object 16: k
- Equation or mathematical object 17: k
- Equation or mathematical object 18: q
- Equation or mathematical object 19: \mathcal{A}=\mathcal{A}^{+}\cup\mathcal{A}^{-}
- Equation or mathematical object 20: w_{a}

### 2.2 Method and information flow

- Method evidence 1: Production LLM coding agents drift over long sessions: they forget user-specified constraints, slip into mistakes the user already flagged, and confabulate prior agreements. White-box approaches such as persona vectors require model weights and so cannot be applied to closed APIs (Claude, GPT-4) that most users actually interact with.
- Method evidence 2: We present Nautilus Compass , a black-box persona drift detector and agent memory layer for production coding agents. The method operates entirely at the prompt-text layer: cosine similarity between user prompts and behavioral anchor texts, aggregated by a weighted top- k k mean using BGE-m3 embeddings. Compass is, to our knowledge, the only public agent memory layer (among Mem0, Letta, Cognee, Zep, MemOS, smrti...
- Method evidence 3: (a) Memory plugins like mem0 (Mem0 Team, 2025 ) , Letta (Packer et al. , 2023 ) , and Zep (Rasmussen et al. , 2025 ) focus on retrieval quality: finding the right memory entries to inject into the prompt. While these systems produce strong retrieval metrics (e.g., on LongMemEval (Wu et al. , 2024 ) ), they leave open the question of whether the LLM honors the retrieved content. (b) White-box safety methods ,...
- Method evidence 4: This paper makes a claim about detection accuracy : given a user-issued prompt and a curated anchor set, our method classifies whether the prompt aligns with task-shaped positive patterns or deviates toward known failure modes, with held-out AUC reported in Section 4.7 . We additionally report a cross-vendor behavior-steering A/B (Section 6 ) across six production LLMs (Google Gemini-2.5 {pro, flash}, MiniMax M2.7,...
- Method evidence 5: A growing line of work addresses the limited context window of LLMs through external memory. mem0 (Mem0 Team, 2025 ) stores key-value memories extracted from conversations and retrieves them via dense embeddings; the system targets production agent use cases and reports retrieval Recall@5 in the 0.5–0.6 range on LongMemEval-S. Letta (formerly MemGPT) (Packer et al. , 2023 ) introduces a hierarchical memory...
- Method evidence 6: The chief limitation, from the perspective of the typical end-user of a coding agent, is that this method requires access to model weights or hidden states. This puts it out of reach of users interacting with proprietary LLMs (Claude, GPT-4, Gemini) through API or product surfaces. Our work targets exactly this deployment gap: providing a black-box analog that runs in user-space hooks without model-internal access.
- Method evidence 7: We build on the BGE family of embedding models (Chen et al. , 2024 ) . BGE-m3 in particular offers multilingual support across 100+ languages with a 1024-dimensional output, making it suitable for mixed Chinese/English production agent contexts. For retrieval reranking, we leverage bge-reranker-v2-m3 (BAAI, 2024 ) , a cross-encoder trained on similar data, which scores (query, candidate) pairs jointly rather than...
- Method evidence 8: Our anchor-based approach draws conceptual lineage from several threads. Reward modeling in RLHF aggregates many preference comparisons into a scalar reward; in our case we aggregate cosine similarity to many positive/negative behavioral anchors into a scalar drift score. Behavioral cloning in robotics learns from demonstrations; our positive anchors function as “demonstrations of correct task patterns”. Few-shot...
- Method evidence 9: The core innovation in our system is not any single technical component but the empirical methodology for designing anchor sets that yield strong detection signal (Section 3.9 ), together with the integration with hook-level production deployment.
- Method evidence 10: We discovered this principle empirically (Section 3.9 documents the ablation): rewriting an entire anchor set from maxim form to task-shaped form moves drift ROC AUC from 0.51 (coin toss) to 0.79 with no other change. We surface this finding as a design principle here because the size of the effect—driven by grammatical alignment rather than semantic content—is the load-bearing methodological insight of our work and...
- Method evidence 11: An eval gate runs the full drift evaluation (Section 4.2 ) against both the original and proposed adapted anchor sets. The gate accepts the proposal only if the AUC delta exceeds + 0.005 +0.005 ; rejects on Δ < − 0.01 \Delta<-0.01 ; flags as “marginal” otherwise. This prevents silent regressions.
- Method evidence 12: We caution that production deployment of these starter profiles should involve review by domain experts; they serve primarily to demonstrate the methodology generalizes across domains rather than as drop-in production assets.
- Method evidence 13: Step 4 incorporates patterns from misclassified test prompts back into the anchor set; the resulting 0.9232 AUC on the original 100-prompt set is therefore an in-set number rather than a generalization estimate. To mitigate this, Section 4.7 reports performance on a held-out test set generated from real Claude Code session traces and labeled by an independent LLM judge (Vertex AI Gemini); the held-out AUC is the...
- Method evidence 14: We will publish held-out AUC numbers on the project repository as part of the v0.7.1 reproducibility checkpoint; this paper version provides the methodology and frozen test data, with empirical held-out numbers to follow before the full-version journal submission.
- Method evidence 15: Chen et al. ( 2025 ) ’s white-box approach has one major advantage and one major disadvantage relative to ours.
- Method evidence 16: The method requires model weights and (typically) a training set of contrastive prompts to extract the persona vectors. End-users of proprietary LLMs (Claude, GPT-4, Gemini) have neither.
- Method evidence 17: The takeaway: do not draw conclusions from n = 4 n=4 retrieval benchmarks , especially when the dataset has known per-type variance (Wu et al. , 2024 ) . We were initially confident in the null because the experimental setup was correct (clean control, deterministic pipeline); the failure was statistical rather than methodological.
- Method evidence 18: We expect the labeling burden to follow a power law: the first 10 labels yield large gains; the next 100 yield diminishing incremental AUC; beyond 1000 labels the marginal gain approaches zero. Verifying this empirically is part of the long-term deployment study.
- Method evidence 19: The empirical contribution of this paper combines (a) drift detection accuracy —does our system correctly classify a prompt (Sections 4.2 , 4.7 )—and (b) behavior steering , a separate empirical question: does an LLM, when given the drift alert as additional context, actually behave more safely on the same deviation prompt. We address (b) with an A/B evaluation across six production LLMs from six different vendors,...
- Method evidence 20: Honest framing. These numbers establish that drift injection produces a specific, statistically detectable improvement on fabrication resistance , while leaving verify/destruct/secret essentially unchanged on aggregate. Reported AUC for detection still must not be interpreted as a guarantee of behavior change overall; the effect is axis-specific and modest. We treat this as preliminary evidence motivating two...

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: Nautilus Compass: Black-box Persona Drift Detection for Production LLM Agents
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: Existing approaches fall into two categories.
- Source section 5: Our contribution.
- Source section 6: Methodological insight.
- Source section 7: Open source and reproducibility.
- Source section 8: Relation to Persona Vectors.
- Source section 9: Scope and limits of claims.
- Source section 10: 2 Related Work
- Source section 11: 2.1 Memory Systems for LLM Agents
- Source section 12: 2.2 White-box Persona Monitoring
- Source section 13: 2.3 Black-box Safety and Behavior Classifiers
- Source section 14: 2.4 Embedders and Cross-Encoder Rerankers
- Source section 15: 2.5 Long-Context Memory Benchmarks
- Source section 16: 2.6 Anchor-Based Behavior Detection (Analogies)
- Source section 17: 2.7 Strategy Distillation
- Source section 18: 3 Method
- Source section 19: 3.1 Architecture Overview
- Source section 20: 3.2 Anchor Schema
- Source section 21: 3.3 Design Principle: Task-shaped Authoring
- Source section 22: 3.4 Drift Scoring: Weighted Top- k k Mean
- Source section 23: 3.5 Tri-band Output
- Source section 24: 3.6 Adaptive Learning Loop
- Source section 25: 3.7 False-Positive Filter
- Source section 26: 3.8 Cross-Domain Anchor Profiles
- Source section 27: 3.9 Empirical Ablation: Four-step Evolution
- Source section 28: Step 1: From abstract maxims to task-shaped anchors (§ 3.3 ).
- Source section 29: Step 2: From centroid mean to weighted top- k k mean (§ 3.4 ).
- Source section 30: Step 3: From bge-small-zh-v1.5 to bge-m3.
- Source section 31: Step 4: Adding hard false-positive examples to negative anchors.
- Source section 32: Train-test contamination caveat.
- Source section 33: 4 Evaluation
- Source section 34: 4.1 Setup
- Source section 35: Drift detection test set.
- Source section 36: LongMemEval-S.
- Source section 37: Metrics.
- Source section 38: 4.2 Drift Detection: Four-step Methodology Ablation
- Source section 39: Step 1 commentary.
- Source section 40: Step 4 commentary.
- Source section 41: Score distribution.
- Source section 42: 4.3 Cross-Encoder Drift Scoring: Null Result
- Source section 43: 4.4 Retrieval: LongMemEval-S Full 500
- Source section 44: Subset 12 vs. full 500.
- Source section 45: 4.5 mem0 Head-to-Head (Subset 12)
- Source section 46: Per-type lift.
- Source section 47: Compared to mem0.
- Source section 48: Why subset 12 for mem0.
- Source section 49: 4.6 Local Recall Sanity Check
- Source section 50: 4.7 Held-out Test Set: Generalization Beyond Self-Authored Prompts
- Source section 51: 4.8 Baselines
- Source section 52: 4.9 Latency
- Source section 53: 5 Discussion
- Source section 54: 5.1 Anchor Design as the Dominant Variable
- Source section 55: 5.2 Black-box vs. White-box Persona Monitoring
- Source section 56: Advantage of white-box.
- Source section 57: Disadvantage of white-box.
- Source section 58: Complementarity.
- Source section 59: 5.3 Why Reranker Helps Retrieval but Not Drift
- Source section 60: 5.4 The Subset 4 vs. Subset 12 Lesson
- Source section 61: 5.5 Active Learning in Practice
- Source section 62: 5.6 Implications for Production Deployment
- Source section 63: 6 Limitations and Future Work
- Source section 64: Behavior steering: preliminary cross-vendor evidence.
- Source section 65: Train-on-test concern in the in-set ablation.
- Source section 66: Synthetic drift test set.
- Source section 67: Single-session-user retrieval ceiling.
- Source section 68: Subset 12 for mem0 only.
- Source section 69: Cross-domain anchor profiles need expert review.
- Source section 70: Daemon caches a single anchor profile.
- Source section 71: Windows m3 cold-load failure modes.
- Source section 72: Future directions.
- Source section 73: 7 Open Source and Reproducibility
- Source section 74: 7.1 Repository
- Source section 75: 7.2 Reproducibility
- Source section 76: 7.3 Continuous Integration
- Source section 77: 7.4 Acknowledgments
- Source section 78: References

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: Our central finding is that the design of the anchor set, not the choice of embedder or aggregation function, is the dominant factor determining detection accuracy. We document a four-step iteration that improves ROC AUC from 0.51 (random) to 0.92 on a 100-prompt synthetic test set (Figure 1 ), and report negative results (e.g., that substituting a cross-encoder reranker for the bi-encoder cosine yields no...
- Evaluation evidence 2: All code, anchor sets, evaluation scripts, and benchmark data are released under MIT (anchors under CC0) at https://github.com/chunxiaoxx/nautilus-compass . The four-step ablation, the head-to-head comparison with mem0 on LongMemEval-S, and the negative cross-encoder result are all reproducible from the repository in under one hour on a CPU-only machine.
- Evaluation evidence 3: This paper makes a claim about detection accuracy : given a user-issued prompt and a curated anchor set, our method classifies whether the prompt aligns with task-shaped positive patterns or deviates toward known failure modes, with held-out AUC reported in Section 4.7 . We additionally report a cross-vendor behavior-steering A/B (Section 6 ) across six production LLMs (Google Gemini-2.5 {pro, flash}, MiniMax M2.7,...
- Evaluation evidence 4: A natural question is whether a cross-encoder can substitute for the bi-encoder cosine in our drift detection step (anchor matching). In Section 4.3 , we report a negative result: cross-encoder substitution improves drift AUC by only 0.0008 at 36 × \times the latency, suggesting that the short, semantically heterogeneous nature of behavioral anchor text leaves little token-interaction signal for cross-encoders to...
- Evaluation evidence 5: We evaluate retrieval performance using LongMemEval-S (Wu et al. , 2024 ) , which provides 500 question-haystack pairs across six question types: knowledge update, multi-session synthesis, single-session-user/assistant/preference factual recall, and temporal reasoning. Each haystack contains roughly 50 candidate sessions, making top-5 retrieval a meaningful target metric. Other relevant benchmarks include LoCoMo...
- Evaluation evidence 6: We discovered this principle empirically (Section 3.9 documents the ablation): rewriting an entire anchor set from maxim form to task-shaped form moves drift ROC AUC from 0.51 (coin toss) to 0.79 with no other change. We surface this finding as a design principle here because the size of the effect—driven by grammatical alignment rather than semantic content—is the load-bearing methodological insight of our work and...
- Evaluation evidence 7: An eval gate runs the full drift evaluation (Section 4.2 ) against both the original and proposed adapted anchor sets. The gate accepts the proposal only if the AUC delta exceeds + 0.005 +0.005 ; rejects on Δ < − 0.01 \Delta<-0.01 ; flags as “marginal” otherwise. This prevents silent regressions.
- Evaluation evidence 8: Our v0.5 anchor set consisted of declarative principles such as “simplicity over cleverness” and “verify before claiming done”. These maxims have an abstract grammatical shape that does not match the form of typical user prompts. Cosine similarity between maxim and prompt is consistently low across both aligned and deviation prompts, yielding ROC AUC 0.5056. Rewriting every anchor in task-shaped form moves AUC to...
- Evaluation evidence 9: Replacing the centroid-mean aggregator with weighted top- k k mean produced a marginal AUC gain (0.79 → \to 0.79+) but a qualitative improvement: alert provenance, since we know which anchors fired. We adopt top- k k as our default on the strength of the qualitative gain.
- Evaluation evidence 10: Our initial embedder was bge-small-zh-v1.5 (512 dim, Chinese-focused). This embedder yields strong intra-language retrieval (MRR 0.918 on local Chinese corpus) but cannot represent English-language anchors and prompts effectively. Switching to bge-m3 (1024 dim, multilingual) yielded a further AUC bump to 0.8352 , primarily from improved separation on cross-lingual prompt/anchor pairs.
- Evaluation evidence 11: Step 4 incorporates patterns from misclassified test prompts back into the anchor set; the resulting 0.9232 AUC on the original 100-prompt set is therefore an in-set number rather than a generalization estimate. To mitigate this, Section 4.7 reports performance on a held-out test set generated from real Claude Code session traces and labeled by an independent LLM judge (Vertex AI Gemini); the held-out AUC is the...
- Evaluation evidence 12: For retrieval evaluation we use LongMemEval-S (Wu et al. , 2024 ) , which provides 500 question–haystack pairs across six question types: knowledge-update (78 questions), multi-session (133), single-session-user (70), single-session-assistant (56), single-session-preference (30), and temporal-reasoning (133). Each haystack contains roughly 50 candidate sessions, of which 1–2 are “ground-truth” sessions known to...
- Evaluation evidence 13: Table 1 reports the four-step ablation described in Section 3.9 . Each row is a controlled change with all other variables fixed. The progression takes ROC AUC from random ( 0.5056 0.5056 ) to strong ( 0.9232 0.9232 ).
- Evaluation evidence 14: Figure 2 shows the resulting drift score distributions for aligned and deviation prompts. The two distributions have substantial separation around the calibrated threshold drift = − 0.032 \mathrm{drift}=-0.032 (best Youden’s J), but with overlapping tails that account for the residual error rate.
- Evaluation evidence 15: Given the success of cross-encoder reranking in retrieval pipelines (BAAI, 2024 ) , we hypothesized that substituting bge-reranker-v2-m3 for the bi-encoder cosine in the drift score (i.e., scoring each (prompt, anchor) pair jointly) would further improve detection AUC. Empirically, this is not the case (Table 2 ).
- Evaluation evidence 16: We attribute this null result to a structural difference between retrieval and drift contexts: in retrieval, candidates are long passages where token-level interaction provides genuine signal; in drift detection, anchors are short ( ≤ 30 \leq 30 tokens) and semantically heterogeneous, leaving little for cross-encoders to exploit beyond what bi-encoders already capture.
- Evaluation evidence 17: The 36 × 36\times latency cost makes cross-encoder substitution inappropriate for hook deployment regardless of accuracy. We publish this null result to save practitioners the implementation effort.
- Evaluation evidence 18: Table 3 reports retrieval metrics for the bge-m3 bi-encoder pipeline on the full 500 LongMemEval-S benchmark, with the per-question-type breakdown in Table 4 . Total runtime was 2h 49min on a single CPU-only Windows host.
- Evaluation evidence 19: The reranker re-evaluation on the full 500 (using GPU acceleration on a GTX 1060 6 GB, 67 min wall clock) closes the loop on the subset-12 reranker-lift result reported in Section 4.5 below: the same + 0.10 +0.10 + MRR gain seen on subset 12 generalizes to + 0.170 +0.170 on the full 500, with no ceiling artifact and consistent direction across all 6 question types (except single-session-assistant, which is already...
- Evaluation evidence 20: We previously reported subset-12 numbers ( n = 12 n=12 , 2 per type) during development: P@5 = 0.750 \,=\,0.750 , MRR = 0.732 \,=\,0.732 . The full-500 results show higher P@5 ( 0.860 0.860 , + 0.110 +0.110 ) but slightly lower MRR ( 0.685 0.685 , − 0.047 -0.047 ). The discrepancy is fully explained by the fact that the full 500 includes 70 single-session-user questions ( 14 % 14\% of the benchmark) at MRR = 0.398...
- Evaluation evidence 21: The reranker lift is concentrated on the question types where bi-encoder alone is weakest (Figure 4 ): single-session-user sees MRR rise from 0.091 to 0.522 ( ∼ 5 × \sim 5\times ); multi-session sees MRR rise from 0.55 to 0.75. Question types where the bi-encoder already achieves perfect P@5 see no further lift, consistent with the per-type results in Table 4 .
- Evaluation evidence 22: The mem0 head-to-head requires resetting and re-populating mem0’s index per question (approximately 30 s/question of Vertex AI calls), making full-500 prohibitive. This subset, with balanced per-type sampling, allows a reproducible direct comparison within a practical compute budget. The reranker numbers, in contrast, are now reported on the full 500 (Table 4 ); the n=12 ablation P@5 = 0.917 =0.917 and MRR = 0.837...
- Evaluation evidence 23: This is a comfortably high baseline indicating the embedder is not the limiting factor in our retrieval results; the gaps in LongMemEval-S come from the difficulty of the question types, not embedder quality.
- Evaluation evidence 24: The four-step ablation reported in Table 1 is on a 100-prompt set authored by the authors. To address train-test contamination concerns (Section 3.9 ), we additionally constructed a frozen held-out test set:
- Evaluation evidence 25: This construction has three properties relevant to train-test isolation: (i) prompts come from real production traces, not authored alongside anchors; (ii) labels come from an independent LLM judge, not the anchor authors; (iii) the resulting JSON file is committed to the repository with a frozen-at timestamp and a warning that anchor tuning may never reference it.
- Evaluation evidence 26: We acknowledge that LLM-as-judge labeling is itself a noisy signal; we treat held-out AUC as a complementary metric to the in-set ablation rather than a definitive replacement. The proper end-to-end test (does drift alert injection cause behavior change?) is described in Section 6 .
- Evaluation evidence 27: The takeaway: do not draw conclusions from n = 4 n=4 retrieval benchmarks , especially when the dataset has known per-type variance (Wu et al. , 2024 ) . We were initially confident in the null because the experimental setup was correct (clean control, deterministic pipeline); the failure was statistical rather than methodological.
- Evaluation evidence 28: Our active learning module surfaces alerts whose drift score is near the decision boundary ( | drift | < 0.05 |\mathrm{drift}|<0.05 ) for preferential user labeling. The theoretical case is straightforward: labels near the boundary disambiguate the largest amount of uncertainty. In practice, the long-term value of this loop depends on user willingness to label, which we have not yet measured at scale (current...
- Evaluation evidence 29: We highlight seven known limitations of nautilus-compass v0.7.1 and the empirical evaluation in this paper. The first is the most consequential and should be read carefully.
- Evaluation evidence 30: The empirical contribution of this paper combines (a) drift detection accuracy —does our system correctly classify a prompt (Sections 4.2 , 4.7 )—and (b) behavior steering , a separate empirical question: does an LLM, when given the drift alert as additional context, actually behave more safely on the same deviation prompt. We address (b) with an A/B evaluation across six production LLMs from six different vendors,...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1: Drift detection ROC AUC across the four-step methodology evolution. Each step is a controlled change with all other variables fixed; aggregate improvement is from random ( 0.5056 0.5056 ) to strong ( 0.9232 0.9232 ). The largest single jump comes from rewriting abstract maxim anchors as task-shaped sentences matching the prompt distribution.
- Visual/table evidence 2: Figure 2: System architecture. A user-issued prompt enters UserPromptSubmit , which forks three parallel paths: top- k k memory recall, drift scoring against behavioral anchors, and DPT-style strategy retrieval. The combined output is injected back into the LLM’s system prompt for the same turn.
- Visual/table evidence 3: Table 1: Four-step drift detection ablation. All evaluated on the same 50/50 prompt test set (Section 4.1 ). Top-3 mean (step 2) provides marginal AUC gain but qualitatively sharper alert provenance: we know which anchor fired.
- Visual/table evidence 4: Figure 3: Drift score distribution for the v0.7.1 final configuration. 2 2 2 Evaluation snapshot is v0.7.1; current code release is v1.0.0 (2026-05-08). The compass surfaces evaluated here (drift detector, BGE+rerank pipeline, anchor schema) are unchanged from v0.7.1 to 1.0.0. Aligned prompts (green) cluster around + 0.06 +0.06 ; deviation prompts (red) cluster around − 0.05 -0.05 . The decision threshold (best...
- Visual/table evidence 5: Table 2: Cross-encoder drift scoring vs. bi-encoder cosine top-3 mean, evaluated on the same 100-prompt test set.
- Visual/table evidence 6: Table 3: LongMemEval-S retrieval on the full 500 questions. Reranker is bge-reranker-v2-m3 (cross-encoder) applied to top-50 bi-encoder candidates, returning top-5. GPU run: bi-encoder 169 min CPU and reranker eval 67 min on GTX 1060 6 GB.
- Visual/table evidence 7: Table 4: Per-question-type breakdown on the full 500. Bi-encoder vs. bi-encoder + bge-reranker-v2-m3. The reranker dramatically improves the hardest types ( single-session-user MRR 0.398 → 0.586 0.398\rightarrow 0.586 , single-session-preference 0.537 → 0.788 0.537\rightarrow 0.788 , knowledge-update 0.635 → 0.848 0.635\rightarrow 0.848 ) while leaving already-saturated types unchanged.
- Visual/table evidence 8: Table 5: LongMemEval-S subset 12 retrieval. Same 12 questions (2 per type), three retrieval pipelines.
- Visual/table evidence 9: Figure 4: Reranker MRR lift on LongMemEval-S subset 12 by question type. Hardest question type (single-session-user) sees 5 × 5\times MRR improvement; easier types are already near ceiling for bi-encoder alone.
- Visual/table evidence 10: Figure 5: Per-question-type MRR on LongMemEval-S subset 12. nautilus-compass (m3+rerank) vs. mem0 (Vertex text-embedding-005). Same dataset, same 12 questions, infer=False for mem0. Largest gaps: single-session-user ( + 0.27 +0.27 MRR for us) and temporal-reasoning ( + 0.25 +0.25 ).
- Visual/table evidence 11: Table 6: Drift detection baselines on 100-prompt synthetic test set (same set as the four-step ablation). The zero-shot baseline uses a generic deviation keyword list as anchors instead of curated task-shaped anchors, isolating the contribution of anchor curation from embedder strength.
- Visual/table evidence 12: Table 7: Hot-path latency per UserPromptSubmit hook invocation, warm daemon, CPU-only.
- Visual/table evidence 13: Table 8: Behavior steering A/B by subject LLM. Each row averages 20 paired prompts judged by Moonshot Kimi-k2.6 on four axes. “W/T/L” counts prompts where the prompt-level mean of B exceeds A by > 0.05 >0.05 (win), is within ± 0.05 \pm 0.05 (tie), or is below A by > 0.05 >0.05 (loss).
- Visual/table evidence 14: Table 9: Per-axis A/B effect, pooled across all 6 subjects ( n = 120 n=120 ).
- Visual/table evidence 15: Figure 6: Cross-vendor behavior steering A/B heatmap. Each cell is the Δ \Delta judge score (B with drift alert minus A without) for one subject LLM × one behavioral axis. Significance markers from per-subject paired t t -tests: · p < 0.10 p<0.10 , ⋆ \star p < 0.05 p<0.05 , ⋆ ⁣ ⋆ \star\star p < 0.01 p<0.01 (df = 19 per cell). Fabrication-resistance is positive across 5 of 6 subjects; verify and destruct show notable...

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: Our central finding is that the design of the anchor set, not the choice of embedder or aggregation function, is the dominant factor determining detection accuracy. We document a four-step iteration that improves ROC AUC from 0.51 (random) to 0.92 on a 100-prompt synthetic test set (Figure 1 ), and report negative results (e.g., that substituting a cross-encoder reranker for the bi-encoder cosine yields no...
- Evaluation evidence 2: All code, anchor sets, evaluation scripts, and benchmark data are released under MIT (anchors under CC0) at https://github.com/chunxiaoxx/nautilus-compass . The four-step ablation, the head-to-head comparison with mem0 on LongMemEval-S, and the negative cross-encoder result are all reproducible from the repository in under one hour on a CPU-only machine.
- Evaluation evidence 3: This paper makes a claim about detection accuracy : given a user-issued prompt and a curated anchor set, our method classifies whether the prompt aligns with task-shaped positive patterns or deviates toward known failure modes, with held-out AUC reported in Section 4.7 . We additionally report a cross-vendor behavior-steering A/B (Section 6 ) across six production LLMs (Google Gemini-2.5 {pro, flash}, MiniMax M2.7,...
- Evaluation evidence 4: A natural question is whether a cross-encoder can substitute for the bi-encoder cosine in our drift detection step (anchor matching). In Section 4.3 , we report a negative result: cross-encoder substitution improves drift AUC by only 0.0008 at 36 × \times the latency, suggesting that the short, semantically heterogeneous nature of behavioral anchor text leaves little token-interaction signal for cross-encoders to...
- Evaluation evidence 5: We evaluate retrieval performance using LongMemEval-S (Wu et al. , 2024 ) , which provides 500 question-haystack pairs across six question types: knowledge update, multi-session synthesis, single-session-user/assistant/preference factual recall, and temporal reasoning. Each haystack contains roughly 50 candidate sessions, making top-5 retrieval a meaningful target metric. Other relevant benchmarks include LoCoMo...
- Evaluation evidence 6: We discovered this principle empirically (Section 3.9 documents the ablation): rewriting an entire anchor set from maxim form to task-shaped form moves drift ROC AUC from 0.51 (coin toss) to 0.79 with no other change. We surface this finding as a design principle here because the size of the effect—driven by grammatical alignment rather than semantic content—is the load-bearing methodological insight of our work and...
- Evaluation evidence 7: An eval gate runs the full drift evaluation (Section 4.2 ) against both the original and proposed adapted anchor sets. The gate accepts the proposal only if the AUC delta exceeds + 0.005 +0.005 ; rejects on Δ < − 0.01 \Delta<-0.01 ; flags as “marginal” otherwise. This prevents silent regressions.
- Evaluation evidence 8: Our v0.5 anchor set consisted of declarative principles such as “simplicity over cleverness” and “verify before claiming done”. These maxims have an abstract grammatical shape that does not match the form of typical user prompts. Cosine similarity between maxim and prompt is consistently low across both aligned and deviation prompts, yielding ROC AUC 0.5056. Rewriting every anchor in task-shaped form moves AUC to...
- Evaluation evidence 9: Replacing the centroid-mean aggregator with weighted top- k k mean produced a marginal AUC gain (0.79 → \to 0.79+) but a qualitative improvement: alert provenance, since we know which anchors fired. We adopt top- k k as our default on the strength of the qualitative gain.
- Evaluation evidence 10: Our initial embedder was bge-small-zh-v1.5 (512 dim, Chinese-focused). This embedder yields strong intra-language retrieval (MRR 0.918 on local Chinese corpus) but cannot represent English-language anchors and prompts effectively. Switching to bge-m3 (1024 dim, multilingual) yielded a further AUC bump to 0.8352 , primarily from improved separation on cross-lingual prompt/anchor pairs.
- Evaluation evidence 11: Step 4 incorporates patterns from misclassified test prompts back into the anchor set; the resulting 0.9232 AUC on the original 100-prompt set is therefore an in-set number rather than a generalization estimate. To mitigate this, Section 4.7 reports performance on a held-out test set generated from real Claude Code session traces and labeled by an independent LLM judge (Vertex AI Gemini); the held-out AUC is the...
- Evaluation evidence 12: For retrieval evaluation we use LongMemEval-S (Wu et al. , 2024 ) , which provides 500 question–haystack pairs across six question types: knowledge-update (78 questions), multi-session (133), single-session-user (70), single-session-assistant (56), single-session-preference (30), and temporal-reasoning (133). Each haystack contains roughly 50 candidate sessions, of which 1–2 are “ground-truth” sessions known to...
- Evaluation evidence 13: Table 1 reports the four-step ablation described in Section 3.9 . Each row is a controlled change with all other variables fixed. The progression takes ROC AUC from random ( 0.5056 0.5056 ) to strong ( 0.9232 0.9232 ).
- Evaluation evidence 14: Figure 2 shows the resulting drift score distributions for aligned and deviation prompts. The two distributions have substantial separation around the calibrated threshold drift = − 0.032 \mathrm{drift}=-0.032 (best Youden’s J), but with overlapping tails that account for the residual error rate.
- Evaluation evidence 15: Given the success of cross-encoder reranking in retrieval pipelines (BAAI, 2024 ) , we hypothesized that substituting bge-reranker-v2-m3 for the bi-encoder cosine in the drift score (i.e., scoring each (prompt, anchor) pair jointly) would further improve detection AUC. Empirically, this is not the case (Table 2 ).
- Evaluation evidence 16: We attribute this null result to a structural difference between retrieval and drift contexts: in retrieval, candidates are long passages where token-level interaction provides genuine signal; in drift detection, anchors are short ( ≤ 30 \leq 30 tokens) and semantically heterogeneous, leaving little for cross-encoders to exploit beyond what bi-encoders already capture.
- Evaluation evidence 17: The 36 × 36\times latency cost makes cross-encoder substitution inappropriate for hook deployment regardless of accuracy. We publish this null result to save practitioners the implementation effort.
- Evaluation evidence 18: Table 3 reports retrieval metrics for the bge-m3 bi-encoder pipeline on the full 500 LongMemEval-S benchmark, with the per-question-type breakdown in Table 4 . Total runtime was 2h 49min on a single CPU-only Windows host.
- Evaluation evidence 19: The reranker re-evaluation on the full 500 (using GPU acceleration on a GTX 1060 6 GB, 67 min wall clock) closes the loop on the subset-12 reranker-lift result reported in Section 4.5 below: the same + 0.10 +0.10 + MRR gain seen on subset 12 generalizes to + 0.170 +0.170 on the full 500, with no ceiling artifact and consistent direction across all 6 question types (except single-session-assistant, which is already...
- Evaluation evidence 20: We previously reported subset-12 numbers ( n = 12 n=12 , 2 per type) during development: P@5 = 0.750 \,=\,0.750 , MRR = 0.732 \,=\,0.732 . The full-500 results show higher P@5 ( 0.860 0.860 , + 0.110 +0.110 ) but slightly lower MRR ( 0.685 0.685 , − 0.047 -0.047 ). The discrepancy is fully explained by the fact that the full 500 includes 70 single-session-user questions ( 14 % 14\% of the benchmark) at MRR = 0.398...
- Evaluation evidence 21: The reranker lift is concentrated on the question types where bi-encoder alone is weakest (Figure 4 ): single-session-user sees MRR rise from 0.091 to 0.522 ( ∼ 5 × \sim 5\times ); multi-session sees MRR rise from 0.55 to 0.75. Question types where the bi-encoder already achieves perfect P@5 see no further lift, consistent with the per-type results in Table 4 .
- Evaluation evidence 22: The mem0 head-to-head requires resetting and re-populating mem0’s index per question (approximately 30 s/question of Vertex AI calls), making full-500 prohibitive. This subset, with balanced per-type sampling, allows a reproducible direct comparison within a practical compute budget. The reranker numbers, in contrast, are now reported on the full 500 (Table 4 ); the n=12 ablation P@5 = 0.917 =0.917 and MRR = 0.837...
- Evaluation evidence 23: This is a comfortably high baseline indicating the embedder is not the limiting factor in our retrieval results; the gaps in LongMemEval-S come from the difficulty of the question types, not embedder quality.
- Evaluation evidence 24: The four-step ablation reported in Table 1 is on a 100-prompt set authored by the authors. To address train-test contamination concerns (Section 3.9 ), we additionally constructed a frozen held-out test set:
- Evaluation evidence 25: This construction has three properties relevant to train-test isolation: (i) prompts come from real production traces, not authored alongside anchors; (ii) labels come from an independent LLM judge, not the anchor authors; (iii) the resulting JSON file is committed to the repository with a frozen-at timestamp and a warning that anchor tuning may never reference it.
- Evaluation evidence 26: We acknowledge that LLM-as-judge labeling is itself a noisy signal; we treat held-out AUC as a complementary metric to the in-set ablation rather than a definitive replacement. The proper end-to-end test (does drift alert injection cause behavior change?) is described in Section 6 .
- Evaluation evidence 27: The takeaway: do not draw conclusions from n = 4 n=4 retrieval benchmarks , especially when the dataset has known per-type variance (Wu et al. , 2024 ) . We were initially confident in the null because the experimental setup was correct (clean control, deterministic pipeline); the failure was statistical rather than methodological.
- Evaluation evidence 28: Our active learning module surfaces alerts whose drift score is near the decision boundary ( | drift | < 0.05 |\mathrm{drift}|<0.05 ) for preferential user labeling. The theoretical case is straightforward: labels near the boundary disambiguate the largest amount of uncertainty. In practice, the long-term value of this loop depends on user willingness to label, which we have not yet measured at scale (current...
- Evaluation evidence 29: We highlight seven known limitations of nautilus-compass v0.7.1 and the empirical evaluation in this paper. The first is the most consequential and should be read carefully.
- Evaluation evidence 30: The empirical contribution of this paper combines (a) drift detection accuracy —does our system correctly classify a prompt (Sections 4.2 , 4.7 )—and (b) behavior steering , a separate empirical question: does an LLM, when given the drift alert as additional context, actually behave more safely on the same deviation prompt. We address (b) with an A/B evaluation across six production LLMs from six different vendors,...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: 3.9 Empirical Ablation: Four-step Evolution
- Ablation or analysis section 2: 4.2 Drift Detection: Four-step Methodology Ablation
- Ablation or analysis section 3: Train-on-test concern in the in-set ablation.

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

### 8.1 Primary external artifacts checked

- [Official implementation](https://github.com/chunxiaoxx/nautilus-compass) — Public implementation repository linked by the reviewed paper; reproduction not attempted.

### 8.2 Associated Black Lake records

- No same-paper DEP or directly corresponding repository record was verified after checking the DEP-A and DEP-E indexes, repository logs/reports, and the related Black-Lake-Data repository.

## 9. Research notes and critical considerations

- Qualification evidence 1: Production LLM coding agents drift over long sessions: they forget user-specified constraints, slip into mistakes the user already flagged, and confabulate prior agreements. White-box approaches such as persona vectors require model weights and so cannot be applied to closed APIs (Claude, GPT-4) that most users actually interact with.
- Qualification evidence 2: Modern coding agents based on Large Language Models (LLMs) such as Claude Code, Cursor, and Continue.dev sustain dialogue sessions spanning hundreds to thousands of turns. Within such sessions, a recurring failure mode is persona drift : the assistant gradually forgets user-specified constraints (“always verify before claiming success”), slips into bad habits the user explicitly flagged earlier (“don’t fabricate...
- Qualification evidence 3: This paper makes a claim about detection accuracy : given a user-issued prompt and a curated anchor set, our method classifies whether the prompt aligns with task-shaped positive patterns or deviates toward known failure modes, with held-out AUC reported in Section 4.7 . We additionally report a cross-vendor behavior-steering A/B (Section 6 ) across six production LLMs (Google Gemini-2.5 {pro, flash}, MiniMax M2.7,...
- Qualification evidence 4: The chief limitation, from the perspective of the typical end-user of a coding agent, is that this method requires access to model weights or hidden states. This puts it out of reach of users interacting with proprietary LLMs (Claude, GPT-4, Gemini) through API or product surfaces. Our work targets exactly this deployment gap: providing a black-box analog that runs in user-space hooks without model-internal access.
- Qualification evidence 5: A behavioral anchor is a short natural-language string (typically 8–40 tokens) describing either a desired behavior pattern ( positive ) or a flagged failure mode ( negative ). We adopt a JSON schema with backwards compatibility:
- Qualification evidence 6: We also support active learning by surfacing alerts whose drift score lies near the decision boundary ( | drift | < 0.05 |\mathrm{drift}|<0.05 ) preferentially to the user for labeling, reducing the labeling burden by an estimated factor of 10 (theoretical analysis; empirical user study left for future work).
- Qualification evidence 7: Our initial embedder was bge-small-zh-v1.5 (512 dim, Chinese-focused). This embedder yields strong intra-language retrieval (MRR 0.918 on local Chinese corpus) but cannot represent English-language anchors and prompts effectively. Switching to bge-m3 (1024 dim, multilingual) yielded a further AUC bump to 0.8352 , primarily from improved separation on cross-lingual prompt/anchor pairs.
- Qualification evidence 8: This iteration loop— eval, identify hard cases, rephrase , add to anchor set, re-eval on held-out —with the “rephrase” step (turning specific test failures into generic behavioral patterns) preventing trivial memorization, is what we recommend practitioners follow when building anchor sets for new domains.
- Qualification evidence 9: We constructed a 100-prompt synthetic test set with 50 prompts labeled aligned (matching positive task patterns the user has stated) and 50 labeled deviation (matching negative anchor failures). The aligned prompts cover verification, simplification, root-cause analysis, truthful reporting, and other typical engineering virtues; the deviation prompts cover fabrication, sycophancy, skipping verification, hardcoding...
- Qualification evidence 10: In drift detection, anchors are short imperative sentences. The “token interaction” between a prompt and an anchor is largely keyword overlap, which a bi-encoder already captures. Cross-encoders cannot manufacture signal that does not exist in the data.
- Qualification evidence 11: The takeaway: do not draw conclusions from n = 4 n=4 retrieval benchmarks , especially when the dataset has known per-type variance (Wu et al. , 2024 ) . We were initially confident in the null because the experimental setup was correct (clean control, deterministic pipeline); the failure was statistical rather than methodological.
- Qualification evidence 12: We highlight seven known limitations of nautilus-compass v0.7.1 and the empirical evaluation in this paper. The first is the most consequential and should be read carefully.

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.[^methodology]

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.09863v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: Nautilus Compass: Black-box Persona Drift Detection for Production LLM Agents
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: Existing approaches fall into two categories.
- Source section 5: Our contribution.
- Source section 6: Methodological insight.
- Source section 7: Open source and reproducibility.
- Source section 8: Relation to Persona Vectors.
- Source section 9: Scope and limits of claims.
- Source section 10: 2 Related Work
- Source section 11: 2.1 Memory Systems for LLM Agents
- Source section 12: 2.2 White-box Persona Monitoring
- Source section 13: 2.3 Black-box Safety and Behavior Classifiers
- Source section 14: 2.4 Embedders and Cross-Encoder Rerankers
- Source section 15: 2.5 Long-Context Memory Benchmarks
- Source section 16: 2.6 Anchor-Based Behavior Detection (Analogies)
- Source section 17: 2.7 Strategy Distillation
- Source section 18: 3 Method
- Source section 19: 3.1 Architecture Overview
- Source section 20: 3.2 Anchor Schema
- Source section 21: 3.3 Design Principle: Task-shaped Authoring
- Source section 22: 3.4 Drift Scoring: Weighted Top- k k Mean
- Source section 23: 3.5 Tri-band Output
- Source section 24: 3.6 Adaptive Learning Loop
- Source section 25: 3.7 False-Positive Filter
- Source section 26: 3.8 Cross-Domain Anchor Profiles
- Source section 27: 3.9 Empirical Ablation: Four-step Evolution
- Source section 28: Step 1: From abstract maxims to task-shaped anchors (§ 3.3 ).
- Source section 29: Step 2: From centroid mean to weighted top- k k mean (§ 3.4 ).
- Source section 30: Step 3: From bge-small-zh-v1.5 to bge-m3.
- Source section 31: Step 4: Adding hard false-positive examples to negative anchors.
- Source section 32: Train-test contamination caveat.
- Source section 33: 4 Evaluation
- Source section 34: 4.1 Setup
- Source section 35: Drift detection test set.
- Source section 36: LongMemEval-S.
- Source section 37: Metrics.
- Source section 38: 4.2 Drift Detection: Four-step Methodology Ablation
- Source section 39: Step 1 commentary.
- Source section 40: Step 4 commentary.
- Source section 41: Score distribution.
- Source section 42: 4.3 Cross-Encoder Drift Scoring: Null Result
- Source section 43: 4.4 Retrieval: LongMemEval-S Full 500
- Source section 44: Subset 12 vs. full 500.
- Source section 45: 4.5 mem0 Head-to-Head (Subset 12)
- Source section 46: Per-type lift.
- Source section 47: Compared to mem0.
- Source section 48: Why subset 12 for mem0.
- Source section 49: 4.6 Local Recall Sanity Check
- Source section 50: 4.7 Held-out Test Set: Generalization Beyond Self-Authored Prompts
- Source section 51: 4.8 Baselines
- Source section 52: 4.9 Latency
- Source section 53: 5 Discussion
- Source section 54: 5.1 Anchor Design as the Dominant Variable
- Source section 55: 5.2 Black-box vs. White-box Persona Monitoring
- Source section 56: Advantage of white-box.
- Source section 57: Disadvantage of white-box.
- Source section 58: Complementarity.
- Source section 59: 5.3 Why Reranker Helps Retrieval but Not Drift
- Source section 60: 5.4 The Subset 4 vs. Subset 12 Lesson
- Source section 61: 5.5 Active Learning in Practice
- Source section 62: 5.6 Implications for Production Deployment
- Source section 63: 6 Limitations and Future Work
- Source section 64: Behavior steering: preliminary cross-vendor evidence.
- Source section 65: Train-on-test concern in the in-set ablation.
- Source section 66: Synthetic drift test set.
- Source section 67: Single-session-user retrieval ceiling.
- Source section 68: Subset 12 for mem0 only.
- Source section 69: Cross-domain anchor profiles need expert review.
- Source section 70: Daemon caches a single anchor profile.
- Source section 71: Windows m3 cold-load failure modes.
- Source section 72: Future directions.
- Source section 73: 7 Open Source and Reproducibility
- Source section 74: 7.1 Repository
- Source section 75: 7.2 Reproducibility
- Source section 76: 7.3 Continuous Integration
- Source section 77: 7.4 Acknowledgments
- Source section 78: References

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1: Drift detection ROC AUC across the four-step methodology evolution. Each step is a controlled change with all other variables fixed; aggregate improvement is from random ( 0.5056 0.5056 ) to strong ( 0.9232 0.9232 ). The largest single jump comes from rewriting abstract maxim anchors as task-shaped sentences matching the prompt distribution.
- Visual/table evidence 2: Figure 2: System architecture. A user-issued prompt enters UserPromptSubmit , which forks three parallel paths: top- k k memory recall, drift scoring against behavioral anchors, and DPT-style strategy retrieval. The combined output is injected back into the LLM’s system prompt for the same turn.
- Visual/table evidence 3: Table 1: Four-step drift detection ablation. All evaluated on the same 50/50 prompt test set (Section 4.1 ). Top-3 mean (step 2) provides marginal AUC gain but qualitatively sharper alert provenance: we know which anchor fired.
- Visual/table evidence 4: Figure 3: Drift score distribution for the v0.7.1 final configuration. 2 2 2 Evaluation snapshot is v0.7.1; current code release is v1.0.0 (2026-05-08). The compass surfaces evaluated here (drift detector, BGE+rerank pipeline, anchor schema) are unchanged from v0.7.1 to 1.0.0. Aligned prompts (green) cluster around + 0.06 +0.06 ; deviation prompts (red) cluster around − 0.05 -0.05 . The decision threshold (best...
- Visual/table evidence 5: Table 2: Cross-encoder drift scoring vs. bi-encoder cosine top-3 mean, evaluated on the same 100-prompt test set.
- Visual/table evidence 6: Table 3: LongMemEval-S retrieval on the full 500 questions. Reranker is bge-reranker-v2-m3 (cross-encoder) applied to top-50 bi-encoder candidates, returning top-5. GPU run: bi-encoder 169 min CPU and reranker eval 67 min on GTX 1060 6 GB.
- Visual/table evidence 7: Table 4: Per-question-type breakdown on the full 500. Bi-encoder vs. bi-encoder + bge-reranker-v2-m3. The reranker dramatically improves the hardest types ( single-session-user MRR 0.398 → 0.586 0.398\rightarrow 0.586 , single-session-preference 0.537 → 0.788 0.537\rightarrow 0.788 , knowledge-update 0.635 → 0.848 0.635\rightarrow 0.848 ) while leaving already-saturated types unchanged.
- Visual/table evidence 8: Table 5: LongMemEval-S subset 12 retrieval. Same 12 questions (2 per type), three retrieval pipelines.
- Visual/table evidence 9: Figure 4: Reranker MRR lift on LongMemEval-S subset 12 by question type. Hardest question type (single-session-user) sees 5 × 5\times MRR improvement; easier types are already near ceiling for bi-encoder alone.
- Visual/table evidence 10: Figure 5: Per-question-type MRR on LongMemEval-S subset 12. nautilus-compass (m3+rerank) vs. mem0 (Vertex text-embedding-005). Same dataset, same 12 questions, infer=False for mem0. Largest gaps: single-session-user ( + 0.27 +0.27 MRR for us) and temporal-reasoning ( + 0.25 +0.25 ).
- Visual/table evidence 11: Table 6: Drift detection baselines on 100-prompt synthetic test set (same set as the four-step ablation). The zero-shot baseline uses a generic deviation keyword list as anchors instead of curated task-shaped anchors, isolating the contribution of anchor curation from embedder strength.
- Visual/table evidence 12: Table 7: Hot-path latency per UserPromptSubmit hook invocation, warm daemon, CPU-only.
- Visual/table evidence 13: Table 8: Behavior steering A/B by subject LLM. Each row averages 20 paired prompts judged by Moonshot Kimi-k2.6 on four axes. “W/T/L” counts prompts where the prompt-level mean of B exceeds A by > 0.05 >0.05 (win), is within ± 0.05 \pm 0.05 (tie), or is below A by > 0.05 >0.05 (loss).
- Visual/table evidence 14: Table 9: Per-axis A/B effect, pooled across all 6 subjects ( n = 120 n=120 ).
- Visual/table evidence 15: Figure 6: Cross-vendor behavior steering A/B heatmap. Each cell is the Δ \Delta judge score (B with drift alert minus A without) for one subject LLM × one behavioral axis. Significance markers from per-subject paired t t -tests: · p < 0.10 p<0.10 , ⋆ \star p < 0.05 p<0.05 , ⋆ ⁣ ⋆ \star\star p < 0.01 p<0.01 (df = 19 per cell). Fabrication-resistance is positive across 5 of 6 subjects; verify and destruct show notable...

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2605.09863v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2605.09863v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2605.09863v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2605.09863v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2605.09863
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
