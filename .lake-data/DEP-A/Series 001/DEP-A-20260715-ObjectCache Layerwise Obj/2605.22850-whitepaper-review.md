# ObjectCache Layerwise Object Storage: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “ObjectCache: Layerwise Object-Storage Retrieval for KV Cache Reuse”

**Source paper:** Yu Zhu, Aditya Dhakal, Yunming Xiao, Dejan Milojicic, Gustavo Alonso, “ObjectCache: Layerwise Object-Storage Retrieval for KV Cache Reuse,” arXiv:2605.22850v1.[^source-paper]

**Artifact prepared:** 2026-07-15  
**Local source reviewed:** complete PDF (26 pages) and matching full-paper HTML (71705 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **ObjectCache: Layerwise Object-Storage Retrieval for KV Cache Reuse** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Prefix KV caching has become a key mechanism in LLM serving: it reduces time to first token (TTFT) by avoiding redundant computation across requests that share a prefix (i.e., the system prompt). However, the accumulated KV cache is often larger than what GPU memory and local DRAM can hold.
- Source passage 3: Modern LLM serving increasingly relies on long and reusable input contexts, including system prompts, retrieval-augmented generation, code repositories, long conversations, and agent histories (Xiang et al. , 2026 ; Wang et al.
- Source passage 4: The challenge is that reusable KV cache is much larger than the memory capacity naturally available near GPUs. GPU memory is scarce and needed for model weights and active requests, while local DRAM only scales with the serving nodes that happen to produce or consume the cache.
- Source passage 5: This motivates a different question: can S3-compatible object storage serve as a runtime backend for reusable KV cache? Object storage is attractive because prefix KV blocks are immutable after prefill, naturally addressable by content-derived prefix hashes, and reusable across users, sessions, and compute nodes.
- Source passage 6: The obstacle is not only transport bandwidth. Even with an RDMA data path like Nvidia cuObject (NVIDIA, 2026b , a ) , the standard S3 abstraction remains mismatched to runtime KV-cache reuse.
- Source passage 7: We present ObjectCache, a protocol-scheduling co-design that makes S3-compatible object storage suitable for runtime KV-cache reuse. On the protocol side, ObjectCache keeps KV cache stored as fine-grained, hash-addressed chunks, but extends the S3-compatible request with a compact descriptor that names the matched chunks, model layout, delivery order, and RDMA target.
- Source passage 8: We prototype ObjectCache on a 100 Gbps RoCE cluster using NIXL (Nvidia, 2026 ) , Ceph RGW (Ceph, 2026 ) , and DAOS (DAOS, 2026 ) , and evaluate it with Llama 3.1 8B (Meta, 2026 ) . For 64K-token contexts, ObjectCache adds only 5.6% TTFT over an optimized local layerwise baseline.
- Source passage 9: This paper makes the following contributions:
- Source passage 10: We propose ObjectCache, a protocol-scheduling co-design for serving reusable KV cache from S3-compatible object storage.

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 355 extractable mathematical objects, 44 substantive headings, and 91 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: \times
- Equation or mathematical object 2: G
- Equation or mathematical object 3: L
- Equation or mathematical object 4: n_{\mathrm{kv}}
- Equation or mathematical object 5: d
- Equation or mathematical object 6: p
- Equation or mathematical object 7: G
- Equation or mathematical object 8: \mathrm{KV}_{\mathrm{token}}=2Ln_{\mathrm{kv}}dp,\qquad S_{\mathrm{layer,chunk}}=2Gn_{\mathrm{kv}}dp.
- Equation or mathematical object 9: H_{i}=\mathrm{Hash}(H_{i-1}\parallel\mathrm{tokens}_{i})
- Equation or mathematical object 10: H_{i}

### 2.2 Method and information flow

- Method evidence 1: Prefix KV caching has become a key mechanism in LLM serving: it reduces time to first token (TTFT) by avoiding redundant computation across requests that share a prefix (i.e., the system prompt). However, the accumulated KV cache is often larger than what GPU memory and local DRAM can hold. To preserve latency, current systems keep the KV cache in remote DRAM pools, increasing serving-cluster size and cost. In this...
- Method evidence 2: The challenge is that reusable KV cache is much larger than the memory capacity naturally available near GPUs. GPU memory is scarce and needed for model weights and active requests, while local DRAM only scales with the serving nodes that happen to produce or consume the cache. Recent systems therefore use remote DRAM pools or memory-centric KV-cache tiers to preserve low latency (Qin et al. , 2024 ; Hu et al. ,...
- Method evidence 3: We propose ObjectCache, a protocol-scheduling co-design for serving reusable KV cache from S3-compatible object storage.
- Method evidence 4: We introduce a bandwidth scheduler that allocates storage bandwidth according to per-layer compute-transfer overlap, reducing TTFT under contention.
- Method evidence 5: We demonstrate on a 100 Gbps RoCE prototype that ObjectCache approaches local layerwise KV-cache performance for long-context workloads and improves TTFT under shared bandwidth limits.
- Method evidence 6: The resulting architecture has three roles (Figure 6 ). The GPU serving node issues S3-compatible requests and consumes KV cache data through the inference engine’s layerwise interface. The gateway terminates the S3 control plane, parses the ObjectCache descriptor, forwards the multi-object request to the storage server, and upgrades the S3 control plane to express prefix-aware, layer-scheduled KV transfer. The...
- Method evidence 7: The serving node uses the NIXL object backend to issue S3 operations. For ordinary objects, the backend can use TCP, an RDMA-buffer mode, or the RDMA-direct path used in our microbenchmarks. For ObjectCache requests, LMCache (LMCache, 2026 ) provides the matched prefix chunk keys and the model layout parameters ( L L , G G , and S S ). NIXL serializes these fields into the HTTP descriptor and attaches an RDMA target...
- Method evidence 8: Local DAOS reads exceed the 100 Gbps NIC capacity at 256 KB block size with C = 32 C{=}32 , and saturate the SSD throughput at 4 MB block size with C = 32 C{=}32 . DAOS over RDMA approaches the 100 Gbps hardware limit on cold reads at 1 MB block size with C = 8 C{=}8 , while TCP lags consistently (Figure 8 ). We use the saturated RDMA configurations as the reference point for later S3 and ObjectCache experiments:...

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: ObjectCache: Layerwise Object-Storage Retrieval for KV Cache Reuse
- Source section 2: Abstract.
- Source section 3: 1. Introduction
- Source section 4: 2. Background and Motivation
- Source section 5: 2.1. KV cache and Prefix Reuse
- Source section 6: 2.2. Serving-Path KV Access Pattern
- Source section 7: 3. ObjectCache Design
- Source section 8: 3.1. Required Semantics
- Source section 9: 3.2. The ObjectCache Descriptor
- Source section 10: 3.3. Server-Side Layer Aggregation
- Source section 11: 3.4. Server-Side Mode Selection
- Source section 12: 3.5. Layerwise Prefetch
- Source section 13: 3.6. Bandwidth-Aware Scheduling
- Source section 14: 4. Implementation
- Source section 15: 4.1. S3-Compatible Paths
- Source section 16: 4.2. GPU Serving Node
- Source section 17: 4.3. Gateway and Storage Server Execution
- Source section 18: 4.4. Tracing and Rate Control
- Source section 19: 4.5. Testbed
- Source section 20: 5. Evaluation
- Source section 21: 5.1. Raw Storage Baseline
- Source section 22: Setup.

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: We demonstrate on a 100 Gbps RoCE prototype that ObjectCache approaches local layerwise KV-cache performance for long-context workloads and improves TTFT under shared bandwidth limits.
- Evaluation evidence 2: In disaggregated serving, a prefill worker materializes KV cache for the input prompt, while a decode worker later uses the resulting KV state to generate tokens. When a new request shares a cached prefix, prefix lookup returns an ordered list of matched KV chunks that the prefill worker can reuse before computing the remaining suffix. Although these chunks are stored independently, the model consumes them layer by...
- Evaluation evidence 3: The resulting architecture has three roles (Figure 6 ). The GPU serving node issues S3-compatible requests and consumes KV cache data through the inference engine’s layerwise interface. The gateway terminates the S3 control plane, parses the ObjectCache descriptor, forwards the multi-object request to the storage server, and upgrades the S3 control plane to express prefix-aware, layer-scheduled KV transfer. The...
- Evaluation evidence 4: We use the following names in the end-to-end TTFT evaluation:
- Evaluation evidence 5: The serving node uses the NIXL object backend to issue S3 operations. For ordinary objects, the backend can use TCP, an RDMA-buffer mode, or the RDMA-direct path used in our microbenchmarks. For ObjectCache requests, LMCache (LMCache, 2026 ) provides the matched prefix chunk keys and the model layout parameters ( L L , G G , and S S ). NIXL serializes these fields into the HTTP descriptor and attaches an RDMA target...
- Evaluation evidence 6: To understand request overhead, the prototype propagates request identifiers from the NIXL client through RGW into DAOS traces, allowing the evaluation to split latency into client, gateway, storage, and RDMA-bulk components. For the multi-tenant scheduling experiments, we add a layerwise pacing control that limits the effective layer delivery rate. This lets us emulate shared bandwidth caps while keeping the same...
- Evaluation evidence 7: The evaluation uses a 100 Gbps RoCEv2 (RDMA over Converged Ethernet, v2) cluster with a GPU serving node for inference, an S3 gateway, and a DAOS storage node. The GPU is an A100 80 GB GPU (CUDA 12.8, PyTorch 2.10.0, vLLM v0.19.0, the modified LMCache v0.4.2, the modified NIXL v1.0.0); the gateway runs the modified Ceph RGW v18.2.7; the storage node runs DAOS v2.7.102 with its default UCX (UCX, 2026 ) transport,...
- Evaluation evidence 8: Our evaluation follows the same mechanisms introduced in the design. First, the storage and network substrate must transfer data fast enough. Second, the S3 control path must be measured separately from the RDMA data path. Third, ObjectCache’s server-side aggregation must achieve high throughput for fine-grained KV chunks. Fourth, layerwise delivery must overlap with GPU compute in configurations where measured...
- Evaluation evidence 9: We measure DAOS throughput as seen by the NIXL object client without the Ceph RGW gateway, isolating the storage backend from S3 protocol overhead (Figure 8 ). The benchmark ranges block sizes from 64 KB to 4 MB and client concurrency C ∈ { 8 , 32 } C\in\{8,32\} , where C = 8 C{=}8 uses a single thread with 8 in-flight requests and C = 32 C{=}32 uses 4 threads each with 8 in-flight requests. Writes populate...
- Evaluation evidence 10: We next measure S3 PUT and GET throughput across block sizes from 64 KB to 4 MB. The benchmark compares S3TCP, S3RDMA Buffer that uses a gateway staging buffer, and S3RDMA Direct where the S3 request carries control information data going through the DAOS RDMA data path; Section 4 gives the corresponding path names. The two concurrency points, C = 8 C{=}8 and C = 32 C{=}32 , match the canonical raw-storage...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1 . Long-context LLM tasks increasingly reuse long-lived prefixes, growing the aggregate KV cache footprint that a serving cluster must retain.
- Visual/table evidence 2: Figure 2 . Per-layer KV payload for a 16-token chunk across recent open-weight LLM families. The 64 KB dashed line marks the grouped-query attention (GQA) baseline with 8 KV heads of 128 dimensions; multi-head latent attention (MLA) and smaller head counts push recent models below this threshold.
- Visual/table evidence 3: (a) Fine granularity preserves intermediate branch points.
- Visual/table evidence 4: (b) Coarse granularity merges branch points.
- Visual/table evidence 5: Figure 3 . Prefix reuse under fine and coarse storage granularities. Coarse chunks reduce index depth but lose branch points where requests can diverge, forcing otherwise reusable tokens to be recomputed.
- Visual/table evidence 6: Figure 4 . Prefix-hash lookup cost is small relative to tokenization, even at 16-token granularity. Fine-grained indexing is therefore not the request critical-path bottleneck.
- Visual/table evidence 7: Figure 5 . ObjectCache in a disaggregated cluster. Prefix KV caches are stored in an object-storage tier through an S3-compatible interface, decoupling prefill and decode workers from the machines that produced the cache. ObjectCache extends this interface so object storage can serve KV cache reuse with the granularity and layerwise delivery order expected by LLM serving systems.
- Visual/table evidence 8: Figure 6 . ObjectCache system design. HTTP preserves S3-compatible control, while the storage server aggregates matched chunk ranges into layerwise KV payloads and delivers them to the serving node over RDMA.
- Visual/table evidence 9: Table 1 . ObjectCache request descriptor.
- Visual/table evidence 10: (a) Chunkwise loading waits for the full cached prefix before compute can start.
- Visual/table evidence 11: (b) Transfer-bound layerwise prefetch.
- Visual/table evidence 12: (c) Compute-bound layerwise prefetch.

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: We demonstrate on a 100 Gbps RoCE prototype that ObjectCache approaches local layerwise KV-cache performance for long-context workloads and improves TTFT under shared bandwidth limits.
- Evaluation evidence 2: In disaggregated serving, a prefill worker materializes KV cache for the input prompt, while a decode worker later uses the resulting KV state to generate tokens. When a new request shares a cached prefix, prefix lookup returns an ordered list of matched KV chunks that the prefill worker can reuse before computing the remaining suffix. Although these chunks are stored independently, the model consumes them layer by...
- Evaluation evidence 3: The resulting architecture has three roles (Figure 6 ). The GPU serving node issues S3-compatible requests and consumes KV cache data through the inference engine’s layerwise interface. The gateway terminates the S3 control plane, parses the ObjectCache descriptor, forwards the multi-object request to the storage server, and upgrades the S3 control plane to express prefix-aware, layer-scheduled KV transfer. The...
- Evaluation evidence 4: We use the following names in the end-to-end TTFT evaluation:
- Evaluation evidence 5: The serving node uses the NIXL object backend to issue S3 operations. For ordinary objects, the backend can use TCP, an RDMA-buffer mode, or the RDMA-direct path used in our microbenchmarks. For ObjectCache requests, LMCache (LMCache, 2026 ) provides the matched prefix chunk keys and the model layout parameters ( L L , G G , and S S ). NIXL serializes these fields into the HTTP descriptor and attaches an RDMA target...
- Evaluation evidence 6: To understand request overhead, the prototype propagates request identifiers from the NIXL client through RGW into DAOS traces, allowing the evaluation to split latency into client, gateway, storage, and RDMA-bulk components. For the multi-tenant scheduling experiments, we add a layerwise pacing control that limits the effective layer delivery rate. This lets us emulate shared bandwidth caps while keeping the same...
- Evaluation evidence 7: The evaluation uses a 100 Gbps RoCEv2 (RDMA over Converged Ethernet, v2) cluster with a GPU serving node for inference, an S3 gateway, and a DAOS storage node. The GPU is an A100 80 GB GPU (CUDA 12.8, PyTorch 2.10.0, vLLM v0.19.0, the modified LMCache v0.4.2, the modified NIXL v1.0.0); the gateway runs the modified Ceph RGW v18.2.7; the storage node runs DAOS v2.7.102 with its default UCX (UCX, 2026 ) transport,...
- Evaluation evidence 8: Our evaluation follows the same mechanisms introduced in the design. First, the storage and network substrate must transfer data fast enough. Second, the S3 control path must be measured separately from the RDMA data path. Third, ObjectCache’s server-side aggregation must achieve high throughput for fine-grained KV chunks. Fourth, layerwise delivery must overlap with GPU compute in configurations where measured...
- Evaluation evidence 9: We measure DAOS throughput as seen by the NIXL object client without the Ceph RGW gateway, isolating the storage backend from S3 protocol overhead (Figure 8 ). The benchmark ranges block sizes from 64 KB to 4 MB and client concurrency C ∈ { 8 , 32 } C\in\{8,32\} , where C = 8 C{=}8 uses a single thread with 8 in-flight requests and C = 32 C{=}32 uses 4 threads each with 8 in-flight requests. Writes populate...
- Evaluation evidence 10: We next measure S3 PUT and GET throughput across block sizes from 64 KB to 4 MB. The benchmark compares S3TCP, S3RDMA Buffer that uses a gateway staging buffer, and S3RDMA Direct where the S3 request carries control information data going through the DAOS RDMA data path; Section 4 gives the corresponding path names. The two concurrency points, C = 8 C{=}8 and C = 32 C{=}32 , match the canonical raw-storage...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: 5.6. Sensitivity to Bandwidth Changes

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

## 9. Research notes and critical considerations

- Qualification evidence 1: The challenge is that reusable KV cache is much larger than the memory capacity naturally available near GPUs. GPU memory is scarce and needed for model weights and active requests, while local DRAM only scales with the serving nodes that happen to produce or consume the cache. Recent systems therefore use remote DRAM pools or memory-centric KV-cache tiers to preserve low latency (Qin et al. , 2024 ; Hu et al. ,...
- Qualification evidence 2: The key challenge is that this serving path needs more than ordinary object reads. ObjectCache is built around one design rule: keep storage objects small enough for prefix reuse, but expose a serving interface that returns large, layer-ordered transfers . The design is therefore an interface extension, not a new transport. The underlying S3-over-RDMA data path moves bytes; ObjectCache changes what the serving...
- Qualification evidence 3: As an illustration, on our 100 Gbps prototype with Llama 3.1 8B we use Θ ≈ 512 \Theta\approx 512 MB, the payload size at which network transfer time at line rate becomes comparable to the prefill compute window. Under this rule, the 4K configurations in Section 5 fall on the chunkwise side and the 16K/64K configurations fall on the aggregated-layerwise side, matching where each mode wins in Figure 13 . The...
- Qualification evidence 4: This behavior follows from layerwise overlap. In S3Batch-CW, computation cannot start until the chunkwise KV load completes, so a bandwidth reduction largely appears as additional TTFT. In S3Agg-LW, only the non-overlapped portion of each layer transfer contributes to TTFT. Therefore, bandwidth changes affect TTFT mainly when the required overlap throughput exceeds the available bandwidth. Appendix Table A8 explains...
- Qualification evidence 5: In this paper we optimize the serving-path data movement mechanism for S3-compatible object storage. Our prototype evaluates the critical data path and scheduling policy on a small cluster, which is sufficient to isolate the effects of aggregation, RDMA transfer, layerwise delivery, and bandwidth scheduling, but does not capture all production effects. Future work should validate ObjectCache with concurrent clients,...

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.22850v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: ObjectCache: Layerwise Object-Storage Retrieval for KV Cache Reuse
- Source section 2: Abstract.
- Source section 3: 1. Introduction
- Source section 4: 2. Background and Motivation
- Source section 5: 2.1. KV cache and Prefix Reuse
- Source section 6: 2.2. Serving-Path KV Access Pattern
- Source section 7: 3. ObjectCache Design
- Source section 8: 3.1. Required Semantics
- Source section 9: 3.2. The ObjectCache Descriptor
- Source section 10: 3.3. Server-Side Layer Aggregation
- Source section 11: 3.4. Server-Side Mode Selection
- Source section 12: 3.5. Layerwise Prefetch
- Source section 13: 3.6. Bandwidth-Aware Scheduling
- Source section 14: 4. Implementation
- Source section 15: 4.1. S3-Compatible Paths
- Source section 16: 4.2. GPU Serving Node
- Source section 17: 4.3. Gateway and Storage Server Execution
- Source section 18: 4.4. Tracing and Rate Control
- Source section 19: 4.5. Testbed
- Source section 20: 5. Evaluation
- Source section 21: 5.1. Raw Storage Baseline
- Source section 22: Setup.

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1 . Long-context LLM tasks increasingly reuse long-lived prefixes, growing the aggregate KV cache footprint that a serving cluster must retain.
- Visual/table evidence 2: Figure 2 . Per-layer KV payload for a 16-token chunk across recent open-weight LLM families. The 64 KB dashed line marks the grouped-query attention (GQA) baseline with 8 KV heads of 128 dimensions; multi-head latent attention (MLA) and smaller head counts push recent models below this threshold.
- Visual/table evidence 3: (a) Fine granularity preserves intermediate branch points.
- Visual/table evidence 4: (b) Coarse granularity merges branch points.
- Visual/table evidence 5: Figure 3 . Prefix reuse under fine and coarse storage granularities. Coarse chunks reduce index depth but lose branch points where requests can diverge, forcing otherwise reusable tokens to be recomputed.
- Visual/table evidence 6: Figure 4 . Prefix-hash lookup cost is small relative to tokenization, even at 16-token granularity. Fine-grained indexing is therefore not the request critical-path bottleneck.
- Visual/table evidence 7: Figure 5 . ObjectCache in a disaggregated cluster. Prefix KV caches are stored in an object-storage tier through an S3-compatible interface, decoupling prefill and decode workers from the machines that produced the cache. ObjectCache extends this interface so object storage can serve KV cache reuse with the granularity and layerwise delivery order expected by LLM serving systems.
- Visual/table evidence 8: Figure 6 . ObjectCache system design. HTTP preserves S3-compatible control, while the storage server aggregates matched chunk ranges into layerwise KV payloads and delivers them to the serving node over RDMA.
- Visual/table evidence 9: Table 1 . ObjectCache request descriptor.
- Visual/table evidence 10: (a) Chunkwise loading waits for the full cached prefix before compute can start.
- Visual/table evidence 11: (b) Transfer-bound layerwise prefetch.
- Visual/table evidence 12: (c) Compute-bound layerwise prefetch.

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2605.22850v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2605.22850v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2605.22850v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2605.22850v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2605.22850
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
