# HERALD High Throughput Block: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “HERALD: High-Throughput Block Diffusion LLM Serving via CPU-GPU Cooperative KV Cache Retrieval”

**Source paper:** Omin Kwon, Doyeon Kim, Jongseok Park, Seung Yul Lee, Ion Stoica, Jae W. Lee, “HERALD: High-Throughput Block Diffusion LLM Serving via CPU-GPU Cooperative KV Cache Retrieval,” arXiv:2606.21633v1.[^source-paper]

**Artifact prepared:** 2026-07-15  
**Local source reviewed:** complete PDF (14 pages) and matching full-paper HTML (52065 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **HERALD: High-Throughput Block Diffusion LLM Serving via CPU-GPU Cooperative KV Cache Retrieval** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Diffusion LLMs (dLLMs) improve GPU utilization over autoregressive decoding by generating multiple tokens per forward pass, but their KV cache still grows linearly with context, limiting throughput at long contexts. KV cache offloading to host DRAM alleviates this memory pressure, but the limited PCIe bandwidth necessitates recalling only a sparse subset of KV entries.
- Source passage 3: Large Language Model (LLM) inference under the autoregressive (AR) decoding paradigm suffers from two well-known inefficiencies. First, each decode step generates a single token while re-reading the entire model weights and KV cache, yielding low arithmetic intensity that severely underutilizes GPU compute.
- Source passage 4: Diffusion LLMs (dLLMs), now deployed in production systems such as Google’s Gemini Diffusion (Google DeepMind, 2025 ) , have emerged as an alternative that generates multiple tokens per forward pass through iterative denoising, achieving higher arithmetic intensity and better GPU utilization than AR decoding. Among them, Block dLLMs (Bie et al.
- Source passage 5: The KV cache memory pressure, however, persists. A block dLLM’s prefix KV cache still grows linearly with context and dominates GPU capacity.
- Source passage 6: Fortunately, block dLLMs possess a structural property that enables substantially higher selection accuracy than existing AR approaches achieve. Across the denoising steps of a block, attention patterns exhibit strong temporal consistency : the top- k k KV entries at the first step (step 0) are similar to top- k k of subsequent denoising step.
- Source passage 7: Realizing this sparse selection under KV offloading, however, is fundamentally difficult, posing two challenges. The first is a hardware mapping challenge : when the KV cache resides in CPU DRAM, this full-cache exact attention at step 0 can run on either the CPU or the GPU, but the two face opposite bottlenecks.
- Source passage 8: We identify two structural opportunities, unique to block dLLMs, that resolve these challenges.
- Source passage 9: Opportunity 1: A single center- [MASK] query suffices. Every selection targets a block still entirely [MASK] tokens, so the B B query vectors share an identical pre-RoPE embedding and differ only through RoPE’s positional rotation.
- Source passage 10: Opportunity 2: Step-0 logits provide a free semantic proxy for the finalized block. At step 0 of a block, the model produces logits at every one of the B B positions conditioned on the full preceding context.

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 281 extractable mathematical objects, 26 substantive headings, and 13 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: k
- Equation or mathematical object 2: 1.59\times
- Equation or mathematical object 3: 2.47\times
- Equation or mathematical object 4: B
- Equation or mathematical object 5: B
- Equation or mathematical object 6: T~({<}~B)
- Equation or mathematical object 7: B
- Equation or mathematical object 8: T
- Equation or mathematical object 9: B
- Equation or mathematical object 10: k

### 2.2 Method and information flow

- Method evidence 1: Fortunately, block dLLMs possess a structural property that enables substantially higher selection accuracy than existing AR approaches achieve. Across the denoising steps of a block, attention patterns exhibit strong temporal consistency : the top- k k KV entries at the first step (step 0) are similar to top- k k of subsequent denoising step. Recent dLLM sparse-attention work (Kwon et al. , 2026 ; Wang et al. ,...
- Method evidence 2: We evaluate HERALD on three production block dLLMs: SDAR-8B-Chat, TraDo-8B-Instruct, and LLaDA 2.0-mini 16B—against Dense (full GPU), InfiniGen (Lee et al. , 2024 ) , and MAGE-Offload, an offloading adaptation of the dLLM sparse-attention state of the art. On LongBench across five long-context tasks, HERALD reaches near-lossless accuracy at a 5–10% KV budget, within a few percentage points of in-GPU MAGE, while AR...
- Method evidence 3: Diffusion LLMs (dLLMs) have recently emerged as a promising architecture that directly addresses this inefficiency. Google’s Gemini Diffusion (Google DeepMind, 2025 ) was released as a production-level dLLM, demonstrating that dLLMs have moved beyond the research stage into practical deployment. Unlike AR models, dLLMs generate multiple tokens in a single forward pass, achieving higher decoding throughput (Kim et...
- Method evidence 4: To address this GPU memory constraint, the most prominent approach is KV offloading , which stores the KV cache in host CPU DRAM—offering an order-of-magnitude larger capacity—and fetches it back to the GPU when needed for attention computation. However, the PCIe interconnect between host and device provides roughly two orders of magnitude lower bandwidth than HBM in GPUs, making the transfer cost a critical...
- Method evidence 5: InfiniGen (Lee et al. , 2024 ) pioneered this approach by speculatively prefetching important KV entries for layer i i using the attention input from layer i − 1 i{-}1 , which allows for reduced KV and also overlapped communication and computation. ShadowKV (Sun et al. , 2025 ) retains a low-rank key cache and outlier entries on the GPU for lightweight selection, fetching only the value cache from CPU on demand....
- Method evidence 6: As shown in Figure 5 (b), this exact selection with reuse achieves 82–88% selection accuracy, substantially higher than per-step approximate methods applied to dLLMs (54–69%). Moreover, since selection is performed only once per block rather than at every step, the selection overhead is amortized over T T denoising steps. Due to high accuracy, exact selection with reuse has consequently become the de facto approach...
- Method evidence 7: Applying KV cache offloading to block dLLMs requires deciding how to select the sparse KV subset recalled to the GPU for each block. As established in Section 2 , selection accuracy directly governs recall latency: a less accurate selection must recall a larger KV subset to preserve generation quality, proportionally amplifying the PCIe transfer latency. The exact-selection-with-reuse approach is therefore...
- Method evidence 8: Challenge. However, realizing this approach under KV offloading is fundamentally difficult. Under exact-selection-with-reuse, full-cache exact attention must run at step 0 of each block to identify the top- k k entries before sparse denoising begins at steps 1 through T T . Because the full KV cache resides in CPU DRAM, this selection is inherently expensive: transferring the entire cache to the GPU incurs a PCIe...

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: HERALD: High-Throughput Block Diffusion LLM Serving via CPU-GPU Cooperative KV Cache Retrieval
- Source section 2: Abstract.
- Source section 3: 1. Introduction
- Source section 4: 2. Background
- Source section 5: 2.1. Limitations of Autoregressive LLM Inference
- Source section 6: 2.2. Diffusion Large Language Models
- Source section 7: 2.3. Long-Context KV Cache Offloading
- Source section 8: 2.4. Sparse Attention in Diffusion LLMs
- Source section 9: 3. Motivation
- Source section 10: 3.1. Full KV Exact Selection under KV Offloading
- Source section 11: 3.2. Key Opportunities
- Source section 12: 4. HERALD
- Source section 13: 4.1. Overview
- Source section 14: 4.2. HERALD Execution Walkthrough
- Source section 15: 4.3. CPU-GPU Cooperative KV Cache Retrieval
- Source section 16: 4.4. Optimized CPU Kernel for Full KV Selection
- Source section 17: 5. Implementation
- Source section 18: 6. Evaluation
- Source section 19: 6.1. Experimental Setup
- Source section 20: 6.2. Accuracy Evaluation
- Source section 21: 6.3. Performance Evaluation
- Source section 22: 6.4. HERALD Latency Breakdown

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: Diffusion LLMs (dLLMs) improve GPU utilization over autoregressive decoding by generating multiple tokens per forward pass, but their KV cache still grows linearly with context, limiting throughput at long contexts. KV cache offloading to host DRAM alleviates this memory pressure, but the limited PCIe bandwidth necessitates recalling only a sparse subset of KV entries. In block dLLMs, the relevant KV entries remain...
- Evaluation evidence 2: Fortunately, block dLLMs possess a structural property that enables substantially higher selection accuracy than existing AR approaches achieve. Across the denoising steps of a block, attention patterns exhibit strong temporal consistency : the top- k k KV entries at the first step (step 0) are similar to top- k k of subsequent denoising step. Recent dLLM sparse-attention work (Kwon et al. , 2026 ; Wang et al. ,...
- Evaluation evidence 3: Opportunity 2: Step-0 logits provide a free semantic proxy for the finalized block. At step 0 of a block, the model produces logits at every one of the B B positions conditioned on the full preceding context. The confidence-based scheduler commits only the highest-confidence positions and discards the rest, but these discarded logits are the model’s own best single-shot prediction for the entire block—and thus a...
- Evaluation evidence 4: We evaluate HERALD on three production block dLLMs: SDAR-8B-Chat, TraDo-8B-Instruct, and LLaDA 2.0-mini 16B—against Dense (full GPU), InfiniGen (Lee et al. , 2024 ) , and MAGE-Offload, an offloading adaptation of the dLLM sparse-attention state of the art. On LongBench across five long-context tasks, HERALD reaches near-lossless accuracy at a 5–10% KV budget, within a few percentage points of in-GPU MAGE, while AR...
- Evaluation evidence 5: Across three production block dLLMs and five long-context tasks, HERALD achieves near-lossless accuracy at a 5–10% KV budget, up to 1.59 × 1.59\times lower per-block latency, and up to 2.47 × 2.47\times decode throughput over the Dense GPU baseline, with the speedup growing with context length.
- Evaluation evidence 6: Typically, LLM inference consists of two phases: a prefill phase that processes the input prompt, and a decode phase that generates output tokens autoregressively. Many LLM inference scenarios, decode dominates the end-to-end latency as the number of output tokens grows, making it the primary optimization target (Kwon et al. , 2023 ; Sheng et al. , 2023 ; Zhong et al. , 2024 ) . The decode phase suffers from two...
- Evaluation evidence 7: Block Diffusion has since become the dominant paradigm in practical dLLMs, adopted by state-of-the-art models including LLaDA 2.0 (Bie et al. , 2025 ) , Fast-dLLM v2 (Wu et al. , 2026 ) , SDAR (Cheng et al. , 2025 ) , all of which achieve competitive accuracy with leading AR models.
- Evaluation evidence 8: As shown in Figure 5 (b), this exact selection with reuse achieves 82–88% selection accuracy, substantially higher than per-step approximate methods applied to dLLMs (54–69%). Moreover, since selection is performed only once per block rather than at every step, the selection overhead is amortized over T T denoising steps. Due to high accuracy, exact selection with reuse has consequently become the de facto approach...
- Evaluation evidence 9: Applying KV cache offloading to block dLLMs requires deciding how to select the sparse KV subset recalled to the GPU for each block. As established in Section 2 , selection accuracy directly governs recall latency: a less accurate selection must recall a larger KV subset to preserve generation quality, proportionally amplifying the PCIe transfer latency. The exact-selection-with-reuse approach is therefore...
- Evaluation evidence 10: A natural remedy is to begin block N + 1 N{+}1 ’s selection earlier, overlapping it with block N N ’s denoising to hide the cost behind the T T denoising steps. Yet this is not straightforward: block N + 1 N{+}1 ’s selection requires block N N ’s finalized tokens as prefix context, creating a data dependency that prevents selection from starting before block N N ’s denoising completes. Addressing both the high cost...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1 . Challenges HERALD addresses and its key ideas.
- Visual/table evidence 2: Figure 2 . Comparison of decoding processes. (a) AR LLM: Token-by-token generation per forward pass using standard causal attention. (b) Block Diffusion LLM: Block-wise parallel generation over multiple forward passes. Block causal attention mask enables bidirectional attention within the current block while attending to fixed cached tokens.
- Visual/table evidence 3: Figure 3 . KV cache pressure and offloading overhead in LLaDA 2.0 ( B = 64 B=64 , 8K–32K context). (a) GPU memory breakdown showing KV cache scaling beyond the 80 GB limit of a single H100. (b) Latency of fetching the full KV cache from host DRAM, normalized to the model forward time.
- Visual/table evidence 4: Figure 4 . Comparison of Offloading Methods applied to Block Diffusion LLM
- Visual/table evidence 5: Figure 5 . Analysis of KV cache selection strategies on LLaDA 2.0. (a) Temporal consistency of top- k k recall across the denoising progress, comparing different k k values. (b) top- k k selection accuracy against oracle: the dLLM selection method (MAGE) using first-step exact selection with reuse vs. the AR-LLM selection method (Quest) using every-step approximate selection. Results are averaged over LongBench...
- Visual/table evidence 6: Figure 6 . (a) Top- k k recall of a single center [MASK] query vs. the full B B -query union. (b) CPU attention speedup when reducing from B = 32 B{=}32 queries to N N (8K–32K; measured on Intel Xeon Platinum 8481C). Reducing to N = 1 N{=}1 yields 22–24 × \times speedup while retaining 84% top- k k recall.
- Visual/table evidence 7: Figure 7 . System overview of Herald . Left : the prefill phase appends a [MASK] block to the prompt, offloads KV states to CPU DRAM layer-wise, and performs top- k k selection to bootstrap the sparse KV pool. Right : the decode phase operates two concurrent streams with double-buffered KV pools; the retrieval stream writes the next block’s entries to the idle pool while the main stream denoises from the active pool.
- Visual/table evidence 8: Figure 8 . Full pipeline of HERALD over two consecutive blocks. The main stream denoises block N N while the retrieval stream concurrently prepares block N + 1 N{+}1 ’s Top- k k entries through cooperative CPU–GPU execution, completing before denoising finishes. At the block boundary, the double-buffered KV pools are swapped and the pattern repeats for block N + 1 N{+}1 .
- Visual/table evidence 9: Figure 9 . LongBench accuracy across all three models and five tasks. HERALD closely tracks the Dense baseline at 5–10% KV cache budget, while InfiniGen degrades substantially even at high budgets. MAGE is consistently robust across all settings.
- Visual/table evidence 10: Figure 10 . Per-block decode latency (TPOB, ms) at batch sizes B = 1 B{=}1 , 16 16 , 32 32 , and 64 64 across context lengths. Columns correspond to models. Dense runs the full KV cache in HBM; all other methods offload KV to host DRAM. Dashed red bars indicate GPU out-of-memory.
- Visual/table evidence 11: Table 1 . Peak decode throughput (tokens/s) with the batch size that maximizes it.
- Visual/table evidence 12: Figure 11 . Per-block latency breakdown of HERALD on LLaDA 2.0-mini 16B at two context lengths (8K, 32K) and three batch sizes ( B = 16 , 32 , 64 B{=}16,32,64 ).

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: Diffusion LLMs (dLLMs) improve GPU utilization over autoregressive decoding by generating multiple tokens per forward pass, but their KV cache still grows linearly with context, limiting throughput at long contexts. KV cache offloading to host DRAM alleviates this memory pressure, but the limited PCIe bandwidth necessitates recalling only a sparse subset of KV entries. In block dLLMs, the relevant KV entries remain...
- Evaluation evidence 2: Fortunately, block dLLMs possess a structural property that enables substantially higher selection accuracy than existing AR approaches achieve. Across the denoising steps of a block, attention patterns exhibit strong temporal consistency : the top- k k KV entries at the first step (step 0) are similar to top- k k of subsequent denoising step. Recent dLLM sparse-attention work (Kwon et al. , 2026 ; Wang et al. ,...
- Evaluation evidence 3: Opportunity 2: Step-0 logits provide a free semantic proxy for the finalized block. At step 0 of a block, the model produces logits at every one of the B B positions conditioned on the full preceding context. The confidence-based scheduler commits only the highest-confidence positions and discards the rest, but these discarded logits are the model’s own best single-shot prediction for the entire block—and thus a...
- Evaluation evidence 4: We evaluate HERALD on three production block dLLMs: SDAR-8B-Chat, TraDo-8B-Instruct, and LLaDA 2.0-mini 16B—against Dense (full GPU), InfiniGen (Lee et al. , 2024 ) , and MAGE-Offload, an offloading adaptation of the dLLM sparse-attention state of the art. On LongBench across five long-context tasks, HERALD reaches near-lossless accuracy at a 5–10% KV budget, within a few percentage points of in-GPU MAGE, while AR...
- Evaluation evidence 5: Across three production block dLLMs and five long-context tasks, HERALD achieves near-lossless accuracy at a 5–10% KV budget, up to 1.59 × 1.59\times lower per-block latency, and up to 2.47 × 2.47\times decode throughput over the Dense GPU baseline, with the speedup growing with context length.
- Evaluation evidence 6: Typically, LLM inference consists of two phases: a prefill phase that processes the input prompt, and a decode phase that generates output tokens autoregressively. Many LLM inference scenarios, decode dominates the end-to-end latency as the number of output tokens grows, making it the primary optimization target (Kwon et al. , 2023 ; Sheng et al. , 2023 ; Zhong et al. , 2024 ) . The decode phase suffers from two...
- Evaluation evidence 7: Block Diffusion has since become the dominant paradigm in practical dLLMs, adopted by state-of-the-art models including LLaDA 2.0 (Bie et al. , 2025 ) , Fast-dLLM v2 (Wu et al. , 2026 ) , SDAR (Cheng et al. , 2025 ) , all of which achieve competitive accuracy with leading AR models.
- Evaluation evidence 8: As shown in Figure 5 (b), this exact selection with reuse achieves 82–88% selection accuracy, substantially higher than per-step approximate methods applied to dLLMs (54–69%). Moreover, since selection is performed only once per block rather than at every step, the selection overhead is amortized over T T denoising steps. Due to high accuracy, exact selection with reuse has consequently become the de facto approach...
- Evaluation evidence 9: Applying KV cache offloading to block dLLMs requires deciding how to select the sparse KV subset recalled to the GPU for each block. As established in Section 2 , selection accuracy directly governs recall latency: a less accurate selection must recall a larger KV subset to preserve generation quality, proportionally amplifying the PCIe transfer latency. The exact-selection-with-reuse approach is therefore...
- Evaluation evidence 10: A natural remedy is to begin block N + 1 N{+}1 ’s selection earlier, overlapping it with block N N ’s denoising to hide the cost behind the T T denoising steps. Yet this is not straightforward: block N + 1 N{+}1 ’s selection requires block N N ’s finalized tokens as prefix context, creating a data dependency that prevents selection from starting before block N N ’s denoising completes. Addressing both the high cost...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: 6.5. Ablation Study

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

## 9. Research notes and critical considerations

- Qualification evidence 1: Realizing this sparse selection under KV offloading, however, is fundamentally difficult, posing two challenges. The first is a hardware mapping challenge : when the KV cache resides in CPU DRAM, this full-cache exact attention at step 0 can run on either the CPU or the GPU, but the two face opposite bottlenecks. The CPU is closest to the KV cache, and prior AR offloading systems place attention there to avoid PCIe...
- Qualification evidence 2: We identify two structural opportunities, unique to block dLLMs, that resolve these challenges.
- Qualification evidence 3: We analyze the challenges of realizing sparse KV selection under offloading for block dLLMs and identify two fundamental obstacles: a hardware mapping challenge and a dependency challenge.
- Qualification evidence 4: We uncover two structural opportunities in block dLLMs, a center- [MASK] query sufficiency and step-0 draft proxy, that together resolve both challenges, making KV selection tractable on the CPU and removing it from the critical path.
- Qualification evidence 5: Challenge. However, realizing this approach under KV offloading is fundamentally difficult. Under exact-selection-with-reuse, full-cache exact attention must run at step 0 of each block to identify the top- k k entries before sparse denoising begins at steps 1 through T T . Because the full KV cache resides in CPU DRAM, this selection is inherently expensive: transferring the entire cache to the GPU incurs a PCIe...
- Qualification evidence 6: We identify two structural properties of block dLLMs that together resolve these challenges.

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.21633v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: HERALD: High-Throughput Block Diffusion LLM Serving via CPU-GPU Cooperative KV Cache Retrieval
- Source section 2: Abstract.
- Source section 3: 1. Introduction
- Source section 4: 2. Background
- Source section 5: 2.1. Limitations of Autoregressive LLM Inference
- Source section 6: 2.2. Diffusion Large Language Models
- Source section 7: 2.3. Long-Context KV Cache Offloading
- Source section 8: 2.4. Sparse Attention in Diffusion LLMs
- Source section 9: 3. Motivation
- Source section 10: 3.1. Full KV Exact Selection under KV Offloading
- Source section 11: 3.2. Key Opportunities
- Source section 12: 4. HERALD
- Source section 13: 4.1. Overview
- Source section 14: 4.2. HERALD Execution Walkthrough
- Source section 15: 4.3. CPU-GPU Cooperative KV Cache Retrieval
- Source section 16: 4.4. Optimized CPU Kernel for Full KV Selection
- Source section 17: 5. Implementation
- Source section 18: 6. Evaluation
- Source section 19: 6.1. Experimental Setup
- Source section 20: 6.2. Accuracy Evaluation
- Source section 21: 6.3. Performance Evaluation
- Source section 22: 6.4. HERALD Latency Breakdown

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1 . Challenges HERALD addresses and its key ideas.
- Visual/table evidence 2: Figure 2 . Comparison of decoding processes. (a) AR LLM: Token-by-token generation per forward pass using standard causal attention. (b) Block Diffusion LLM: Block-wise parallel generation over multiple forward passes. Block causal attention mask enables bidirectional attention within the current block while attending to fixed cached tokens.
- Visual/table evidence 3: Figure 3 . KV cache pressure and offloading overhead in LLaDA 2.0 ( B = 64 B=64 , 8K–32K context). (a) GPU memory breakdown showing KV cache scaling beyond the 80 GB limit of a single H100. (b) Latency of fetching the full KV cache from host DRAM, normalized to the model forward time.
- Visual/table evidence 4: Figure 4 . Comparison of Offloading Methods applied to Block Diffusion LLM
- Visual/table evidence 5: Figure 5 . Analysis of KV cache selection strategies on LLaDA 2.0. (a) Temporal consistency of top- k k recall across the denoising progress, comparing different k k values. (b) top- k k selection accuracy against oracle: the dLLM selection method (MAGE) using first-step exact selection with reuse vs. the AR-LLM selection method (Quest) using every-step approximate selection. Results are averaged over LongBench...
- Visual/table evidence 6: Figure 6 . (a) Top- k k recall of a single center [MASK] query vs. the full B B -query union. (b) CPU attention speedup when reducing from B = 32 B{=}32 queries to N N (8K–32K; measured on Intel Xeon Platinum 8481C). Reducing to N = 1 N{=}1 yields 22–24 × \times speedup while retaining 84% top- k k recall.
- Visual/table evidence 7: Figure 7 . System overview of Herald . Left : the prefill phase appends a [MASK] block to the prompt, offloads KV states to CPU DRAM layer-wise, and performs top- k k selection to bootstrap the sparse KV pool. Right : the decode phase operates two concurrent streams with double-buffered KV pools; the retrieval stream writes the next block’s entries to the idle pool while the main stream denoises from the active pool.
- Visual/table evidence 8: Figure 8 . Full pipeline of HERALD over two consecutive blocks. The main stream denoises block N N while the retrieval stream concurrently prepares block N + 1 N{+}1 ’s Top- k k entries through cooperative CPU–GPU execution, completing before denoising finishes. At the block boundary, the double-buffered KV pools are swapped and the pattern repeats for block N + 1 N{+}1 .
- Visual/table evidence 9: Figure 9 . LongBench accuracy across all three models and five tasks. HERALD closely tracks the Dense baseline at 5–10% KV cache budget, while InfiniGen degrades substantially even at high budgets. MAGE is consistently robust across all settings.
- Visual/table evidence 10: Figure 10 . Per-block decode latency (TPOB, ms) at batch sizes B = 1 B{=}1 , 16 16 , 32 32 , and 64 64 across context lengths. Columns correspond to models. Dense runs the full KV cache in HBM; all other methods offload KV to host DRAM. Dashed red bars indicate GPU out-of-memory.
- Visual/table evidence 11: Table 1 . Peak decode throughput (tokens/s) with the batch size that maximizes it.
- Visual/table evidence 12: Figure 11 . Per-block latency breakdown of HERALD on LLaDA 2.0-mini 16B at two context lengths (8K, 32K) and three batch sizes ( B = 16 , 32 , 64 B{=}16,32,64 ).

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2606.21633v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2606.21633v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2606.21633v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2606.21633v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2606.21633
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
