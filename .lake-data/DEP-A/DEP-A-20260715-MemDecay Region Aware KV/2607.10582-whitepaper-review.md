# MemDecay Region Aware KV: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference”

**Source paper:** Venkatesha Matam, Keon Kim, “MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference,” arXiv:2607.10582v1.[^source-paper]

**Artifact prepared:** 2026-07-15  
**Local source reviewed:** complete PDF (9 pages) and matching full-paper HTML (39377 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Large language models (LLMs) are increasingly deployed as agents whose contexts accumulate system instructions, plans, user turns, tool outputs, retrieved documents, and intermediate reasoning over many steps. The key–value (KV) cache that supports autoregressive attention grows with this trace, and its memory footprint constrains long-horizon inference and serving.
- Source passage 3: We propose MemDecay , a training-free KV-cache eviction framework for long-horizon LLM agents. MemDecay assigns each token a region label supplied by the orchestration layer, combines region-specific base priorities with attention-derived importance, and applies region-specific temporal decay to each token’s retention priority.
- Source passage 4: We further describe a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses them to calibrate decay rates. In a controlled study spanning two context lengths (roughly 450 and 1,700 tokens) and two model sizes (Qwen2.5-1.5B and 3B), measured lifetimes differ by an order of magnitude across regions in every setting (system half-lives of 148 to 189 decode steps against 14 to 16 for scratchpad reasoning), and the ordering of the movable regions is robust to insertion order.
- Source passage 5: Large language models (LLMs) are increasingly used in long-horizon agentic workflows that combine multi-turn interaction, retrieval, tool use, and iterative planning. Unlike single-pass long-context tasks, these systems accumulate a persistent execution trace composed of system instructions, safety constraints, evolving plans, user turns, retrieved evidence, tool interactions, and intermediate reasoning states.
- Source passage 6: This shift places new pressure on the key–value (KV) cache that supports autoregressive decoding. Because the KV cache grows with sequence length, long interaction traces translate into substantial memory overhead, increased bandwidth demand, and higher end-to-end latency [ 1 , 2 ] .
- Source passage 7: That assumption is poorly matched to agentic workloads. In LLM-based agents, memory is a core component that supports long-term and complex interactions with the environment [ 8 ] .
- Source passage 8: This mismatch motivates aligning cache retention with the semantic structure already present in orchestrated prompts. We propose MemDecay , a training-free, prompt-region-aware KV-cache decay and eviction framework for long-horizon LLM agents.
- Source passage 9: MemDecay treats forgetting as a scheduled process. Region-specific temporal decay lowers a token’s retention priority smoothly over time, and attention to the token resets its decay clock, so continued use keeps a token alive.
- Source passage 10: Our contributions are threefold:

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 136 extractable mathematical objects, 22 substantive headings, and 8 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: \times
- Equation or mathematical object 2: i
- Equation or mathematical object 3: t=1,2,\dots
- Equation or mathematical object 4: i
- Equation or mathematical object 5: r(i)\in\mathcal{R}
- Equation or mathematical object 6: t_{i}^{\mathrm{ins}}
- Equation or mathematical object 7: \mathcal{R}
- Equation or mathematical object 8: \mathcal{K}_{t}
- Equation or mathematical object 9: t
- Equation or mathematical object 10: B

### 2.2 Method and information flow

- Method evidence 1: We propose MemDecay , a training-free KV-cache eviction framework for long-horizon LLM agents. MemDecay assigns each token a region label supplied by the orchestration layer, combines region-specific base priorities with attention-derived importance, and applies region-specific temporal decay to each token’s retention priority. Under a fixed cache budget, MemDecay evicts the pages holding the lowest-priority tokens...
- Method evidence 2: This shift places new pressure on the key–value (KV) cache that supports autoregressive decoding. Because the KV cache grows with sequence length, long interaction traces translate into substantial memory overhead, increased bandwidth demand, and higher end-to-end latency [ 1 , 2 ] . Prior work reduces this overhead through selective retention and compression, including policies that adapt eviction to stable...
- Method evidence 3: This mismatch motivates aligning cache retention with the semantic structure already present in orchestrated prompts. We propose MemDecay , a training-free, prompt-region-aware KV-cache decay and eviction framework for long-horizon LLM agents. MemDecay assumes that the orchestration layer provides region labels for tokens, such as system prompt, plan, user message, tool input, tool output, retrieval, or scratchpad....
- Method evidence 4: We introduce MemDecay , a training-free, prompt-region-aware eviction policy that combines region base priorities, region-specific temporal decay with attention-triggered refresh, and attention-derived importance in a single interpretable retention score under a fixed KV-cache budget.
- Method evidence 5: KV-cache management has become a first-order systems problem for scalable LLM serving because cache footprint grows with context length and directly constrains memory capacity, bandwidth, and throughput [ 1 , 2 ] . System-level approaches focus on how cached states are allocated, retained, and reused across requests. PagedAttention organizes the cache into fixed-size pages to reduce fragmentation and enable sharing...
- Method evidence 6: Later work showed that locally accumulated attention statistics are biased and can misestimate future utility [ 16 , 17 ] . Several methods reallocate budgets across heads or layers, reflecting evidence that attention modules have heterogeneous roles and sensitivities [ 3 , 4 ] . Others score tokens with query-aware or semantic-cluster criteria [ 18 , 19 ] , keep or drop contiguous semantic chunks instead of...
- Method evidence 7: MemDecay tracks one dynamic signal per token, an attention importance a i a_{i} , and one clock per token, the time since the token was last used. Fused attention kernels do not materialize the attention matrix, so reading per-step attention scores is impractical in production kernels [ 33 ] . MemDecay therefore samples attention only at observation steps, one every k k decode steps, using windowed scores in the...
- Method evidence 8: Table III reports the short-tier recall and Table IV the long-tier recall for both models, after eviction to 25% and 50% of the cache, overall and excluding the 24 probes whose answers live in the pinned system region. Baselines are the unconstrained full cache, random (4 sinks, 16 most recent, uniform sample, three seeds), streaming (4 sinks plus the most recent tokens [ 6 ] ), and h2o , a reimplementation of...

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference
- Source section 2: Abstract
- Source section 3: I Introduction
- Source section 4: II Related Work
- Source section 5: II-A KV-Cache Memory Management for LLM Serving
- Source section 6: II-B KV-Cache Eviction and Compression for Long-Context LLMs
- Source section 7: II-C Agent- and Workflow-Aware KV Management
- Source section 8: II-D Memory and Forgetting
- Source section 9: II-E Positioning MemDecay
- Source section 10: III Proposed Method
- Source section 11: III-A Problem Setup
- Source section 12: III-B Structural Priors
- Source section 13: III-C Retention Score with Region-Specific Decay
- Source section 14: III-D Budget-Aware Eviction at Page Granularity
- Source section 15: III-E Scope and Tiered Extension
- Source section 16: IV Evaluation
- Source section 17: IV-A Setup
- Source section 18: IV-B Measured Region Lifetimes Across Scales
- Source section 19: IV-C Budget-Constrained Recall Across Scales
- Source section 20: IV-D Sensitivity and Overhead
- Source section 21: V Discussion and Limitations
- Source section 22: References

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: We further describe a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses them to calibrate decay rates. In a controlled study spanning two context lengths (roughly 450 and 1,700 tokens) and two model sizes (Qwen2.5-1.5B and 3B), measured lifetimes differ by an order of magnitude across regions in every setting (system half-lives of 148 to 189 decode steps against 14...
- Evaluation evidence 2: MemDecay treats forgetting as a scheduled process. Region-specific temporal decay lowers a token’s retention priority smoothly over time, and attention to the token resets its decay clock, so continued use keeps a token alive. When a fixed KV-cache budget is exceeded, tokens with the lowest decayed priorities are evicted first, while pinned instruction regions and, optionally, active plan segments are preserved. The...
- Evaluation evidence 3: We specify a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses the fitted decay rates to calibrate the policy, and we execute it across two context lengths and two model sizes (4,320 scored eviction probes): the region-lifetime hierarchy replicates in every setting, with the longest- and shortest-lived regions separated by an order of magnitude at non-overlapping...
- Evaluation evidence 4: Concurrent work brings role and time structure into cache policies themselves. RoleKV tags KV blocks with semantic roles and applies role-calibrated decay to serving-layer retention, reporting that the oldest blocks in agent traces, including system prompts and tool outputs, receive the most persistent attention while recent reasoning tokens decay fastest [ 10 ] . IntentKV scores history tokens against an...
- Evaluation evidence 5: where α ≥ 0 \alpha\geq 0 weights how much observed attention can protect a token beyond its structural prior. Recency enters only through the decay clock, so the score contains no separate recency term and elapsed time is not counted twice. The three terms have direct interpretations: b r b_{r} sets where a token starts, λ r \lambda_{r} sets how quickly structure alone stops protecting it, and α ​ a i \alpha a_{i}...
- Evaluation evidence 6: This section reports a controlled study across two context lengths and two model sizes, executed end to end by the released harness: every number below comes from the raw result files at github.com/venkateshamatam/memdecay , and the study totals 4,320 scored eviction probes plus three full measurement passes. The findings are stated the way the evidence supports them. The region-lifetime measurement replicates at...
- Evaluation evidence 7: Fourth, the mechanism behind MemDecay’s unpinned loss is the same at every scale: the oldest unpinned facts score lowest under a decay prior. The user-region fact sits in the opening turn, and at the half budget MemDecay recalls 0 of 24 (short), 7 of 24 (1.5B long), and 5 of 24 (3B long) of them while h2o recalls 11 to 20 of 24; attention identifies exactly the tokens that structure ranks for eviction. The ablation...
- Evaluation evidence 8: An ablation on the short tier varies the attention weight α ∈ { 0.2 , 0.5 , 0.8 } \alpha\in\{0.2,0.5,0.8\} and disables pinning, holding everything else fixed. The outcome is an invariance: all four configurations produce near-identical keep sets and near-identical probe outcomes (4 of 576 rows differ in kept count and one in recall, a single spurious flip that does not touch the user-region loss). Two scale facts...
- Evaluation evidence 9: Irreversibility. The base policy cannot recover an evicted token when later context makes it relevant again, and such saliency shifts are documented in multi-turn agent traces [ 25 ] ; the user-region result in Section IV-C , where the oldest unpinned facts are evicted and later probed, is a measured instance. The tiered extension of Section III-E would reduce this exposure by offloading before dropping.
- Evaluation evidence 10: Future work. Three directions follow directly from the results. First, from static calibration to prediction: the ablation shows no weight on the current EWMA rescues old facts, so the attention term needs magnitude normalization (for example, relative to the uniform attention level) and a slower usage memory; the same machinery extends to decay rates that adapt online from observed refresh frequency per region, and...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1: MemDecay in the serving stack. The orchestrator labels each prompt segment with its region, the decode engine appends KV entries to region-aligned pages, and MemDecay maintains attention statistics and decay clocks, evicting the lowest-scoring non-pinned pages when the budget binds.
- Visual/table evidence 2: TABLE I: Default region configuration. Calibration (Section IV ) replaces the qualitative decay rates with per-workload fitted values.
- Visual/table evidence 3: Figure 2: Retention score trajectories from ( 4 ) under example parameters, with α ​ a i = 0 \alpha a_{i}=0 for clarity; the curves illustrate the equation and are not measurements. Pinned system tokens decay negligibly and plan tokens decay slowly, while short-lived regions decay quickly unless attention above the threshold τ \tau resets the decay clock, shown for tool_out at age 40 40 .
- Visual/table evidence 4: Algorithm 1 MemDecay update and eviction at an observation step t t
- Visual/table evidence 5: TABLE II: Fitted attention half-lives in decode steps (Eqs. 6 – 7 ), with 95% scenario-bootstrap confidence intervals, across the three settings. R 2 R^{2} spans 0.30 to 0.95; it is lowest for retrieval , whose near-flat curves leave little age-driven variance to explain.
- Visual/table evidence 6: Figure 3: Measured mean attention mass per token by region and token age for the largest setting (Qwen2.5-3B, long tier, 24 episodes; age buckets of 4 steps, log scale; point estimates). system stays persistently attended, scratchpad decays fastest, and retrieval outlives every other movable region at each of its three insertion positions. Table II shows the same hierarchy in the other two settings.
- Visual/table evidence 7: TABLE III: Short-tier recall (Qwen2.5-1.5B, 405–507-token episodes) after eviction, with 95% cluster-bootstrap confidence intervals over 8 scenarios (96 probes per cell; random averages 3 seeds). “Non- sys ” excludes probes answered in the pinned system region.
- Visual/table evidence 8: TABLE IV: Long-tier recall (1,656–1,723-token episodes) after eviction, for both models, with 95% cluster-bootstrap confidence intervals over 8 scenarios (96 probes per cell; random averages 3 seeds).

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: We further describe a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses them to calibrate decay rates. In a controlled study spanning two context lengths (roughly 450 and 1,700 tokens) and two model sizes (Qwen2.5-1.5B and 3B), measured lifetimes differ by an order of magnitude across regions in every setting (system half-lives of 148 to 189 decode steps against 14...
- Evaluation evidence 2: MemDecay treats forgetting as a scheduled process. Region-specific temporal decay lowers a token’s retention priority smoothly over time, and attention to the token resets its decay clock, so continued use keeps a token alive. When a fixed KV-cache budget is exceeded, tokens with the lowest decayed priorities are evicted first, while pinned instruction regions and, optionally, active plan segments are preserved. The...
- Evaluation evidence 3: We specify a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses the fitted decay rates to calibrate the policy, and we execute it across two context lengths and two model sizes (4,320 scored eviction probes): the region-lifetime hierarchy replicates in every setting, with the longest- and shortest-lived regions separated by an order of magnitude at non-overlapping...
- Evaluation evidence 4: Concurrent work brings role and time structure into cache policies themselves. RoleKV tags KV blocks with semantic roles and applies role-calibrated decay to serving-layer retention, reporting that the oldest blocks in agent traces, including system prompts and tool outputs, receive the most persistent attention while recent reasoning tokens decay fastest [ 10 ] . IntentKV scores history tokens against an...
- Evaluation evidence 5: where α ≥ 0 \alpha\geq 0 weights how much observed attention can protect a token beyond its structural prior. Recency enters only through the decay clock, so the score contains no separate recency term and elapsed time is not counted twice. The three terms have direct interpretations: b r b_{r} sets where a token starts, λ r \lambda_{r} sets how quickly structure alone stops protecting it, and α ​ a i \alpha a_{i}...
- Evaluation evidence 6: This section reports a controlled study across two context lengths and two model sizes, executed end to end by the released harness: every number below comes from the raw result files at github.com/venkateshamatam/memdecay , and the study totals 4,320 scored eviction probes plus three full measurement passes. The findings are stated the way the evidence supports them. The region-lifetime measurement replicates at...
- Evaluation evidence 7: Fourth, the mechanism behind MemDecay’s unpinned loss is the same at every scale: the oldest unpinned facts score lowest under a decay prior. The user-region fact sits in the opening turn, and at the half budget MemDecay recalls 0 of 24 (short), 7 of 24 (1.5B long), and 5 of 24 (3B long) of them while h2o recalls 11 to 20 of 24; attention identifies exactly the tokens that structure ranks for eviction. The ablation...
- Evaluation evidence 8: An ablation on the short tier varies the attention weight α ∈ { 0.2 , 0.5 , 0.8 } \alpha\in\{0.2,0.5,0.8\} and disables pinning, holding everything else fixed. The outcome is an invariance: all four configurations produce near-identical keep sets and near-identical probe outcomes (4 of 576 rows differ in kept count and one in recall, a single spurious flip that does not touch the user-region loss). Two scale facts...
- Evaluation evidence 9: Irreversibility. The base policy cannot recover an evicted token when later context makes it relevant again, and such saliency shifts are documented in multi-turn agent traces [ 25 ] ; the user-region result in Section IV-C , where the oldest unpinned facts are evicted and later probed, is a measured instance. The tiered extension of Section III-E would reduce this exposure by offloading before dropping.
- Evaluation evidence 10: Future work. Three directions follow directly from the results. First, from static calibration to prediction: the ablation shows no weight on the current EWMA rescues old facts, so the attention term needs magnitude normalization (for example, relative to the uniform attention level) and a slower usage memory; the same machinery extends to decay rates that adapt online from observed refresh frequency per region, and...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: IV-D Sensitivity and Overhead

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

## 9. Research notes and critical considerations

- Qualification evidence 1: We further describe a measurement protocol that estimates region-conditioned attention lifetimes from agent traces and uses them to calibrate decay rates. In a controlled study spanning two context lengths (roughly 450 and 1,700 tokens) and two model sizes (Qwen2.5-1.5B and 3B), measured lifetimes differ by an order of magnitude across regions in every setting (system half-lives of 148 to 189 decode steps against 14...
- Qualification evidence 2: Memory is a defining component of LLM-based agents because long-horizon interaction depends on retaining, updating, and selectively reusing prior state [ 8 , 27 ] . Within KV-cache research, several recent papers argue that irreversible keep-or-drop policies are too coarse: token value can shift as decoding proceeds, and a token evicted early cannot be recovered when later queries make it relevant again [ 25 , 28 ]...
- Qualification evidence 3: MemDecay evicts at page granularity: when | 𝒦 t | > B |\mathcal{K}_{t}|>B , non-pinned pages are ordered by ascending S p ​ ( t ) S_{p}(t) and evicted until the budget is met. This greedy rule is optimal for the page-level objective because pages have equal size; it can deviate from the token-level optimum of ( 1 ) when a page mixes regions, since the mean in ( 5 ) can mask a high-scoring token among low-scoring...
- Qualification evidence 4: Second, recency collapses with context length. Streaming is competitive on the short tier (0.36 non-system at the half budget, within the intervals of random at 0.37 and h2o at 0.40) and near zero on the long tier at both models (0.01 to 0.13), because its window physically cannot span the trace. MemDecay’s paired per-scenario comparison against streaming flips accordingly: 0 of 8 scenarios positive on the short...
- Qualification evidence 5: Irreversibility. The base policy cannot recover an evicted token when later context makes it relevant again, and such saliency shifts are documented in multi-turn agent traces [ 25 ] ; the user-region result in Section IV-C , where the oldest unpinned facts are evicted and later probed, is a measured instance. The tiered extension of Section III-E would reduce this exposure by offloading before dropping.
- Qualification evidence 6: Cross-region information flow. Agents copy content across regions, for example by quoting a tool output inside scratchpad reasoning. Under the default configuration both copies decay quickly, so the information can vanish from the cache even though the model produced and consumed it twice. Per-workload calibration reduces this risk; a mechanism that links copies to their sources is future work.

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2607.10582v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference
- Source section 2: Abstract
- Source section 3: I Introduction
- Source section 4: II Related Work
- Source section 5: II-A KV-Cache Memory Management for LLM Serving
- Source section 6: II-B KV-Cache Eviction and Compression for Long-Context LLMs
- Source section 7: II-C Agent- and Workflow-Aware KV Management
- Source section 8: II-D Memory and Forgetting
- Source section 9: II-E Positioning MemDecay
- Source section 10: III Proposed Method
- Source section 11: III-A Problem Setup
- Source section 12: III-B Structural Priors
- Source section 13: III-C Retention Score with Region-Specific Decay
- Source section 14: III-D Budget-Aware Eviction at Page Granularity
- Source section 15: III-E Scope and Tiered Extension
- Source section 16: IV Evaluation
- Source section 17: IV-A Setup
- Source section 18: IV-B Measured Region Lifetimes Across Scales
- Source section 19: IV-C Budget-Constrained Recall Across Scales
- Source section 20: IV-D Sensitivity and Overhead
- Source section 21: V Discussion and Limitations
- Source section 22: References

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1: MemDecay in the serving stack. The orchestrator labels each prompt segment with its region, the decode engine appends KV entries to region-aligned pages, and MemDecay maintains attention statistics and decay clocks, evicting the lowest-scoring non-pinned pages when the budget binds.
- Visual/table evidence 2: TABLE I: Default region configuration. Calibration (Section IV ) replaces the qualitative decay rates with per-workload fitted values.
- Visual/table evidence 3: Figure 2: Retention score trajectories from ( 4 ) under example parameters, with α ​ a i = 0 \alpha a_{i}=0 for clarity; the curves illustrate the equation and are not measurements. Pinned system tokens decay negligibly and plan tokens decay slowly, while short-lived regions decay quickly unless attention above the threshold τ \tau resets the decay clock, shown for tool_out at age 40 40 .
- Visual/table evidence 4: Algorithm 1 MemDecay update and eviction at an observation step t t
- Visual/table evidence 5: TABLE II: Fitted attention half-lives in decode steps (Eqs. 6 – 7 ), with 95% scenario-bootstrap confidence intervals, across the three settings. R 2 R^{2} spans 0.30 to 0.95; it is lowest for retrieval , whose near-flat curves leave little age-driven variance to explain.
- Visual/table evidence 6: Figure 3: Measured mean attention mass per token by region and token age for the largest setting (Qwen2.5-3B, long tier, 24 episodes; age buckets of 4 steps, log scale; point estimates). system stays persistently attended, scratchpad decays fastest, and retrieval outlives every other movable region at each of its three insertion positions. Table II shows the same hierarchy in the other two settings.
- Visual/table evidence 7: TABLE III: Short-tier recall (Qwen2.5-1.5B, 405–507-token episodes) after eviction, with 95% cluster-bootstrap confidence intervals over 8 scenarios (96 probes per cell; random averages 3 seeds). “Non- sys ” excludes probes answered in the pinned system region.
- Visual/table evidence 8: TABLE IV: Long-tier recall (1,656–1,723-token episodes) after eviction, for both models, with 95% cluster-bootstrap confidence intervals over 8 scenarios (96 probes per cell; random averages 3 seeds).

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2607.10582v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2607.10582v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2607.10582v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2607.10582v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2607.10582
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
