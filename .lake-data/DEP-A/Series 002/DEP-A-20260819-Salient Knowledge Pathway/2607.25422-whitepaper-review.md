# Whitepaper Review: Salient Knowledge Pathways: Sparse Cross-Modal Routing for Efficient Knowledge-Intensive Multimodal Question Answering

**Canonical paper:** arXiv:2607.25422v1  
**Complete authors:** Noor Islam S. Mohammad; Uluğ Bayazıt  
**Review type:** full-paper review and independent re-conceptualization  
**Primary public records:** [abstract](https://arxiv.org/abs/2607.25422v1), [PDF](https://arxiv.org/pdf/2607.25422v1), [full-paper HTML](https://arxiv.org/html/2607.25422v1), [DOI](https://doi.org/10.48550/arXiv.2607.25422)

## 1. Executive Assessment

At a high level, the paper makes its memory decision surface explicit and evaluates that decision under named operational constraints.

The paper's strongest contribution is a concrete mechanism connected to an explicit evaluation, rather than a claim resting on the abstract alone. SKIP achieves the highest accuracy on every benchmark while consuming 4.07 × 4.07\times fewer FLOPs and 2.69 × 2.69\times less wall-clock latency than the strongest baseline (RA-CM3). The accuracy improvement is not an artifact of more compute spent elsewhere: at matched FLOPs (0.42 TFLOPs, the BLIP-2 budget), SKIP outperforms BLIP-2 by + 17.3 +17.3 points on OK-VQA and + 19.5 +19.5 points on InfoSeek. QVS achieves 99 % 99\% of the no-pruning accuracy at just 11 % 11\% retention. We show that question-conditional pruning is qualitatively different in knowledge-intensive settings: at 11 % 11\% retention, QVS outperforms unconditional methods by 5 5 – 10 10 accuracy points ( Figure 4 ). These are paper reports. No experiment, benchmark, training run, kernel measurement, or security test was independently rerun for this review.[^scope]

The source gate passed before drafting: the local PDF is 33 pages, begins and ends as a complete PDF, parses, contains the expected title and full body structure, and includes references; the matching full-paper HTML contains 125788 readable body characters, document structure, headings, and paper sections.[^integrity] The evidence is therefore suitable for a full-paper assessment rather than an abstract-level summary.

My bottom-line judgment is favorable but conditional. The method is technically legible and the reported comparisons establish a meaningful operating region. We characterize SKIP’s failure modes by examining the 36.8 % 36.8\% of OK-VQA queries it misses. The compositional retrieval failure mode points to a clear next step: extending BSCA to a k k -partite structure that explicitly models multi-hop knowledge dependencies. Treating sparsity as a first-class design principle, jointly across modalities and across retrieval, opens substantial headroom that we believe will only grow as models and corpora scale. The durable value is the mechanism and its testable boundary, not the largest number in isolation.

## 2. Problem Framing and Research Questions

The paper addresses a resource or state-management problem in which a naive representation becomes too expensive, stale, unsafe, or poorly aligned with a downstream task. Its research question is whether a structured decision can preserve the relevant outcome while reducing cost or strengthening an invariant. The right comparison is therefore a frontier: outcome versus compute, memory, bandwidth, latency, storage, or integrity under the same information boundary.

Five questions guide this review. First, what object is measured or transformed? Second, which decision is novel and which components are inherited? Third, what cost does the transformation add before the downstream consumer runs? Fourth, under which datasets, models, traces, budgets, and hardware does the reported advantage hold? Fifth, what observation would falsify the proposed explanation rather than merely lower an aggregate score?

The paper's framing is useful because it makes one previously implicit control surface explicit. That does not mean the chosen proxy is identical to user value. A cache error may or may not alter an answer; a memory hit may or may not be used; a smaller representation may or may not reduce wall time; authenticated latent state may still contain false reasoning. The review therefore keeps mechanism, proxy, task result, and deployment objective separate.

## 3. Technical Reconstruction, Architecture, and Pipeline

### 3.1 Mechanism and information flow

Knowledge-intensive multimodal question answering (KI-MMQA)-answering visual questions whose correct answers cannot be derived from the image alone but must instead be retrieved from an external knowledge source-has emerged as a central testbed for grounded, knowledge-dependent reasoning in vision-language models ( 39 ; 44 ; 10 ; 40 ) . In practice, a single KI-MMQA query through a competitive retrieval-augmented vision-language model costs approximately 1.7 1.7 TFLOPs and exceeds 800 800 ms on an A100 GPU, with KV-cache memory surpassing 12 12 GB—costs paid uniformly per query, entirely regardless of how simple, visually sparse, or knowledge-light the question actually is. We introduce SKIP ( Salient Knowledge-Injected Pathways ), an inference architecture that routes computation along sparse, question-conditional pathways spanning visual encoding, retrieval, and cross-modal fusion.

The reconstructed pipeline is: validate source or input identity; compute the method-specific signal; make the constrained decision; materialize the new representation or state; execute the downstream consumer; and measure both outcome and end-to-end resource use. Every intermediate artifact needs an identity and version so a later failure can be traced to the signal, decision, transformation, or consumer rather than guessed from a final score.

### 3.2 Formal objective and equation audit

The source exposes 80 mathematical objects in the full-paper rendering. Representative forms are `\rho_{v}; N; \bar{\Delta}`. The audit treats every objective, ratio, probability, budget, and normalization as paper-reported and checks its direction and conditioning against adjacent definitions; no theorem or numerical result was independently re-derived.

The mathematical audit checks direction, normalization, conditioning, and denominators. A larger importance or energy score must have a documented effect; a ratio must state retained over original or the reverse; probabilities must identify their sample space; and a guarantee must retain the assumptions under which it was derived. Discrete relaxations, greedy allocations, learned proxies, and approximate composition are mechanisms, not licenses to erase approximation error.

Representative source mathematical objects are listed below. They are navigation evidence, not a substitute for the paper's definitions:

- Mathematical object 1: `\rho_{v}` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 2: `N` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 3: `\bar{\Delta}` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 4: `O(1/\sqrt{N})` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 5: `3.4` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 6: `6.8\times` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 7: `2.7\times` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 8: `0.2` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 9: `0.4` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 10: `0.6` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 11: `0.8` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.
- Mathematical object 12: `1` - inspected with sign, normalization, conditioning, and denominator checks; exact symbol definitions remain anchored to adjacent source text.

### 3.3 Training, calibration, and inference semantics

Construction-time work and runtime work are distinct. Training or calibration may learn scores, rotations, policies, state transitions, or thresholds. Runtime may route, quantize, retrieve, merge, transfer, rebind, prune, or update. The review charges each phase to the claim it supports. If a runtime speed claim excludes offline training that is acceptable when declared; if it excludes request-dependent preprocessing or data movement it is incomplete.

An implementation should preserve an immutable input identity, the exact decision parameters, achieved rather than requested budget, fallback status, and downstream outcome. This is a reviewer inference motivated by the paper's mechanism.[^inference] It converts an opaque optimization into an observable state transition without claiming to prove correctness.

## 4. Experimental Design and Evidence Reconstructed

SKIP achieves the highest accuracy on every benchmark while consuming 4.07 × 4.07\times fewer FLOPs and 2.69 × 2.69\times less wall-clock latency than the strongest baseline (RA-CM3). On InfoSeek—the hardest benchmark for entity-centric retrieval—SKIP improves over RA-CM3 by + 4.3 +4.3 absolute points; on Encyclopedic-VQA by + 3.4 +3.4 . We introduced SKIP, a unified architecture for efficient knowledge-intensive multimodal QA that routes computation along sparse, question-conditional pathways. We derived an information-bottleneck bound on the optimal visual sparsity rate and empirically validated it across five benchmarks, achieving 3 3 – 7 × 7\times compute reductions while improving accuracy over the strongest dense baselines.

The paper's complete section inventory was screened, including introduction, prior work, method or formal analysis, experiments, limitations, references, and appendices where present. Tables and figures were reconciled as numbered source objects rather than copied into this review. The evaluation is read along four axes: task outcome, representation or state cost, systems behavior, and failure behavior.

The experimental unit matters. Model-level averages cannot substitute for task-level failures, trace-level averages cannot substitute for P99 latency, and visual examples cannot substitute for a blinded quality protocol. Where the paper supplies only one layer of evidence, the stronger inference remains open. The review also distinguishes a controlled benchmark from a production workload and a simulation from the physical system it models.

Novelty must be separated from inheritance. Backbones, tokenizers, attention kernels, training corpora, benchmark harnesses, and standard metrics are inherited unless the paper changes them. The claimed contribution is the new decision or representation layer and the evidence that its behavior differs under controlled conditions. This distinction matters because a stronger base model can otherwise be misread as a stronger mechanism.

Resource accounting must follow the whole path. Preprocessing, calibration, auxiliary scoring, transforms, metadata, cache movement, compilation, synchronization, retries, and fallback all belong to the cost of the method. A smaller stored object is not automatically lower latency; a lower arithmetic count is not automatically higher throughput; and an average gain does not determine tail behavior.

Baseline fairness requires the same information boundary, comparable tuning, matched model revision, compatible budget, and competent implementation. If one method receives future-query information, a recent-token buffer, extra calibration data, or a more favorable kernel, the comparison must say so. The closest practical baseline and an intentionally simple baseline serve different diagnostic purposes and both are useful.

Metric semantics are part of the claim. Accuracy, exact match, F1, recall, perplexity, logit error, visual quality, throughput, time to first token, time between tokens, goodput, bytes, and nominal bits answer different questions. Ratios need explicit numerator and denominator, and conditioned results must not be presented as unconditional service behavior.

Uncertainty should be reported at the experimental unit that can fail. Seeds, trials, task instances, traces, models, and hardware repetitions are not interchangeable. Close means need intervals or paired tests; large effects still need failure distributions. Maximum improvements identify an operating point but do not summarize a frontier.

Tail cases deserve their own ledger. Long inputs, rare entities, unusual attention patterns, abrupt workload bursts, adversarial state, stale calibration, numerical overflow, and out-of-distribution tasks can be hidden by averages. A deployable system needs a conservative path whose trigger is measurable and whose outcome is retained for later audit.

Reproducibility has levels: a URL may exist, files may be inspectable, an environment may build, a command may run, and a reported table may reproduce. These are separate receipts. This review verifies source provenance and records author-linked artifacts where found, but it does not claim that code, data, checkpoints, or hardware reproduced the experiments.

Versioning is substantive. The reviewed arXiv version, model revision, dataset snapshot, code commit, and kernel environment define the evidence object. A later arXiv version is not a second paper by default, but it can alter claims or results. Any correction should compare material differences rather than silently replacing the earlier record.

Governance applies to derived state as well as raw sources. Compressed caches, learned memories, semantic identifiers, latent fragments, task states, and quantized representations can retain sensitive or licensed information. Ownership, retention, deletion, tenant isolation, and provenance must survive transformation even when the representation is hard to inspect.

Operational rollout should begin in shadow mode. The new path and a conservative reference path should receive matched requests, with outcome deltas and resource deltas joined by request identity. Thresholds, bypass controls, drift detection, rollback, and a declared circuit-breaker policy are necessary for safe learning from failures.

## 5. Results and Quantitative Interpretation

SKIP achieves the highest accuracy on every benchmark while consuming 4.07 × 4.07\times fewer FLOPs and 2.69 × 2.69\times less wall-clock latency than the strongest baseline (RA-CM3). The accuracy improvement is not an artifact of more compute spent elsewhere: at matched FLOPs (0.42 TFLOPs, the BLIP-2 budget), SKIP outperforms BLIP-2 by + 17.3 +17.3 points on OK-VQA and + 19.5 +19.5 points on InfoSeek. QVS achieves 99 % 99\% of the no-pruning accuracy at just 11 % 11\% retention. We show that question-conditional pruning is qualitatively different in knowledge-intensive settings: at 11 % 11\% retention, QVS outperforms unconditional methods by 5 5 – 10 10 accuracy points ( Figure 4 ).

The correct interpretation is bounded by SKIP achieves the highest accuracy on every benchmark while consuming 4.07 × 4.07\times fewer FLOPs and 2.69 × 2.69\times less wall-clock latency than the strongest baseline (RA-CM3). On InfoSeek—the hardest benchmark for entity-centric retrieval—SKIP improves over RA-CM3 by + 4.3 +4.3 absolute points; on Encyclopedic-VQA by + 3.4 +3.4 . We introduced SKIP, a unified architecture for efficient knowledge-intensive multimodal QA that routes computation along sparse, question-conditional pathways. We derived an information-bottleneck bound on the optimal visual sparsity rate and empirically validated it across five benchmarks, achieving 3 3 – 7 × 7\times compute reductions while improving accuracy over the strongest dense baselines. Reported maxima are kept as maxima, averages as averages, and matched-budget claims as matched-budget claims. The review does not turn a logit metric into task accuracy, cache compression into latency, offline lift into online value, structural convergence into semantic correctness, or anomaly detection into cryptographic integrity.

Three gains should be separated. Representation gain means fewer bytes, tokens, channels, states, or bits. Execution gain means lower latency, memory traffic, transfer, or higher throughput. Outcome gain means better correctness, quality, integrity, or task completion. A paper may establish one, two, or all three under its setup; deployment should require the combination relevant to the service objective.

## 6. Ablations, Sensitivity, and Failure Analysis

Ablations are persuasive when they change one named component while holding model, data, budget, training effort, and implementation competence fixed. The source's analysis and appendix sections were inspected for component removals, budget sweeps, sensitivity tests, and failure examples. Missing axes are retained as gaps rather than assumed favorable.

The paper-specific failure boundary is: We characterize SKIP’s failure modes by examining the 36.8 % 36.8\% of OK-VQA queries it misses. The compositional retrieval failure mode points to a clear next step: extending BSCA to a k k -partite structure that explicitly models multi-hop knowledge dependencies. Treating sparsity as a first-class design principle, jointly across modalities and across retrieval, opens substantial headroom that we believe will only grow as models and corpora scale. A replication should stress the variable most likely to invalidate the mechanism, not merely repeat the easiest central setting. Negative controls should include a no-transformation ceiling, a simple fixed policy, a random or shuffled signal, a cost-matched auxiliary baseline, and a conservative fallback.

Sensitivity should span input distribution, model revision, budget, precision, concurrency, hardware, and time. Learned policies also need calibration curves and drift tests. Systems claims need warmup, synchronization, failure handling, and queueing details. Integrity claims need key or identity lifecycle analysis. Benchmark claims need leakage and judge audits.

## 7. Claim-by-Claim Vetting

| Claim | Direct evidence | Assessment | Boundary |
|---|---|---|---|
| The proposed mechanism is technically specified. | Full method, formal objects, architecture figures, and appendices. | Supported as a paper description. | Independent implementation was not performed. |
| The paper reports the results summarized in this review. | Results prose, numbered tables, figures, and experimental sections. | Supported as paper-reported evidence. | No experiment was independently rerun. |
| The mechanism causes the reported advantage. | Ablations and matched comparisons where present. | Partially supported. | Bundled changes, tuning, and proxy choice can remain confounders. |
| The resource saving is end-to-end. | Runtime and resource accounting in the source. | Conditional. | Preprocessing, data movement, fallback, and hidden metadata must be included. |
| The approach generalizes broadly. | Tested models, data, traces, budgets, and hardware. | Not established outside tested coverage. | Shift, scale, and implementation changes remain open. |
| The method is production-ready. | Paper-level technical and experimental evidence. | Not established. | Reliability, privacy, security, governance, and local cost require service-specific validation. |

Supported means that direct evidence exists within the named scope. It does not mean independent reproduction. Conditional means that the statement is true only when its model, data, budget, hardware, or formal premises are retained.

## 8. External Primary-Source Context

Canonical identity and current version were checked against the [arXiv record](https://arxiv.org/abs/2607.25422v1), [canonical PDF](https://arxiv.org/pdf/2607.25422v1), [canonical HTML](https://arxiv.org/html/2607.25422v1), and [DOI resolver](https://doi.org/10.48550/arXiv.2607.25422). The bounded primary-source check records this publication status: arXiv preprint; no peer-reviewed venue was independently verified.[^external]

- Author-linked artifact verified as a primary source: https://pmlrbd.github.io/skip/

The live Black Lake DEP-A and DEP-E indexes, repository artifacts, logs, and reports were searched by base arXiv ID and normalized title; no same-paper public record was found. Method-neighbor records are context rather than duplicates. Strong method-neighbor records are contextual only where repository evidence warrants them; associations do not transfer claims between papers.[^association]

## 9. Independent Re-conceptualization

Reviewer proposal: Treat Salient Knowledge Pathways: Sparse Cross-Modal Routing for Efficient Knowledge-Intensive Multimodal Question Answering as an evidence-linked control mechanism: preserve the exact input identity, intermediate decisions, realized resource use, fallback behavior, and downstream outcome; then test the paper's proposed mechanism against matched-budget alternatives and distribution shifts that could falsify the claimed causal account. This proposal is not an author claim and has not been experimentally validated.

The general design is an evidence-linked controller with four records: source identity, decision evidence, execution evidence, and outcome evidence. Source identity pins the exact model, data, state, and version. Decision evidence records the signal and chosen action. Execution evidence records realized resource use and fallback. Outcome evidence records task quality and failures. The four records make the method inspectable without pretending observability is a security certification.[^governance]

The controller should support three paths: full or conservative processing, the proposed optimized path, and an explicit refusal or retry path when evidence is invalid. Selection uses calibrated uncertainty and service policy, not the average benchmark score. A shadow sampler continues to run the conservative path so drift can be measured after rollout.

The important shift is from a static representation to a governed state transition. Compression, routing, memory, cache reuse, distributed merging, or task-state maintenance becomes an action with a reason, version, owner, expiry, and rollback. This architecture preserves the paper's useful mechanism while making its assumptions operationally testable.

## 10. Research Notes, Limitations, and Evidence Boundary

The complete local PDF and full-paper HTML for arXiv:2607.25422v1 were inspected. Abstract HTML was treated only as metadata. The review covered section structure, central formal objects, experimental design, results, ablations, limitations, references, appendices, numbered tables and figures, footnote or disclosure signals, and code/data statements where present.

Evidence layers are explicit. Layer one is the paper report. Layer two is external primary evidence such as arXiv and author-linked repositories. Layer three is reviewer inference. Layer four is hypothesis or proposal. A sentence in one layer is not silently promoted into another.

The principal limitations are: We characterize SKIP’s failure modes by examining the 36.8 % 36.8\% of OK-VQA queries it misses. The compositional retrieval failure mode points to a clear next step: extending BSCA to a k k -partite structure that explicitly models multi-hop knowledge dependencies. Treating sparsity as a first-class design principle, jointly across modalities and across retrieval, opens substantial headroom that we believe will only grow as models and corpora scale. The source may contain additional detail that is not repeated verbatim here; the coverage ledger demonstrates inspection while avoiding republication of tables or captions. No numerical result was independently reproduced, no dataset or model was downloaded, and no service deployment was observed.

This review assigns 18/20 on the methodology rubric: source integrity 2, full-paper coverage 2, technical fidelity 2, experimental reconstruction 2, results and ablations 2, provenance 2, evidence separation 2, reproducibility 1, clarity 2, implications 1. No category is zero. The score rates review completeness, not the paper's truth and not security certification.

## 11. Implications for Research, Deployment, and Governance

Researchers should publish full cost-quality frontiers, per-example or per-trace failures, achieved budgets, uncertainty, exact configuration, and artifact versions. Close comparisons need paired uncertainty; maximum improvements need the distribution beneath them. Assumptions used by proofs or simulators should be measured in the same evaluation.

Builders should integrate the mechanism behind a budget and risk controller rather than as irreversible preprocessing. Rollout needs shadow mode, conservative fallback, reason codes, model and data pins, drift alarms, and task-level outcome joins. A performance regression should be diagnosable as a signal, decision, transform, resource, or consumer failure.

Governance covers derived representations. Latent state, cached tensors, learned relevance, semantic IDs, task roles, and compressed vectors may still reveal their sources. Access, retention, correction, deletion, provenance, and tenant isolation are therefore requirements even when the object no longer looks like the original content.

## 12. Replication and Falsification Agenda

1. Pin arXiv:2607.25422v1, source hashes, code revision, model and tokenizer, data snapshot, prompts, configuration, seeds, hardware, drivers, and metric implementation.
2. Reconstruct the strongest baseline and the no-transformation ceiling before running the proposed method.
3. Match information access, requested and achieved budget, calibration data, tuning effort, and fallback behavior across methods.
4. Report task outcome, representation size, end-to-end latency, throughput or goodput, peak memory, preprocessing, data movement, and failure cost.
5. Sweep at least five operating points and publish the full frontier rather than one favorable coordinate.
6. Stratify results by input length, difficulty, rarity, workload pressure, model, and achieved decision; retain worst-decile and catastrophic failures.
7. Remove, shuffle, or randomize the proposed signal while preserving cost to test whether the named mechanism causes the effect.
8. Test a neighboring model, an out-of-domain workload, a changed hardware path, and a later version to measure portability and drift.
9. Exercise invalid input, stale metadata, resource exhaustion, interrupted execution, and fallback; verify that each produces a bounded observable state.
10. Archive raw task-level results, configuration hashes, negative outcomes, and an independently reviewable cost ledger.

Falsifiable hypotheses follow directly. The paper's advantage should remain directionally visible at matched complete cost; a randomized decision signal should lose that advantage; a calibrated fallback should reduce catastrophic errors without eliminating most of the resource gain; and performance should degrade predictably rather than silently when the core assumption is violated. A failure of these tests would weaken the mechanistic explanation even if one benchmark mean remains favorable.

## 13. Durable Restatement

The durable lesson of Salient Knowledge Pathways: Sparse Cross-Modal Routing for Efficient Knowledge-Intensive Multimodal Question Answering is not that its mechanism should always run. It is that the paper makes a previously implicit decision surface explicit, connects that decision to measurable evidence, and exposes conditions that later work can test. The defensible long-term claim is bounded: the authors provide a technically specified method and paper-reported evidence under named conditions. Wider value depends on independent reproduction, complete cost accounting, failure distributions, versioned artifacts, and governed fallback.

## 14. Complete Table, Figure, Equation, and Appendix Coverage Ledger

| Item | Source locator | Review disposition |
|---|---|---|
| Table 1 | Source object `coverage-table-1`; caption length 255 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 2 | Source object `coverage-table-2`; caption length 171 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 3 | Source object `coverage-table-3`; caption length 52 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 4 | Source object `coverage-table-4`; caption length 65 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 5 | Source object `coverage-table-5`; caption length 65 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 6 | Source object `coverage-table-6`; caption length 123 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 7 | Source object `coverage-table-7`; caption length 118 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 8 | Source object `coverage-table-8`; caption length 64 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 9 | Source object `coverage-table-9`; caption length 56 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 10 | Source object `coverage-table-10`; caption length 102 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 11 | Source object `coverage-table-11`; caption length 72 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 12 | Source object `coverage-table-12`; caption length 69 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 13 | Source object `coverage-table-13`; caption length 75 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 14 | Source object `coverage-table-14`; caption length 49 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 15 | Source object `coverage-table-15`; caption length 128 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 16 | Source object `coverage-table-16`; caption length 86 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 17 | Source object `coverage-table-17`; caption length 58 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 18 | Source object `coverage-table-18`; caption length 105 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 19 | Source object `coverage-table-19`; caption length 144 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 20 | Source object `coverage-table-20`; caption length 238 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 21 | Source object `coverage-table-21`; caption length 163 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 22 | Source object `coverage-table-22`; caption length 346 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 23 | Source object `coverage-table-23`; caption length 243 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 24 | Source object `coverage-table-24`; caption length 168 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Table 25 | Source object `coverage-table-25`; caption length 171 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 1 | Source object `coverage-figure-1`; caption length 193 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 2 | Source object `coverage-figure-2`; caption length 288 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 3 | Source object `coverage-figure-3`; caption length 135 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 4 | Source object `coverage-figure-4`; caption length 222 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 5 | Source object `coverage-figure-5`; caption length 303 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Figure 6 | Source object `coverage-figure-6`; caption length 26 characters. | Caption and surrounding argument inspected; values remain paper-reported and were not independently reproduced. |
| Equations | 625 unique mathematical renderings exposed by HTML; representative objects listed in Section 3.2. | Central objective interpreted; signs, normalization, conditioning, and denominators audited; no numerical reproduction. |
| Sections | 163 substantive headings exposed by HTML. | All headings screened; central method, evaluation, results, limitations, references, and appendices reconstructed above. |

Appendix and supplementary coverage:

- Appendix or supplementary heading: Appendix A Notation and Preliminaries
- Appendix or supplementary heading: Appendix B Complete Architecture Details
- Appendix or supplementary heading: Appendix C Theoretical Analysis
- Appendix or supplementary heading: Appendix D Training Procedure and Hyperparameters
- Appendix or supplementary heading: Appendix E Prompt Templates
- Appendix or supplementary heading: Appendix F Extended Ablation Studies
- Appendix or supplementary heading: Appendix G Long-Tail Entity Analysis
- Appendix or supplementary heading: Appendix H Multi-Hop Reasoning Analysis
- Appendix or supplementary heading: Appendix I Generalisation Beyond KI-MMQA
- Appendix or supplementary heading: Appendix J Computational Complexity Analysis
- Appendix or supplementary heading: Appendix K Implementation Details and Reproducibility
- Appendix or supplementary heading: Appendix L Datasets and Corpora
- Appendix or supplementary heading: Appendix M Hyperparameter Sensitivity
- Appendix or supplementary heading: Appendix N Validating Assumption 4.2 : Saliency Fidelity
- Appendix or supplementary heading: Appendix O Additional Baselines
- Appendix or supplementary heading: Appendix P Per-Category Breakdown
- Appendix or supplementary heading: Appendix Q Extended Error Analysis
- Appendix or supplementary heading: Appendix R Qualitative Examples
- Appendix or supplementary heading: Appendix S Memory Usage Analysis
- Appendix or supplementary heading: Appendix T Limitations and Negative Results

Complete section inventory:

- Source section: Salient Knowledge Pathways: Sparse Cross-Modal Routing for Efficient Knowledge-Intensive Multimodal Question Answering
- Source section: Abstract
- Source section: Keywords:
- Source section: 1 Introduction
- Source section: Contributions.
- Source section: 2 Related Work
- Source section: Knowledge-intensive multimodal QA.
- Source section: Visual token pruning.
- Source section: Efficient retrieval.
- Source section: Speculative decoding.
- Source section: Conditional computation.
- Source section: 3 Method: Salient Knowledge Pathways
- Source section: 3.1 Problem Setup
- Source section: 3.2 Architecture Overview
- Source section: 3.3 Question-Guided Visual Saliency (QVS)
- Source section: Why pre-retrieval pruning?
- Source section: Training.
- Source section: 3.4 Region-Conditional Sparse Retrieval (RCSR)
- Source section: Why per-region retrieval?
- Source section: Number of regions.
- Source section: 3.5 Bipartite Sparse Cross-Attention (BSCA)
- Source section: Why bipartite rather than fully sparse?
- Source section: 3.6 Difficulty-Adaptive Budget Controller (DBC)
- Source section: What does the DBC learn?
- Source section: 3.7 Speculative Knowledge Verification (SKV)
- Source section: 4 Theoretical Analysis
- Source section: Assumption 4.1 (Piecewise-Lipschitz mutual information) .
- Source section: Assumption 4.2 (Bounded saliency error) .
- Source section: Theorem 4.3 (Sparsity-accuracy tradeoff) .
- Source section: Proof sketch.
- Source section: Corollary 4.4 .
- Source section: 5 Experiments
- Source section: 5.1 Setup
- Source section: Benchmarks.
- Source section: Knowledge corpora.
- Source section: Baselines.
- Source section: Metrics.
- Source section: Implementation.
- Source section: 5.2 Main Results
- Source section: 5.3 Compute Accuracy Frontier
- Source section: 5.4 Ablations
- Source section: 5.5 Sparsity Analysis
- Source section: 5.6 Failure Modes and Limitations
- Source section: 6 Discussion
- Source section: Why does joint sparsity beat individual sparsity?
- Source section: Generality.
- Source section: When does SKIP fail to help?
- Source section: 7 Conclusion
- Source section: Impact Statement
- Source section: Appendices
- Source section: Appendix A Notation and Preliminaries
- Source section: Appendix B Complete Architecture Details
- Source section: B.1 End-to-End Architecture Diagram
- Source section: B.2 Question-Guided Visual Token Pruning (QVS)
- Source section: Motivation.
- Source section: Complexity.
- Source section: B.3 Region-Conditional Sparse Retrieval (RCSR)
- Source section: Region Proposal.
- Source section: B.4 Bipartite Sparse Cross-Attention (BSCA)
- Source section: B.5 Speculative Knowledge Verification (SKV)
- Source section: B.6 Difficulty-Adaptive Budget Controller (DBC)
- Source section: B.7 End-to-End SKIP Inference
- Source section: Appendix C Theoretical Analysis
- Source section: C.1 Proof of the Sparsity–Accuracy Tradeoff (Theorem 4.3 )
- Source section: Theorem (Sparsity-accuracy tradeoff, restated) .
- Source section: Proof.
- Source section: Step 1: Bounding the symmetric difference.
- Source section: Step 2: Information loss from misranking.
- Source section: Step 3: Information loss from sparsification.
- Source section: Step 4: Concentration argument.
- Source section: Step 5: Combining.
- Source section: Tightness.
- Source section: Extension to retrieval sparsity.
- Source section: C.2 Formal Assumptions and Empirical Justification
- Source section: Justification.
- Source section: C.3 Main Efficiency–Fidelity Theorem
- Source section: C.4 Proof of Theorem C.3
- Source section: Accuracy bound ( 26 ).
- Source section: Lemma C.1 (SKV Preservation of Relevant Passages) .
- Source section: FLOPs bound ( 27 ).
- Source section: C.5 Corollary: DBC Pareto Optimality
- Source section: Appendix D Training Procedure and Hyperparameters
- Source section: D.1 Stage Overview
- Source section: D.2 Stage I: Backbone Warm-Up
- Source section: D.3 Stage II: QVS Warm-Up with Gumbel-Softmax
- Source section: D.4 Stage III: RCSR+BSCA with Gumbel Annealing
- Source section: D.5 Stage IV: SKV Knowledge Distillation
- Source section: D.6 Stage V: DBC Calibration
- Source section: D.7 Stage VI: End-to-End Fine-Tuning
- Source section: Training stability.
- Source section: D.8 General Optimization Settings
- Source section: Optimizer.
- Source section: Learning rate schedule.
- Source section: Batch size.
- Source section: Loss weights.
- Source section: Data sampling.
- Source section: Synthetic data generation prompt.
- Source section: Evaluation protocol.
- Source section: Submission-time hyperparameters.
- Source section: D.9 Complete Hyperparameter Reference
- Source section: Appendix E Prompt Templates
- Source section: E.1 QVS Saliency Query (Stage 1)
- Source section: E.2 RCSR Region Identification (Stage 1)
- Source section: E.3 RCSR Retrieval Query (Stage 2)
- Source section: E.4 SKV Passage Verification (Stage 3)
- Source section: E.5 DBC-Conditioned Answer Generation (Stage 4)
- Source section: Appendix F Extended Ablation Studies
- Source section: F.1 Visual Token Retention Ratio ρ v \rho_{v}
- Source section: F.2 Number of RCSR Regions N N
- Source section: F.3 Backbone VLM Comparison
- Source section: F.4 BSCA Degree Target Δ ¯ \bar{\Delta}
- Source section: Appendix G Long-Tail Entity Analysis
- Source section: G.1 Failure Mode Taxonomy
- Source section: G.2 Entity Popularity Analysis
- Source section: G.3 Mitigation: Augmented Dense Retrieval (SKIP-ADR)
- Source section: Appendix H Multi-Hop Reasoning Analysis
- Source section: H.1 Failure Characterisation
- Source section: H.2 Iterative SKIP (SKIP-Iter)
- Source section: Appendix I Generalisation Beyond KI-MMQA
- Source section: I.1 General VQA (VQAv2)
- Source section: I.2 Medical Visual QA (PathVQA)
- Source section: Appendix J Computational Complexity Analysis
- Source section: J.1 Asymptotic Analysis
- Source section: Dense baseline.
- Source section: SKIP complexity.
- Source section: Memory (7B-class backbone).
- Source section: J.2 Empirical FLOPs Breakdown
- Source section: Memory footprint (this configuration).
- Source section: Appendix K Implementation Details and Reproducibility
- Source section: K.1 Model and Retrieval Configuration
- Source section: Vision encoder.
- Source section: Retrieval index.
- Source section: Backbone.
- Source section: K.2 Synthetic Data Augmentation
- Source section: K.3 Hardware and Software
- Source section: K.4 Reproducibility Checklist
- Source section: Appendix L Datasets and Corpora
- Source section: OK-VQA ( 39 ) .
- Source section: A-OKVQA ( 44 ) .
- Source section: InfoSeek ( 10 ) .
- Source section: Encyclopedic-VQA ( 40 ) .
- Source section: ViQuAE ( 30 ) .
- Source section: Wikipedia corpus.
- Source section: WikiData corpus (Enc-VQA).
- Source section: Appendix M Hyperparameter Sensitivity
- Source section: Sensitivity discussion.
- Source section: Appendix N Validating Assumption 4.2 : Saliency Fidelity
- Source section: Appendix O Additional Baselines
- Source section: Appendix P Per-Category Breakdown
- Source section: Observations.
- Source section: Appendix Q Extended Error Analysis
- Source section: Long-tail entities.
- Source section: Compositional retrieval.
- Source section: SKV misroutes.
- Source section: OCR failures.
- Source section: Appendix R Qualitative Examples
- Source section: Appendix S Memory Usage Analysis
- Source section: Why KV cache shrinks.
- Source section: Activations.
- Source section: Appendix T Limitations and Negative Results
- Source section: Limitations.
- Source section: Negative results.
- Source section: Future work.

This ledger is completeness evidence, not reproduction. It records that each numbered object and section was screened without copying the source's tables, figures, or long captions.

## 15. Source and Evidence Notes

- Canonical record: https://arxiv.org/abs/2607.25422v1
- Canonical PDF: https://arxiv.org/pdf/2607.25422v1
- Canonical full-paper HTML: https://arxiv.org/html/2607.25422v1
- DOI resolver: https://doi.org/10.48550/arXiv.2607.25422
- Reviewed identity: arXiv:2607.25422v1
- Complete authors: Noor Islam S. Mohammad; Uluğ Bayazıt
- Local source documents were verified and withheld from the public repository.
- No PDF, HTML, source archive, extracted source, cache, private validator, or local path is included in the DEP-A package.

## 16. Footnotes

[^scope]: Paper report means attributable to the authors and supported by the reviewed artifact; it is not an independent reproduction.
[^integrity]: Source integrity requires a matching complete PDF and full-paper HTML with identity, structure, references, and readable-body checks.
[^inference]: Reviewer inference labels conclusions derived from the source rather than asserted by the authors.
[^external]: External primary-source vetting establishes provenance or availability only for the record actually inspected.
[^association]: An associated DEP is contextual evidence, not a duplicate and not proof of the reviewed paper's claims.
[^governance]: Governance and deployment proposals are hypotheses; this methodology supports auditability and traceable lineage, not security certification.
