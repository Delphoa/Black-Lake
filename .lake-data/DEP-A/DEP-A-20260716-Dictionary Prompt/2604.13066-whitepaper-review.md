# Whitepaper Review: Lossless Prompt Compression via Dictionary-Encoding and In-Context Learning: Enabling Cost-Effective LLM Analysis of Repetitive Data

**Canonical paper:** arXiv:2604.13066v1
**Authors:** de Campos, Andresa Rodrigues; Lee, David; Kissos, Imry; Paritosh, Piyush
**Review type:** full-paper review and independent re-conceptualization
**Public records:** [abstract](https://arxiv.org/abs/2604.13066v1), [PDF](https://arxiv.org/pdf/2604.13066v1), [full-paper HTML](https://arxiv.org/html/2604.13066v1)

## 1. Executive Assessment

This paper examines **lossless dictionary-encoded prompt compression**. The authors finds repeated subsequences, keeps replacements whose savings exceed dictionary overhead, assigns meta-tokens, and teaches the dictionary in the system prompt. The work matters because reduction is an information-control decision, not just a token-count operation. A saving is useful only when the downstream outcome remains acceptable and the transformation's own cost is counted.

The headline evidence is: roughly 60â€“80% compression; Claude template exact match above 0.99 and algorithmic Levenshtein similarity above 0.91; ratio explains under 2% of similarity variance. These are paper reports, not independently reproduced facts.[^scope] The full PDF and full-paper HTML passed identity, version, structure, and coverage checks before drafting; the abstract page was metadata only.[^integrity] My assessment is positive but conditional: the mechanism or taxonomy is useful, while general deployment requires workload-specific reproduction, tail-risk tests, and reversible controls.

The durable contribution is the change in objective. Instead of maximizing a nominal ratio, the system should preserve task value under a resource constraint. A production decision should therefore compare a complete cost-quality frontier, not select one attractive operating point.

## 2. Problem Framing and Research Questions

Finite context, memory, or execution capacity collides with growing dialogue, repositories, histories, media, or serving demand. Truncation saves resources but deletes evidence without valuing it. Uniform reduction ignores differences in density and consequence. The paper asks whether a structured, learned, or formal policy can allocate the budget better.

The abstract, restated for orientation, says: In-context learning has established itself as an important learning paradigm for Large Language Models (LLMs). In this paper, we demonstrate that LLMs can learn encoding keys in-context and perform analysis directly on encoded representations. This finding enables lossless prompt compression via dictionary encoding without model fine-tuning: frequently occurring subsequences are replaced with compact meta-tokens, and when provided with the compression dictionary in the system prompt, LLMs correctly interpret these meta-tokens during analysis, producing outputs equivalent to those from uncompressed inputs. We present a compression algorithm that identifies repetitive patterns at multiple length scales, incorporating a token-savings optimization criterion that ensures compression reduces costs by preventing dictionary overhead from exceeding savings. The algorithm achieves compression ratios up to 80% depending on dataset characteristics. To validate that LLM analytical accuracy is preserved under compression, we use decompression as a proxy task with unambiguous ground truth. Evaluation on the LogHub 2.0 benchmark using Claude 3.7 Sonnet demonstrates exact match rates exceeding 0.99 for template-based compression and average Levenshtein similarity scores above 0.91 for algorithmic compression, even at compression ratios of 60%â€“80%. Additionally, compression ratio explains less than 2% of variance in similarity metrics, indicating that decompression quality depends on dataset characteristics rather than compression intensity. This training-free approach works with API-based LLMs, directly addressing fundamental deployment constraints â€” token limits and API costs â€” and enabling cost-effective analysis of large-scale repetitive datasets, even as data patterns evolve over time. This review does not use that summary as proof. It tests the abstract's claims against methods, results, tables, figures, appendices, and disclosures.

Five research questions govern the audit. Does the method preserve the target outcome at a fixed budget? Does the saving survive end-to-end cost accounting? Does it transfer across data, models, hardware, and tasks? Are severe individual failures visible? Can a deployment explain and reverse the transformation? These questions make the evidence falsifiable.

Compression has asymmetric risk. Thousands of benign deletions can coexist with one missing negation, identifier, citation, dependency, or policy statement that changes the answer. Averages should be paired with calibration, worst-decile performance, and evidence-survival measures.

## 3. Technical Reconstruction, Architecture, and Pipeline

### 3.1 Pipeline and novelty

The end-to-end pipeline is: ingest the complete object; derive a task- or state-dependent signal; choose a budget or structural action; construct a reduced representation; run the consumer; and measure quality plus resources. The paper's distinctive choice is that it finds repeated subsequences, keeps replacements whose savings exceed dictionary overhead, assigns meta-tokens, and teaches the dictionary in the system prompt. Inherited components include base models, benchmark harnesses, metrics, and serving primitives; novelty lies in the control or representation layer connecting them.

Three interfaces must be pinned. The measurement interface says what is observed before reduction. The decision interface maps observations to a ratio, mask, group, memory item, or execution support. The consumer interface says how the downstream model interprets the reduced object. Tokenizer, prompts, revisions, decoding, preprocessing, and hardware can change results through any interface.

### 3.2 Equations and formal audit

- Expression 1: \times. The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 2: T. The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 3: T^{\prime}. The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 4: \mathcal{D}. The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 5: n_{\text{token}}(T^{\prime}+\mathcal{D})<n_{\text{token}}(T). The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 6: (T^{\prime},\mathcal{D}). The audit checks direction, normalization, conditioning, and operational meaning.
- Expression 7: \langle M\#\rangle. The audit checks direction, normalization, conditioning, and operational meaning.

The sign audit asks whether a larger score retains or removes content. The ratio audit asks whether the denominator is retained/original or original/compressed. The normalization audit asks whether sequences of different length are comparable. The conditioning audit asks whether a query, future label, teacher, or decoder state leaks privileged information.

Formal guarantees remain conditional on their premises. Sparsity, restricted isometry, independence, calibration, or submodularity must be measured rather than assumed. Empirical papers without theorems can still establish an operating region, while theoretical proposals without systems measurements do not establish speedup.

### 3.3 Training and inference semantics

The paper-specific design is: fourteen LogHub 2.0 log families at 2k and selected full scales; Claude 3.7 Sonnet and Nova Premier; decompression as a proxy. Training may use synthetic examples, teachers, reinforcement learning, or no learning. Inference may score tokens, run an auxiliary model, search ratios, build dictionaries, migrate cache blocks, or recover sparse support. All such work belongs in cost accounting.

A safe implementation preserves an immutable source identity and a manifest of transformations. Text reduction records offsets and removed spans. Memory records evidence, confidence, expiry, and deletion. Sparse execution records active support and fallback. Distributed state records ownership and consistency. These are reviewer inferences, not claims by every author.[^inference]

## 4. Experimental Design and Evidence Reconstructed

The design is: fourteen LogHub 2.0 log families at 2k and selected full scales; Claude 3.7 Sonnet and Nova Premier; decompression as a proxy. The experimental unit and denominator matter more than the headline factor. Correct-answer-conditioned compression differs from unconditional savings. Latency without preprocessing is not end-to-end latency. Survey tables that combine heterogeneous papers are descriptive evidence, not a controlled leaderboard.

The full-paper inventory includes 31 headings: 1 Introduction; 2 Background and Related Work; 3 Methodology; 3.1 Problem Formulation; 3.2 Dictionary-Encoding Compression; 3.3 Compression Algorithm; 3.4 Token Savings Condition; 3.5 Overlap Prevention; 3.6 Batch-Level Compression Strategy; 4 Experiments; 4.1 Compression Efficiency; 4.2 Decompression Validation; 4.2.1 Template-Based Decompression; 4.2.2 Algorithm-Based Decompression; 5 Results; 5.1 Compression Efficiency; 5.2 Template-Based Decompression; 5.2.1 LogHub 2k Dataset Results; 5.2.2 Full Dataset Results; 5.3 Algorithmic Compression Evaluation; 6 Discussion; 6.1 Mechanism of In-Context Dictionary Learning; 6.2 Role of Semantic Structure in Reconstruction; 6.3 Practical Implications; 7 Limitations and Future Work; 8 Conclusion; Acknowledgements; References; Appendix A Dataset Token Statistics; Appendix B Template-Based Decompression Detailed Metrics; Appendix C Algorithmic Compression Detailed Metrics. Introduction, technical analysis, evaluation or survey evidence, limitations, references, and appendices were screened where present. Tables, figures, equations, footnotes, funding, disclosures, and code/data statements were inventoried separately.

Baseline fairness requires the same information, budget, base model, and competent configuration. Learned and training-free methods have different costs. The metric audit separates exact match, substring accuracy, edit similarity, semantic quality, throughput, latency, peak memory, and subjective quality. None is interchangeable.

Statistical evidence should identify seeds, trials, uncertainty, aggregation unit, and exclusions. Hardware evidence should identify accelerator, precision, batch, length, compiler, and concurrency. Missing controls do not negate a large effect, but they weaken close comparisons and portability.

## 5. Results and Quantitative Interpretation

The main paper-reported result is: roughly 60â€“80% compression; Claude template exact match above 0.99 and algorithmic Levenshtein similarity above 0.91; ratio explains under 2% of similarity variance. Qualifiers such as â€œup to,â€ conditioning, and version are retained. The evidence is a conditional frontier whose position depends on corpus repetition, density, workload, model, and hardware.

Representation gain reduces tokens, states, or bytes. Execution gain reduces latency, traffic, memory, or accelerator work. Outcome gain improves correctness, quality, resolution, or compliance. A method can achieve one and lose another. Adoption should require the combination relevant to the service objective.

Table/prose consistency was checked in the archived rendering. Survey taxonomy is not converted into benchmark evidence, and formal guarantees are not converted into measured runtime. The review attributes every numerical statement to the paper unless external publication metadata is explicitly identified.

## 6. Ablations, Sensitivity, and Failure Analysis

Ablations should isolate the named mechanism from base-model, data, prompt, and compute changes. Important axes are budget, selector, representation, training objective, teacher or auxiliary model, query conditioning, and preprocessing. Missing axes leave causal claims open.

The principal limitation is: decompression does not prove analytical equivalence; logs are unusually repetitive; system-prompt overhead and adversarial strings are underexplored. This is not ceremonial. It describes how a favorable metric can coexist with failure of the user's objective. Follow-up work should target this boundary and publish failure distributions.

Sensitivity spans inputs (length, density, language, duplication, rare dependencies), models (tokenizer, family, quantization, decoding), and systems (batch, concurrency, pressure, hardware). Learned selectors also require calibration curves. Negative controls include random reduction, recency-only reduction, a gold-evidence oracle, a no-reduction ceiling, and a cost-matched auxiliary baseline.

## 7. Claim-by-Claim Vetting

| Claim | Direct evidence | Assessment |
|---|---|---|
| roughly 60â€“80% compression; Claude template exact match above 0.99 and algorithmic Levenshtein similarity above 0.91; ratio explains under 2% of similarity variance | Paper methods, results, tables, figures, and archived full-text context. | Supported as a paper report; not independently rerun. |
| The named mechanism causes the reported gain. | Component comparisons and sensitivity analyses where present. | Partially supported; omitted axes and bundled changes remain possible. |
| Nominal reduction equals end-to-end saving. | Timing, memory, and accounting boundary in the paper. | Conditional; preprocessing, auxiliary work, movement, and retries must be counted. |
| The method is production ready. | No paper-level study proves service-specific reliability, privacy, security, and cost. | Not established; requires local replication and governance. |
| The result generalizes broadly. | Tested datasets, models, and hardware only. | Supported within the reported scope, not universally. |

â€œSupportedâ€ means direct evidence exists within the claim's scope. It does not mean independent reproduction. Conditional claims name the metric, model, workload, or premise that limits them.

## 8. External Primary-Source Context

Canonical identity was checked against [arXiv](https://arxiv.org/abs/2604.13066v1), [the PDF](https://arxiv.org/pdf/2604.13066v1), and [full HTML](https://arxiv.org/html/2604.13066v1). Author-linked artifact candidates are: https://github.com/arXiv/html_feedback/issues, https://github.com/brucemiller/LaTeXML/wiki/Porting-LaTeX-packages-for-LaTeXML, https://github.com/brucemiller/LaTeXML/issues Availability does not prove completeness or reproduction.[^artifact]

No exact same-paper DEP-A or directly corresponding local record was verified; background citations are not owning depositions. The reviewed version is v1; a later version would require a material-difference assessment, not automatic duplication. A bounded cross-repository search reached a public API rate limit. Absence of a broader association is therefore an evidence boundary, not proof of nonexistence.[^access]

## 9. Independent Re-conceptualization

Reviewer proposal: **a typed wire format whose dictionary is a schema and whose round trip is checked before analysis**. This is not an author claim. It preserves the paper's useful mechanism while adding observability and governance.

Every reduction decision should emit a reason record, a reversible or inspectable source mapping, and an outcome test. The reason explains retention or removal. The mapping supports debugging and deletion. The test verifies the service objective stayed within tolerance. Reduction becomes a controlled state transition instead of invisible preprocessing.

The control loop needs a conservative path. When uncertainty is high, input is out of distribution, or saving does not exceed transformation cost, use the less compressed representation. Shadow full-context samples detect drift. No average metric authorizes irreversible deletion of high-consequence evidence.

Governance applies to derived state. Personal or licensed data should not enter memory without policy. Code needs commit and license provenance. Content-protection signals complement rather than replace access control. Shared KV requires tenant isolation and ownership.

## 10. Research Notes, Limitations, and Evidence Boundary

The paper was read from locally archived full PDF and HTML matching arXiv:2604.13066v1. The PDF passed header, EOF, parse, page, title, body, and references checks. HTML passed size, body-text, document-marker, heading, and structure checks. Source documents remain local.

Evidence layers are explicit. Layer one is the paper report. Layer two is external primary evidence. Layer three is reviewer inference. Layer four is hypothesis or proposal. Numerical results were not independently rerun, and no model or dataset was downloaded for reproduction.

This review scores 18/20 on the methodology rubric: source integrity 2, coverage 2, technical fidelity 2, experimental reconstruction 2, results/ablations 2, provenance 2, evidence separation 2, reproducibility 1, clarity 2, implications 1. No category is zero. The score describes review completeness, not security certification or truth of the claims.

## 11. Implications for Research, Deployment, and Governance

Researchers should publish a complete frontier with fixed accounting, per-example failures, selector confidence, and transformation traces. Conditioned metrics must be labeled. Synthetic labels need validation against downstream value.

Builders should place the method behind a budget controller receiving service objective, privacy policy, and resource state. It chooses full, conservative, or aggressive paths and logs the reason. Rollout needs shadow mode, thresholds, and bypass.

Derived memories and compact representations remain data. They require ownership, retention, access, correction, and deletion. A compressed object may be hard to inspect yet still disclose its source.

Evaluation should include end-to-end latency or energy, throughput under concurrency, peak and steady-state memory, and recovery cost. Surveys need versions. Formal proposals need measured assumptions and a break-even demonstration.

## 12. Replication and Falsification Agenda

1. Pin source hashes, code revision, tokenizer, model, prompts, splits, seeds, hardware, and metrics; reproduce the full-context baseline first.
2. Sweep at least five budgets and report unconditional outcome, end-to-end latency, throughput, peak memory, and transformation cost.
3. Test negation, rare identifiers, cross-document dependencies, duplicated distractors, conflicting history, and adversarial instructions; report worst decile and catastrophic failures.
4. Evaluate new domains, languages, models, tokenizers, and hardware. Publish selector calibration and realized threshold violations.
5. Add provenance mappings and measure whether investigators can explain each decision faster than with opaque reduction.
6. Include scoring, auxiliary models, dictionaries, cache movement, compilation, and retries in break-even accounting.
7. Commission an independent preregistered reproduction with confidence intervals and appropriately redacted failure examples.

Falsifiable hypotheses are: task-conditioned policy beats a global ratio at matched cost; uncertainty fallback reduces severe failures without erasing most savings; provenance improves debugging; gains shrink under shift without calibration; and the central trend persists across two independent model families.

## 13. Durable Restatement

The durable lesson is not â€œcompress more.â€ Limited context, memory, or execution should be allocated by measured downstream value, transformation cost, and reversible evidence. The paper contributes a concrete mechanism or taxonomy for making that allocation less naive.

For arXiv:2604.13066v1, the defensible statement is that the authors finds repeated subsequences, keeps replacements whose savings exceed dictionary overhead, assigns meta-tokens, and teaches the dictionary in the system prompt, supported within the design above. Long-term value depends on independent reproduction, explicit denominators, tail-risk analysis, and governed controls.

## 14. Complete Table, Figure, Equation, and Appendix Coverage Ledger

| Item | Paper content | Review disposition |
|---|---|---|
| Table 1 | Table 1: LogHub Full Dataset Template-Based Results (Claude 3.7 Sonnet) | Caption and surrounding discussion covered; not independently reproduced. |
| Table 2 | Table 2: Decompression quality for algorithmic compression across L m â€‹ a â€‹ x L_{max} values 3â€“10. Metrics reported as mean Â± \pm std across compression levels. Most datasets (12/14) achieve Levenshtein similarity above 0.90; outliers (HPC, Thunderbird) reflect dataset-specific characteristics discussed in Section 6 . | Caption and surrounding discussion covered; not independently reproduced. |
| Table 3 | Table 3: Original Token Counts for LogHub-2k Datasets | Caption and surrounding discussion covered; not independently reproduced. |
| Table 4 | Table 4: Per-dataset template-based decompression metrics for Claude 3.7 Sonnet and Nova Premier on LogHub-2k (2000 logs per dataset). Per-dataset values reported as mean Â± \pm SEM; aggregate row reports mean Â± \pm std across datasets. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 1 | Figure 1: System overview showing hierarchical compression algorithm (right), subsequence finding detail (top left), and compressed output with dictionary (bottom left). | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 2 | Algorithm 1 Hierarchical Compression | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 3 | Algorithm 2 Find Subsequences at Length | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 4 | Algorithm 3 Apply Replacements | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 5 | Listing 1: System Prompt Template | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 6 | Figure 2: Compression ratios vs. L m â€‹ a â€‹ x L_{max} without dictionary overhead (right) and with dictionary overhead (left). Most datasets show plateau behavior, while some exhibit optimal L m â€‹ a â€‹ x L_{max} values beyond which dictionary overhead reduces compression efficiency. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 7 | Figure 3: Template-based decompression results comparing Claude 3.7 Sonnet and Nova Premier across LogHub 2k datasets. Each point represents the average metric score computed over 2,000 logs per dataset. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 8 | Figure 4: Compression ratios and decompression quality as a function of L m â€‹ a â€‹ x L_{max} . Top-left: compression ratios by dataset. Remaining panels: Levenshtein, ROUGE, and BLEU scores. Most datasets maintain scores above 0.9 regardless of compression level; outliers (HPC, Thunderbird) show consistently lower scores across all L m â€‹ a â€‹ x L_{max} values, indicating dataset-specific rather than compression-related effects. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 9 | Figure 5: Relationship between compression ratio and decompression quality. Each point represents one (dataset, L m â€‹ a â€‹ x L_{max} ) configuration. Dashed lines show linear regression fits; shaded regions indicate 95% confidence intervals. R 2 values below 0.02 for all metrics demonstrate that compression intensity does not predict reconstruction qualityâ€”performance variation is driven by dataset characteristics rather than compression level. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 10 | Figure 6: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Apache dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 11 | Figure 7: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for BGL dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 12 | Figure 8: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for HDFS dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 13 | Figure 9: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for HPC dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 14 | Figure 10: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Hadoop dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 15 | Figure 11: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for HealthApp dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 16 | Figure 12: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Linux dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 17 | Figure 13: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Mac dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 18 | Figure 14: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for OpenSSH dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 19 | Figure 15: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for OpenStack dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 20 | Figure 16: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Proxifier dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 21 | Figure 17: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Spark dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 22 | Figure 18: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Thunderbird dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Figure 23 | Figure 19: Levenshtein similarity, ROUGE, and BLEU scores for algorithmic compression for Zookeeper dataset and L m â€‹ a â€‹ x L_{max} values. | Caption and surrounding discussion covered; not independently reproduced. |
| Equations | 166 mathematical renderings inventoried; representative expressions appear in Section 3.2. | Signs, ratios, normalization, and conditioning audited; no numerical reproduction. |
| Sections/appendices | 31 headings inventoried. | All screened; central sections reconstructed and repeated evidence covered by ledger. |
| Footnotes/disclosures | Funding, disclosure, code/data, and supplementary statements searched. | Absence means unreported, not proof of nonexistence. |

The coverage ledger is completeness evidence, not reproduction. Dense tables are summarized by decision-relevant trends and limitations rather than copied.

## 15. Source and Evidence Notes

- Canonical record: https://arxiv.org/abs/2604.13066v1
- Canonical PDF: https://arxiv.org/pdf/2604.13066v1
- Canonical HTML: https://arxiv.org/html/2604.13066v1
- Canonical title: Lossless Prompt Compression via Dictionary-Encoding and In-Context Learning: Enabling Cost-Effective LLM Analysis of Repetitive Data
- Complete authors: de Campos, Andresa Rodrigues; Lee, David; Kissos, Imry; Paritosh, Piyush
- Reviewed version: arXiv:2604.13066v1
- Local source documents were verified and withheld.
- No PDF, HTML, source archive, cache, or private validator is uploaded.

## 16. Footnotes

[^scope]: Paper report means attributable to the authors and supported by the artifact, not independently reproduced.
[^integrity]: Source integrity requires matching full PDF and HTML plus structural and content checks.
[^inference]: Reviewer inference labels conclusions derived from evidence rather than asserted by the authors.
[^artifact]: A linked artifact establishes provenance only to the extent identified by the canonical paper.
[^access]: Blocked search is an access limitation and never a negative existence claim.
[^ratio]: Compression-ratio definitions vary; denominator reconciliation is required before comparison.
[^governance]: Governance proposals are hypotheses motivated by the mechanism, not validated paper outcomes.
