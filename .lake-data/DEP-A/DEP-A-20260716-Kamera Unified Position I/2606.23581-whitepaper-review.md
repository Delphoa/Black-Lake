# Kamera Unified Position Invariant: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “Kamera: Unified Position-Invariant Multimodal KV Cache for Training-Free Reuse”

**Source paper:** Bole Ma, Jan Eitzinger, Harald Koestler, Gerhard Wellein, “Kamera: Unified Position-Invariant Multimodal KV Cache for Training-Free Reuse,” arXiv:2606.23581v1.[^source-paper]

**Artifact prepared:** 2026-07-16  
**Local source reviewed:** complete PDF (22 pages) and matching full-paper HTML (62051 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **Kamera: Unified Position-Invariant Multimodal KV Cache for Training-Free Reuse** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Multimodal agents repeatedly re-examine the same video frames, UI screenshots, and rendered artifacts as their context window slides and reasoning iterates, yet every look-back re-encodes from scratch, because prefix caches serve reuse only at a fixed leading position. We show this recompute is avoidable, and identify exactly what naive KV reuse loses: the cross-chunk conditioning a chunk absorbs from its neighbours.
- Source passage 3: A multimodal agent’s context routinely outgrows its attention window. A web agent slides a three-screenshot window over a monotonically growing transcript (He et al.
- Source passage 4: Reuse is enormously cheap when it works: encoding a 1024 1024 -token video segment costs ≈ 230 \approx\!230 ms of vision-tower compute, while replaying its stored KV costs ≈ 5 \approx\!5 ms (Zheng et al. , 2024b ; Kwon et al.
- Source passage 5: Re-prefilling a chunk at a new position recomputes two things that need not be recomputed. First, position : a chunk’s keys differ across offsets only by a RoPE phase rotation, which composes exactly ( R ​ ( δ ) ​ R ​ ( p ) = R ​ ( p + δ ) R(\delta)R(p){=}R(p{+}\delta) ), so relocation is an algebraic re-rotation, not a forward pass.
- Source passage 6: We name the lost term, Δ = KV ​ ( B ∣ A ) − KV ​ ( B ∣ ∅ ) \Delta=\mathrm{KV}(B\!\mid\!A)-\mathrm{KV}(B\!\mid\!\varnothing) ( B B ’s key/value with A A in front of it minus B B cached alone, ∅ \varnothing marking the absent antecedent; § 2 ), measure its shape, and attack it with the operator its shape dictates. The deficit is diffuse across tokens (no small “important-token” set; an oracle token selector needs ≈ 50 % \approx\!50\% of tokens), yet low-rank in features ( ≈ 90 % \approx\!90\% of its output-relevant energy in ≈ 32 \approx\!32 directions) and deep (negligible in shallow layers).
- Source passage 7: where R ​ ( δ ) R(\delta) re-rotates the stored keys’ RoPE phase to the new position and U m ​ V m ⊤ U_{m}V_{m}^{\!\top} is the top- m m SVD of Δ \Delta , supervised by a single conditioned forward at compile time. The same operator covers MLA (DeepSeek-AI and others, 2024 ) , GQA (Ainslie et al.
- Source passage 8: Separating position from conditioning, and storing the canonical apart from the patch, turns three window operations from re-prefills into millisecond cache edits (Fig. 1 , bottom; § 5 ).
- Source passage 9: A position/conditioning separation that makes a cached multimodal chunk position-free: an exact RoPE relocation plus a rank- m m conditioning patch, one training-free operator across MLA, GQA, and MHA (§ 3 ). It is grounded in a diagnosis of what reuse drops, the cross-chunk conditioning that halves accuracy on tasks needing cross-chunk binding while leaving single-hop readout intact, which we measure to be diffuse-in-tokens yet low-rank-in-features and deep across six backbones (§ 4 ).
- Source passage 10: A measured account of three window operations prefix caching cannot serve (reorder, sliding-window survival, and recall under reversible eviction), including the eviction asymmetry (recall needs a patch; survivors need only relocation) across three attention families (§ 5 ).

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 472 extractable mathematical objects, 47 substantive headings, and 20 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: m
- Equation or mathematical object 2: 1
- Equation or mathematical object 3: 1024
- Equation or mathematical object 4: \approx\!230
- Equation or mathematical object 5: \approx\!5
- Equation or mathematical object 6: O(n)
- Equation or mathematical object 7: O(1)
- Equation or mathematical object 8: R(\delta)R(p){=}R(p{+}\delta)
- Equation or mathematical object 9: B
- Equation or mathematical object 10: A
- Equation or mathematical object 11: B
- Equation or mathematical object 12: A
- Equation or mathematical object 13: 0.57
- Equation or mathematical object 14: 0.41\!\to\!0.28
- Equation or mathematical object 15: 0.28\!\to\!0.15
- Equation or mathematical object 16: \Delta=\mathrm{KV}(B\!\mid\!A)-\mathrm{KV}(B\!\mid\!\varnothing)
- Equation or mathematical object 17: B
- Equation or mathematical object 18: A
- Equation or mathematical object 19: B
- Equation or mathematical object 20: \varnothing

### 2.2 Method and information flow

- Method evidence 1: We name the lost term, Δ = KV ​ ( B ∣ A ) − KV ​ ( B ∣ ∅ ) \Delta=\mathrm{KV}(B\!\mid\!A)-\mathrm{KV}(B\!\mid\!\varnothing) ( B B ’s key/value with A A in front of it minus B B cached alone, ∅ \varnothing marking the absent antecedent; § 2 ), measure its shape, and attack it with the operator its shape dictates. The deficit is diffuse across tokens (no small “important-token” set; an oracle token selector needs ≈ 50...
- Method evidence 2: Diffuse across tokens. Low-rank in features does not mean sparse in tokens. There is no small binding-token set: an oracle that selects tokens by true Δ \Delta -magnitude needs p ≈ 0.5 p\!\approx\!0.5 to recover most of the gap, and a first- k k “carve” is worse than nothing. The few binding directions touch a little of most tokens, so token-recompute methods aim at the wrong axis.
- Method evidence 3: The cleanest miss is identical chunks in a different order (multi-image VQA, reranked RAG, reshuffled frames), where content is byte-identical and only positions and cross-chunk conditioning change. Permuting the predecessor set and comparing the stored canonical-order patch ( transfer ), the ordering’s own patch ( exact ), and a single patch averaged over the permutation orbit with the test ordering held out (...
- Method evidence 4: A growing line takes multimodal KV reuse seriously but pushes an orthogonal axis: compressing or evicting within one growing context . Streaming-video and driving models evict or compress KV memories (StreamingVLM (Xu et al. , 2025b ) , StreamMem (Yang et al. , 2025b ) , HERMES (Zhang et al. , 2026 ) ); robotics VLAs cache static frame tokens across control steps (VLA-Cache (Xu et al. , 2025c ) ); agent frameworks...
- Method evidence 5: The body foregrounds the three window operations. Here we collect the evidence behind its claims, in the order they build the argument: blind reuse breaks multi-hop accuracy while the patch restores it (§ C.1 ); the deficit is intrinsically thin (§ C.2 ), which is why the token, layer, and head selection of prior work misses (§ C.3 ); the deficit and its repair are architecture-universal (§ C.4 ); the effect lives...
- Method evidence 6: The repair frontier is architecture-universal too (Table 8 ): across MLA, GQA, deepstack-GQA, MoE, and pure MHA the feature patch reaches near-ceiling fidelity at zero LLM-prefill recompute, while layer re-prefill must re-run ∼ 86 \sim\!86 – 89 % 89\% of layers to match it and token recompute saturates well below the patch at a 50 % 50\% budget.

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: Kamera: Unified Position-Invariant Multimodal KV Cache for Training-Free Reuse
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: Why the miss is not fundamental.
- Source section 5: What we restore, and how.
- Source section 6: The window operations this buys.
- Source section 7: Contributions.
- Source section 8: 2 What reuse loses: conditioning, not readout
- Source section 9: 3 The operator: relocate exactly, patch the conditioning
- Source section 10: One mechanism for MLA, GQA, and MHA.
- Source section 11: Forming and applying the patch.
- Source section 12: 4 The shape of the lost term dictates a feature patch
- Source section 13: 5 Reuse beyond the window
- Source section 14: Reorder: one orbit-patch for all orderings.
- Source section 15: Sliding-window survival: relocate for free.
- Source section 16: Recall: reversible eviction patches the fixed past.
- Source section 17: The window mechanics, end to end.
- Source section 18: 6 Fidelity, deployment, and cost
- Source section 19: The feature patch reaches the re-prefill ceiling; the token axis does not.
- Source section 20: Recompute-free on a live engine.
- Source section 21: The cost is on memory.
- Source section 22: Scope.
- Source section 23: 7 Related work
- Source section 24: Context management beyond the window.
- Source section 25: Position-independent caching repairs the wrong axis.
- Source section 26: 8 Conclusion
- Source section 27: Acknowledgments
- Source section 28: References
- Source section 29: Appendix A Forming and applying the patch
- Source section 30: Appendix B A menu of cross-chunk reuse operating points and its boundary
- Source section 31: Eviction-probe details.
- Source section 32: Appendix C Supporting evidence for the feature patch
- Source section 33: C.1 Reuse breaks multi-hop accuracy; the patch restores it
- Source section 34: C.2 The deficit is low-rank, so the patch is thin
- Source section 35: C.3 Why token, layer, and head selection miss
- Source section 36: C.4 The deficit and its repair are architecture-universal
- Source section 37: C.5 Where the effect lives: modality scope
- Source section 38: C.6 Memory cost and bf16-faithful live deployment
- Source section 39: Appendix D The reuse safety envelope: when a cached patch survives context drift
- Source section 40: Divergent antecedents.
- Source section 41: Excess context (the superset boundary).
- Source section 42: The cheapest correct refresh.
- Source section 43: Appendix E Context management as reversible state edits
- Source section 44: A near-free disposability test.
- Source section 45: Copy-on-write speculative forks.
- Source section 46: Reuse-aware placement and scheduling.
- Source section 47: Two boundaries.

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: Multimodal agents repeatedly re-examine the same video frames, UI screenshots, and rendered artifacts as their context window slides and reasoning iterates, yet every look-back re-encodes from scratch, because prefix caches serve reuse only at a fixed leading position. We show this recompute is avoidable, and identify exactly what naive KV reuse loses: the cross-chunk conditioning a chunk absorbs from its...
- Evaluation evidence 2: Re-prefilling a chunk at a new position recomputes two things that need not be recomputed. First, position : a chunk’s keys differ across offsets only by a RoPE phase rotation, which composes exactly ( R ​ ( δ ) ​ R ​ ( p ) = R ​ ( p + δ ) R(\delta)R(p){=}R(p{+}\delta) ), so relocation is an algebraic re-rotation, not a forward pass. Second, conditioning : prefilling a chunk B B (the content we cache and reuse)...
- Evaluation evidence 3: A position/conditioning separation that makes a cached multimodal chunk position-free: an exact RoPE relocation plus a rank- m m conditioning patch, one training-free operator across MLA, GQA, and MHA (§ 3 ). It is grounded in a diagnosis of what reuse drops, the cross-chunk conditioning that halves accuracy on tasks needing cross-chunk binding while leaving single-hop readout intact, which we measure to be...
- Evaluation evidence 4: Recompute-free serving (amortized after ≈ 9 {\approx}9 reuses) in SGLang’s production paged-attention kernel and KV pool, reconstructing the re-prefill KV to within bf16 rounding (residual next-token KL ≈ 10 − 3 \approx\!10^{-3} , two orders below blind reuse) with downstream accuracy matching the ceiling, and the cost win on the memory axis (full accuracy at a small fraction of the KV bytes), bounded to the...
- Evaluation evidence 5: Against the audit-faithful named PIC baselines given the same relocated KV (token baselines recompute their selected tokens in context , the strongest CacheBlend form), the rank- m m feature patch closes 98 98 – 100 % 100\% of the reuse → \to re-prefill KL across MLA/GQA, Dense/MoE while token-recompute closes 10 10 – 71 % 71\% , and on the pooled items where reuse flips the answer the patch restores the re-prefill...
- Evaluation evidence 6: In SGLang’s production FlashAttention-3 (Shah et al. , 2024 ) paged-attention kernel and KV pool (radix disabled), relocating a cached segment to a δ ≠ 0 \delta\!\neq\!0 mid-sequence position the prefix-keyed radix scheduler cannot express, the reconstructed KV writes into the pool within one bf16 ULP of recompute, and the resulting next-token KL sits at ≈ 10 − 3 \approx\!10^{-3} across four backbones, two orders...
- Evaluation evidence 7: Decode is memory-bandwidth-bound (its arithmetic intensity sits far below the H100 compute ridge), so the binding resource is KV bytes. A rank-64 patch matches full multi-hop accuracy at ≈ 25 % \approx\!25\% of the segment’s KV bytes (rank-16: ≈ 6 % \approx\!6\% ), a fraction that holds on either layout — the per-head K / V K/V page or the MLA latent ( c K ​ V c_{KV} plus k p ​ e k_{pe} ) page — since the patch and...
- Evaluation evidence 8: The body foregrounds the three window operations. Here we collect the evidence behind its claims, in the order they build the argument: blind reuse breaks multi-hop accuracy while the patch restores it (§ C.1 ); the deficit is intrinsically thin (§ C.2 ), which is why the token, layer, and head selection of prior work misses (§ C.3 ); the deficit and its repair are architecture-universal (§ C.4 ); the effect lives...
- Evaluation evidence 9: The distinction from single-hop-readout and single-image prior work is a ground-truth accuracy gap that opens only where the answer needs cross-chunk binding. On MM-NIAH retrieval-image (a single image needle that must bind to the query, not readout-decomposable) and on the multi-hop reasoning-image split, blind KV reuse halves accuracy and a rank-16 conditioning patch restores the re-prefill ceiling — on both...
- Evaluation evidence 10: The depth structure underneath these results is shown in Fig. 8 : shallow layers carry the chunk’s context-free representation and reuse verbatim, while the conditioning deficit emerges in the middle layers and concentrates in the deep ones, spreading across tokens rather than into a few columns. This is why a single shallow layer or a token subset cannot localize it, why the correction is a low-rank patch on the...
- Evaluation evidence 11: The cross-chunk deficit (the cross-chunk-binding loss, since single-hop reads are exactly recovered) holds across six backbones, isolated directly with the 4D mask of § 2 : block B ↛ A B\!\not\to\!A at B B ’s native positions, so the residual is conditioning with zero position contribution by construction (Table 7 ). Over each model’s full valid probe set ( n = 46 n{=}46 – 94 94 ), the position-matched control sits...
- Evaluation evidence 12: Decode is bandwidth-bound (Fig. 11 a), so the binding resource is KV bytes; a rank-64 patch matches full multi-hop accuracy at ≈ 25 % \approx\!25\% of the segment’s KV bytes (layout-invariant; rank-16 ≈ 6 % \approx\!6\% ) and the forming forward amortizes after ≈ 9 \approx\!9 reuses against a prefill-per-reuse baseline (Fig. 11 c), with the LM prefill replaced by patch-apply improving TTFT 1.8 × → 29 ×...
- Evaluation evidence 13: Because the patch is additive and the position-free canonical is already what we store, conditioning is a switch with an asymmetric cost. Reverting to the context-clean view is free: overwrite the chunk’s cache entries with the stored canonical KV ​ ( B ∣ ∅ ) \mathrm{KV}(B\!\mid\!\varnothing) , a copy with no arithmetic at all. Re-applying conditioning is a single rank- m m add, KV ​ ( B ∣ ∅ ) + U m ​ V m ⊤...
- Evaluation evidence 14: To ask whether an antecedent is still load-bearing for a pending query, toggle its patch off and decode on the canonical: if the answer is unchanged the context was not needed and can be dropped, a check that costs one decode rather than a recompute. This is the cheap, query-specific counterpart to an orchestrator’s semantic-liveness guess, and it sidesteps the diffuse-token result (§ 4 ) that rules out...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1: Three reuse patterns beyond the window, and what each costs. Top (prefix/radix): cached vision embeddings still miss whenever reuse sits at a shifted position (reorder, slide, look-back), so the LLM prefill re-runs. Bottom (Kamera): chunks stored position-free; reorder reuses one orbit-patch, a survivor relocates for free ( R ​ ( δ ) R(\delta) , no patch), and a recalled chunk is rehydrated by a patch on...
- Visual/table evidence 2: Figure 2: Position-invariant storage across attention families. (a) the vision tower splits a frame into tokens, each tagged with a time/height/width coordinate. (b,c) every backbone splits its cached keys/values into a position-free content part (the MLA latent, or the GQA/MHA value) and a positional part (the rotary phase on the key); reuse re-rotates only the positional part to the new location—advancing all...
- Visual/table evidence 3: Figure 3: Structure of the conditioning deficit (Kimi-VL, MLA content channel, 27 27 layers). (a) how much of each layer’s deficit a rank- m m patch captures: the useful knee is near rank 32 32 , well left of where 90 % 90\% of the raw energy sits, so the patch keeps the output-relevant directions rather than all the energy. (b) the deficit grows with depth and is almost entirely conditioning , not position, so the...
- Visual/table evidence 4: Table 1: Eviction is asymmetric. Recall needs a fresh patch: the stale stored patch turns harmful as the window turns over ( η < 0 \eta{<}0 on both GQA backbones), while a rank- 32 32 patch on the chunk’s fixed earlier context tracks full rebuild. Survivors need only R ​ ( δ ) R(\delta) : keeping their KV as-is is near-lossless on GQA/MLA; only the deepstack backbone leaves a (low-rank) removal deficit. Cached video...
- Visual/table evidence 5: Table 2: Operating points for cross-chunk KV reuse, graded by the request–cache relationship. Ruled out at the boundary (cheap-estimation negatives that bound the menu’s shape): the attention-sink prosthesis, a per-antecedent linear operator, key-similarity selection, A-side streaming, and a shallow-seed predictor of the deep patch, all failing because the deficit’s coefficients are item-specific even though its...
- Visual/table evidence 6: Figure 4: Why cheap estimation of the deficit is still open. (a) no cheap selector locates it—the best signal (cross-attention mass) reaches 0.70 0.70 recall but does not solve it; (b) the deficit is redundancy-shaped: anti-correlated with motion and uncorrelated with frame similarity, so only an actual vision-embedding difference tracks it.
- Visual/table evidence 7: Table 3: Ground-truth accuracy across two KV families (GQA, MLA) on two MM-NIAH image tasks that both demand cross-chunk binding: retrieval-image (one needle binding to the query) and the harder multi-hop reasoning-image split. Blind reuse drops accuracy toward chance on every model × \times task; the rank- m m conditioning patch restores it to the re-prefill ceiling and beats the token baselines (CacheBlend/sink,...
- Visual/table evidence 8: Table 4: The contribution-defining comparison on two-page multi-hop document QA: the feature-axis patch reaches the re-prefill ceiling at a small fraction of the segment’s KV, while the token-axis methods the literature uses do not, at matched budget. Byte fractions ( ≈ 6 % / 25 % \approx\!6\%/25\% at rank-16/64) are layout-invariant: the same on the per-head K / V K/V page and on the MLA latent ( c K ​ V + k p ​ e...
- Visual/table evidence 9: Figure 5: One intrinsic rank (about 32 32 ) governs the correction across GQA, MoE, and MLA and across 512 512 vs. 1024 1024 hidden width—the saturating rank is a property of the model, not of its width.
- Visual/table evidence 10: Figure 6: Prior position-independent caches select on the wrong target. (a) wrong axis : an oracle that recomputes tokens needs about half of them and a first- k k carve recovers almost nothing, while a rank-16 feature patch closes most of the gap; (b) wrong depth : a single shallow layer explains little of the final deficit; (c) wrong grouping : the deficit is not concentrated in a few attention heads.
- Visual/table evidence 11: Figure 7: Feature-axis patch vs. sparse-token PIC baselines (MuirBench (Wang et al. , 2024a ) , three architectures), all given the same relocated KV. (a) how much of the conditioning gap each closes—the patch nearly all of it ( 98 98 – 100 % 100\% ), the token baselines a fraction ( 10 10 – 71 % 71\% ); (b) how often each restores the re-prefill answer on the items blind reuse flips—the patch almost always ( 96 %...
- Visual/table evidence 12: Table 5: Answer-flip recovery on four MileBench temporal benchmarks (AS = = ActionSequence, AP = = ActionPrediction, OS = = ObjectShuffle, SC = = StateChange; pooled flip subset n = 75 n{=}75 ). On items where blind reuse flips the answer, the rank- 64 64 patch recovers the re-prefill decision recompute-free; token-recompute baselines fail (VLCache no better than a uniform sink); only a shallow partial re-prefill...
- Visual/table evidence 13: Table 6: Fraction of the multi-hop conditioning loss closed at a matched KV-byte budget (Qwen2.5-VL, n = 46 n{=}46 ). The feature patch exceeds every token-axis recovery: decisively over first- k k (EPIC/MPIC) and low-rank- K K (ShadowKV), and with a paired-significant margin over an oracle query-aware selector (rank-64 paired Δ ​ η = 0.24 \Delta\eta{=}0.24 , 95 % 95\% CI [ 0.07 , 0.41 ] [0.07,0.41] ), establishing...
- Visual/table evidence 14: Figure 8: The deficit is shallow-free and deep. Each plane is one layer’s feature-by-token map: shallow layers reuse verbatim, the deficit grows from the middle layers downward, and it spreads across tokens rather than concentrating in a few—so neither a token subset nor a single shallow layer captures it. A low-rank patch on the deep layers restores it; equivalently, recomputing only the deep layers in context...
- Visual/table evidence 15: Table 7: The cross-chunk conditioning deficit holds across six backbones (4D-mask isolation, B B at native positions; this isolation set and the repair-frontier Table 8 each report six, eleven models in union across all experiments). The position-matched control (ctrl-KL) sits 10 10 – 320 × 320\times below the conditioning loss (loss-KL), Δ \Delta is low-rank ( e 90 / n B ≤ 0.30 e_{90}/n_{B}\!\leq\!0.30 on V V ,...
- Visual/table evidence 16: Figure 9: Low-rank Δ \Delta and monotone rank- m m recovery across softmax-attention VLMs (pure-MHA and KV-sharing variants); the gap tracks cross-chunk-binding capability , not size (LLaVA-1.5 and DeepSeek-VL are both 7B/576-tok, only DeepSeek shows a gap).
- Visual/table evidence 17: Table 8: The repair frontier is architecture-universal ( n = 40 n{=}40 /model, η \eta at rank- 64 64 ). The feature patch reaches near-ceiling fidelity at zero LLM-prefill recompute on every family; layer re-prefill must re-run ∼ 86 \sim\!86 – 89 % 89\% of layers (essentially all on DeepSeek-VL) to match it; token recompute saturates well below the patch even at a 50 % 50\% budget. “Re-prefill cost to match” is the...
- Visual/table evidence 18: Figure 10: Modality scope: vision and video show the gap and recover; audio (Qwen2.5-Omni, n = 40 n{=}40 , exploratory) a smaller partial gap; dense text and readout-decomposable multi-image MC are negative controls.
- Visual/table evidence 19: Figure 11: The cost is on memory. (a) decode sits far on the bandwidth-bound side of the H100 roofline, so KV bytes are the binding resource; (b) replacing the per-reuse prefill with a forward-free patch-apply improves time-to-first-token by up to 29 × 29\times as the reused segment grows ( 256 → 2048 256\!\to\!2048 tokens; larger still against full recompute, which also re-runs the vision encoder); (c) the one-time...
- Visual/table evidence 20: Figure 12: Recompute-free temporal reuse in the live SGLang kernel. (a) the operator’s error over four backbones: re-rotation fixes position and the patch fixes conditioning, bringing the reconstructed KV to within one bf16 unit of recompute (residual next-token KL about 10 − 3 10^{-3} , far below blind reuse); (b) relocation stays exact across the different M-RoPE layouts and rotary bases; (c) downstream...

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: Multimodal agents repeatedly re-examine the same video frames, UI screenshots, and rendered artifacts as their context window slides and reasoning iterates, yet every look-back re-encodes from scratch, because prefix caches serve reuse only at a fixed leading position. We show this recompute is avoidable, and identify exactly what naive KV reuse loses: the cross-chunk conditioning a chunk absorbs from its...
- Evaluation evidence 2: Re-prefilling a chunk at a new position recomputes two things that need not be recomputed. First, position : a chunk’s keys differ across offsets only by a RoPE phase rotation, which composes exactly ( R ​ ( δ ) ​ R ​ ( p ) = R ​ ( p + δ ) R(\delta)R(p){=}R(p{+}\delta) ), so relocation is an algebraic re-rotation, not a forward pass. Second, conditioning : prefilling a chunk B B (the content we cache and reuse)...
- Evaluation evidence 3: A position/conditioning separation that makes a cached multimodal chunk position-free: an exact RoPE relocation plus a rank- m m conditioning patch, one training-free operator across MLA, GQA, and MHA (§ 3 ). It is grounded in a diagnosis of what reuse drops, the cross-chunk conditioning that halves accuracy on tasks needing cross-chunk binding while leaving single-hop readout intact, which we measure to be...
- Evaluation evidence 4: Recompute-free serving (amortized after ≈ 9 {\approx}9 reuses) in SGLang’s production paged-attention kernel and KV pool, reconstructing the re-prefill KV to within bf16 rounding (residual next-token KL ≈ 10 − 3 \approx\!10^{-3} , two orders below blind reuse) with downstream accuracy matching the ceiling, and the cost win on the memory axis (full accuracy at a small fraction of the KV bytes), bounded to the...
- Evaluation evidence 5: Against the audit-faithful named PIC baselines given the same relocated KV (token baselines recompute their selected tokens in context , the strongest CacheBlend form), the rank- m m feature patch closes 98 98 – 100 % 100\% of the reuse → \to re-prefill KL across MLA/GQA, Dense/MoE while token-recompute closes 10 10 – 71 % 71\% , and on the pooled items where reuse flips the answer the patch restores the re-prefill...
- Evaluation evidence 6: In SGLang’s production FlashAttention-3 (Shah et al. , 2024 ) paged-attention kernel and KV pool (radix disabled), relocating a cached segment to a δ ≠ 0 \delta\!\neq\!0 mid-sequence position the prefix-keyed radix scheduler cannot express, the reconstructed KV writes into the pool within one bf16 ULP of recompute, and the resulting next-token KL sits at ≈ 10 − 3 \approx\!10^{-3} across four backbones, two orders...
- Evaluation evidence 7: Decode is memory-bandwidth-bound (its arithmetic intensity sits far below the H100 compute ridge), so the binding resource is KV bytes. A rank-64 patch matches full multi-hop accuracy at ≈ 25 % \approx\!25\% of the segment’s KV bytes (rank-16: ≈ 6 % \approx\!6\% ), a fraction that holds on either layout — the per-head K / V K/V page or the MLA latent ( c K ​ V c_{KV} plus k p ​ e k_{pe} ) page — since the patch and...
- Evaluation evidence 8: The body foregrounds the three window operations. Here we collect the evidence behind its claims, in the order they build the argument: blind reuse breaks multi-hop accuracy while the patch restores it (§ C.1 ); the deficit is intrinsically thin (§ C.2 ), which is why the token, layer, and head selection of prior work misses (§ C.3 ); the deficit and its repair are architecture-universal (§ C.4 ); the effect lives...
- Evaluation evidence 9: The distinction from single-hop-readout and single-image prior work is a ground-truth accuracy gap that opens only where the answer needs cross-chunk binding. On MM-NIAH retrieval-image (a single image needle that must bind to the query, not readout-decomposable) and on the multi-hop reasoning-image split, blind KV reuse halves accuracy and a rank-16 conditioning patch restores the re-prefill ceiling — on both...
- Evaluation evidence 10: The depth structure underneath these results is shown in Fig. 8 : shallow layers carry the chunk’s context-free representation and reuse verbatim, while the conditioning deficit emerges in the middle layers and concentrates in the deep ones, spreading across tokens rather than into a few columns. This is why a single shallow layer or a token subset cannot localize it, why the correction is a low-rank patch on the...
- Evaluation evidence 11: The cross-chunk deficit (the cross-chunk-binding loss, since single-hop reads are exactly recovered) holds across six backbones, isolated directly with the 4D mask of § 2 : block B ↛ A B\!\not\to\!A at B B ’s native positions, so the residual is conditioning with zero position contribution by construction (Table 7 ). Over each model’s full valid probe set ( n = 46 n{=}46 – 94 94 ), the position-matched control sits...
- Evaluation evidence 12: Decode is bandwidth-bound (Fig. 11 a), so the binding resource is KV bytes; a rank-64 patch matches full multi-hop accuracy at ≈ 25 % \approx\!25\% of the segment’s KV bytes (layout-invariant; rank-16 ≈ 6 % \approx\!6\% ) and the forming forward amortizes after ≈ 9 \approx\!9 reuses against a prefill-per-reuse baseline (Fig. 11 c), with the LM prefill replaced by patch-apply improving TTFT 1.8 × → 29 ×...
- Evaluation evidence 13: Because the patch is additive and the position-free canonical is already what we store, conditioning is a switch with an asymmetric cost. Reverting to the context-clean view is free: overwrite the chunk’s cache entries with the stored canonical KV ​ ( B ∣ ∅ ) \mathrm{KV}(B\!\mid\!\varnothing) , a copy with no arithmetic at all. Re-applying conditioning is a single rank- m m add, KV ​ ( B ∣ ∅ ) + U m ​ V m ⊤...
- Evaluation evidence 14: To ask whether an antecedent is still load-bearing for a pending query, toggle its patch off and decode on the canonical: if the answer is unchanged the context was not needed and can be dropped, a check that costs one decode rather than a recompute. This is the cheap, query-specific counterpart to an orchestrator’s semantic-liveness guess, and it sidesteps the diffuse-token result (§ 4 ) that rules out...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: C.5 Where the effect lives: modality scope

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

### 8.1 Primary external artifacts checked

- No official implementation, dataset, or project URL beyond the canonical paper record was verified in the paper or primary-source search; artifact availability therefore remains unverified.

### 8.2 Associated Black Lake records

- No same-paper DEP-A or DEP-E record and no directly corresponding Black-Lake-Data DEP was verified after checks by arXiv ID, DOI, normalized title, method, dataset, and benchmark.

## 9. Research notes and critical considerations

- Qualification evidence 1: Multimodal agents repeatedly re-examine the same video frames, UI screenshots, and rendered artifacts as their context window slides and reasoning iterates, yet every look-back re-encodes from scratch, because prefix caches serve reuse only at a fixed leading position. We show this recompute is avoidable, and identify exactly what naive KV reuse loses: the cross-chunk conditioning a chunk absorbs from its...
- Qualification evidence 2: A measured account of three window operations prefix caching cannot serve (reorder, sliding-window survival, and recall under reversible eviction), including the eviction asymmetry (recall needs a patch; survivors need only relocation) across three attention families (§ 5 ).
- Qualification evidence 3: A 4D-attention-mask oracle that blocks B ↛ A B\!\not\to\!A in a single forward reproduces the loss at B B ’s exact positions: the failure is a binding deficit written into the KV, not a boundary attention artifact, so sink/boundary fixes (EPIC-style) cannot repair it. This is the term Eq. 1 ’s patch supplies.
- Qualification evidence 4: Deep. Δ \Delta ’s relative norm grows with depth ( 0.08 → 0.49 0.08\!\to\!0.49 , shallow → \to deep). A single-layer injection explains ≈ 27 % \approx\!27\% of the final deficit applied shallow but ≈ 97 % \approx\!97\% applied deep, with no shallow shortcut. Together: a thin patch can carry the loss (low-rank), a token subset cannot (diffuse), and the correction must live deep .
- Qualification evidence 5: A windowed agent must sometimes reach back to a chunk it evicted. Because the canonical is what we store and the patch is what we add at reuse, eviction is reversible : drop the conditioned KV, keep the canonical, and re-instate later at any position. The question is whether the stored patch can be replayed, and it cannot. A patch frozen at eviction goes stale as the window turns over, decaying monotonically from η...
- Qualification evidence 6: In SGLang’s production FlashAttention-3 (Shah et al. , 2024 ) paged-attention kernel and KV pool (radix disabled), relocating a cached segment to a δ ≠ 0 \delta\!\neq\!0 mid-sequence position the prefix-keyed radix scheduler cannot express, the reconstructed KV writes into the pool within one bf16 ULP of recompute, and the resulting next-token KL sits at ≈ 10 − 3 \approx\!10^{-3} across four backbones, two orders...
- Qualification evidence 7: A multimodal agent’s context outgrows its window through patterns a prefix cache cannot serve: sliding windows, reorderings, look-backs. The recompute is avoidable: the only thing reuse loses is cross-chunk conditioning, diffuse in tokens but low-rank in features and deep, so a position-free canonical plus a rank- m m patch reconstructs a chunk’s KV at any position with one operator across MLA/GQA/MHA. This makes...
- Qualification evidence 8: More broadly, once a chunk’s binding is a small additive object rather than a baked-in recompute, the KV cache stops being a position-indexed array and becomes a structure an orchestrator edits cheaply: reversible eviction, context-clean forks, content-addressed reuse, and—because reorder is free over an orbit—reuse-aware placement , where a window’s contents are a set and chunk order becomes a scheduling variable...
- Qualification evidence 9: Because the deficit is low-rank in features but diffuse in tokens and concentrated deep, the token/layer/head-selection premise of position-independent caching misses on all three axes (Fig. 6 ). Wrong axis : oracle top- p p token recompute needs p ≈ 0.5 p\!\approx\!0.5 and a first- k k carve recovers ≈ 0 \approx\!0 , while a rank-16 feature patch closes 68 % 68\% . Wrong depth : a single shallow layer explains...
- Qualification evidence 10: The depth structure underneath these results is shown in Fig. 8 : shallow layers carry the chunk’s context-free representation and reuse verbatim, while the conditioning deficit emerges in the middle layers and concentrates in the deep ones, spreading across tokens rather than into a few columns. This is why a single shallow layer or a token subset cannot localize it, why the correction is a low-rank patch on the...
- Qualification evidence 11: Because reorder is free over the permutation orbit (§ 5 ), the chunks a window holds are effectively a set , and their arrangement in the deque is a free variable rather than a consequence of arrival order. This turns context assembly into a scheduling problem with two coupled decisions: which chunks to admit under a fixed memory budget, and in what order to place them so that cached patches stay valid (a chunk...
- Qualification evidence 12: First, the scheme is forward -lossless (a chunk with no future direct read is free to drop), not a retroactive edit: an evicted chunk’s already-absorbed influence on the surviving chunks cannot be exactly inverted, because cross-chunk conditioning is not an invertible linear operator. We measure that influence to be small (survivors are near-lossless to keep as-is on GQA and MLA; § 5 ). Second, re-instating a chunk...

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.[^methodology]

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2606.23581v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: Kamera: Unified Position-Invariant Multimodal KV Cache for Training-Free Reuse
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: Why the miss is not fundamental.
- Source section 5: What we restore, and how.
- Source section 6: The window operations this buys.
- Source section 7: Contributions.
- Source section 8: 2 What reuse loses: conditioning, not readout
- Source section 9: 3 The operator: relocate exactly, patch the conditioning
- Source section 10: One mechanism for MLA, GQA, and MHA.
- Source section 11: Forming and applying the patch.
- Source section 12: 4 The shape of the lost term dictates a feature patch
- Source section 13: 5 Reuse beyond the window
- Source section 14: Reorder: one orbit-patch for all orderings.
- Source section 15: Sliding-window survival: relocate for free.
- Source section 16: Recall: reversible eviction patches the fixed past.
- Source section 17: The window mechanics, end to end.
- Source section 18: 6 Fidelity, deployment, and cost
- Source section 19: The feature patch reaches the re-prefill ceiling; the token axis does not.
- Source section 20: Recompute-free on a live engine.
- Source section 21: The cost is on memory.
- Source section 22: Scope.
- Source section 23: 7 Related work
- Source section 24: Context management beyond the window.
- Source section 25: Position-independent caching repairs the wrong axis.
- Source section 26: 8 Conclusion
- Source section 27: Acknowledgments
- Source section 28: References
- Source section 29: Appendix A Forming and applying the patch
- Source section 30: Appendix B A menu of cross-chunk reuse operating points and its boundary
- Source section 31: Eviction-probe details.
- Source section 32: Appendix C Supporting evidence for the feature patch
- Source section 33: C.1 Reuse breaks multi-hop accuracy; the patch restores it
- Source section 34: C.2 The deficit is low-rank, so the patch is thin
- Source section 35: C.3 Why token, layer, and head selection miss
- Source section 36: C.4 The deficit and its repair are architecture-universal
- Source section 37: C.5 Where the effect lives: modality scope
- Source section 38: C.6 Memory cost and bf16-faithful live deployment
- Source section 39: Appendix D The reuse safety envelope: when a cached patch survives context drift
- Source section 40: Divergent antecedents.
- Source section 41: Excess context (the superset boundary).
- Source section 42: The cheapest correct refresh.
- Source section 43: Appendix E Context management as reversible state edits
- Source section 44: A near-free disposability test.
- Source section 45: Copy-on-write speculative forks.
- Source section 46: Reuse-aware placement and scheduling.
- Source section 47: Two boundaries.

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1: Three reuse patterns beyond the window, and what each costs. Top (prefix/radix): cached vision embeddings still miss whenever reuse sits at a shifted position (reorder, slide, look-back), so the LLM prefill re-runs. Bottom (Kamera): chunks stored position-free; reorder reuses one orbit-patch, a survivor relocates for free ( R ​ ( δ ) R(\delta) , no patch), and a recalled chunk is rehydrated by a patch on...
- Visual/table evidence 2: Figure 2: Position-invariant storage across attention families. (a) the vision tower splits a frame into tokens, each tagged with a time/height/width coordinate. (b,c) every backbone splits its cached keys/values into a position-free content part (the MLA latent, or the GQA/MHA value) and a positional part (the rotary phase on the key); reuse re-rotates only the positional part to the new location—advancing all...
- Visual/table evidence 3: Figure 3: Structure of the conditioning deficit (Kimi-VL, MLA content channel, 27 27 layers). (a) how much of each layer’s deficit a rank- m m patch captures: the useful knee is near rank 32 32 , well left of where 90 % 90\% of the raw energy sits, so the patch keeps the output-relevant directions rather than all the energy. (b) the deficit grows with depth and is almost entirely conditioning , not position, so the...
- Visual/table evidence 4: Table 1: Eviction is asymmetric. Recall needs a fresh patch: the stale stored patch turns harmful as the window turns over ( η < 0 \eta{<}0 on both GQA backbones), while a rank- 32 32 patch on the chunk’s fixed earlier context tracks full rebuild. Survivors need only R ​ ( δ ) R(\delta) : keeping their KV as-is is near-lossless on GQA/MLA; only the deepstack backbone leaves a (low-rank) removal deficit. Cached video...
- Visual/table evidence 5: Table 2: Operating points for cross-chunk KV reuse, graded by the request–cache relationship. Ruled out at the boundary (cheap-estimation negatives that bound the menu’s shape): the attention-sink prosthesis, a per-antecedent linear operator, key-similarity selection, A-side streaming, and a shallow-seed predictor of the deep patch, all failing because the deficit’s coefficients are item-specific even though its...
- Visual/table evidence 6: Figure 4: Why cheap estimation of the deficit is still open. (a) no cheap selector locates it—the best signal (cross-attention mass) reaches 0.70 0.70 recall but does not solve it; (b) the deficit is redundancy-shaped: anti-correlated with motion and uncorrelated with frame similarity, so only an actual vision-embedding difference tracks it.
- Visual/table evidence 7: Table 3: Ground-truth accuracy across two KV families (GQA, MLA) on two MM-NIAH image tasks that both demand cross-chunk binding: retrieval-image (one needle binding to the query) and the harder multi-hop reasoning-image split. Blind reuse drops accuracy toward chance on every model × \times task; the rank- m m conditioning patch restores it to the re-prefill ceiling and beats the token baselines (CacheBlend/sink,...
- Visual/table evidence 8: Table 4: The contribution-defining comparison on two-page multi-hop document QA: the feature-axis patch reaches the re-prefill ceiling at a small fraction of the segment’s KV, while the token-axis methods the literature uses do not, at matched budget. Byte fractions ( ≈ 6 % / 25 % \approx\!6\%/25\% at rank-16/64) are layout-invariant: the same on the per-head K / V K/V page and on the MLA latent ( c K ​ V + k p ​ e...
- Visual/table evidence 9: Figure 5: One intrinsic rank (about 32 32 ) governs the correction across GQA, MoE, and MLA and across 512 512 vs. 1024 1024 hidden width—the saturating rank is a property of the model, not of its width.
- Visual/table evidence 10: Figure 6: Prior position-independent caches select on the wrong target. (a) wrong axis : an oracle that recomputes tokens needs about half of them and a first- k k carve recovers almost nothing, while a rank-16 feature patch closes most of the gap; (b) wrong depth : a single shallow layer explains little of the final deficit; (c) wrong grouping : the deficit is not concentrated in a few attention heads.
- Visual/table evidence 11: Figure 7: Feature-axis patch vs. sparse-token PIC baselines (MuirBench (Wang et al. , 2024a ) , three architectures), all given the same relocated KV. (a) how much of the conditioning gap each closes—the patch nearly all of it ( 98 98 – 100 % 100\% ), the token baselines a fraction ( 10 10 – 71 % 71\% ); (b) how often each restores the re-prefill answer on the items blind reuse flips—the patch almost always ( 96 %...
- Visual/table evidence 12: Table 5: Answer-flip recovery on four MileBench temporal benchmarks (AS = = ActionSequence, AP = = ActionPrediction, OS = = ObjectShuffle, SC = = StateChange; pooled flip subset n = 75 n{=}75 ). On items where blind reuse flips the answer, the rank- 64 64 patch recovers the re-prefill decision recompute-free; token-recompute baselines fail (VLCache no better than a uniform sink); only a shallow partial re-prefill...
- Visual/table evidence 13: Table 6: Fraction of the multi-hop conditioning loss closed at a matched KV-byte budget (Qwen2.5-VL, n = 46 n{=}46 ). The feature patch exceeds every token-axis recovery: decisively over first- k k (EPIC/MPIC) and low-rank- K K (ShadowKV), and with a paired-significant margin over an oracle query-aware selector (rank-64 paired Δ ​ η = 0.24 \Delta\eta{=}0.24 , 95 % 95\% CI [ 0.07 , 0.41 ] [0.07,0.41] ), establishing...
- Visual/table evidence 14: Figure 8: The deficit is shallow-free and deep. Each plane is one layer’s feature-by-token map: shallow layers reuse verbatim, the deficit grows from the middle layers downward, and it spreads across tokens rather than concentrating in a few—so neither a token subset nor a single shallow layer captures it. A low-rank patch on the deep layers restores it; equivalently, recomputing only the deep layers in context...
- Visual/table evidence 15: Table 7: The cross-chunk conditioning deficit holds across six backbones (4D-mask isolation, B B at native positions; this isolation set and the repair-frontier Table 8 each report six, eleven models in union across all experiments). The position-matched control (ctrl-KL) sits 10 10 – 320 × 320\times below the conditioning loss (loss-KL), Δ \Delta is low-rank ( e 90 / n B ≤ 0.30 e_{90}/n_{B}\!\leq\!0.30 on V V ,...
- Visual/table evidence 16: Figure 9: Low-rank Δ \Delta and monotone rank- m m recovery across softmax-attention VLMs (pure-MHA and KV-sharing variants); the gap tracks cross-chunk-binding capability , not size (LLaVA-1.5 and DeepSeek-VL are both 7B/576-tok, only DeepSeek shows a gap).
- Visual/table evidence 17: Table 8: The repair frontier is architecture-universal ( n = 40 n{=}40 /model, η \eta at rank- 64 64 ). The feature patch reaches near-ceiling fidelity at zero LLM-prefill recompute on every family; layer re-prefill must re-run ∼ 86 \sim\!86 – 89 % 89\% of layers (essentially all on DeepSeek-VL) to match it; token recompute saturates well below the patch even at a 50 % 50\% budget. “Re-prefill cost to match” is the...
- Visual/table evidence 18: Figure 10: Modality scope: vision and video show the gap and recover; audio (Qwen2.5-Omni, n = 40 n{=}40 , exploratory) a smaller partial gap; dense text and readout-decomposable multi-image MC are negative controls.
- Visual/table evidence 19: Figure 11: The cost is on memory. (a) decode sits far on the bandwidth-bound side of the H100 roofline, so KV bytes are the binding resource; (b) replacing the per-reuse prefill with a forward-free patch-apply improves time-to-first-token by up to 29 × 29\times as the reused segment grows ( 256 → 2048 256\!\to\!2048 tokens; larger still against full recompute, which also re-runs the vision encoder); (c) the one-time...
- Visual/table evidence 20: Figure 12: Recompute-free temporal reuse in the live SGLang kernel. (a) the operator’s error over four backbones: re-rotation fixes position and the patch fixes conditioning, bringing the reconstructed KV to within one bf16 unit of recompute (residual next-token KL about 10 − 3 10^{-3} , far below blind reuse); (b) relocation stays exact across the different M-RoPE layouts and rotary bases; (c) downstream...

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2606.23581v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2606.23581v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2606.23581v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2606.23581v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2606.23581
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
