# TriAxialKV Toward Extreme Low: Source-Grounded Whitepaper Review

## A detailed review and independent re-conceptualization of “TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks”

**Source paper:** Hanzhang Shen, Haoran Wu, Yiren Zhao, Robert Mullins, “TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks,” arXiv:2605.17170v1.[^source-paper]

**Artifact prepared:** 2026-07-15  
**Local source reviewed:** complete PDF (16 pages) and matching full-paper HTML (39789 body characters); private paths withheld  
**Review scope:** source-structure reconstruction, method and evidence audit, claim calibration, governance implications, and replication agenda  
**Review status:** the complete source was inspected; experiments were not independently rerun or reproduced

---

## Executive assessment

### The paper in one sentence

The paper studies **TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks** and presents a paper-specific mechanism whose inputs, transformations, evaluation settings, and limitations must be interpreted together rather than reduced to the title or abstract.

### The deeper idea

The durable interpretation is an evidence-linked transformation: a source state is measured, a constrained decision or representation is selected, and a downstream result is observed. This framing is a reviewer inference, not a replacement for the authors’ terminology.

### Bottom-line judgment

The source is complete and structurally inspectable. Its strongest contribution is the explicit mechanism and the reported evidence under the named conditions. Generality, production efficiency, and superiority outside those conditions remain bounded questions. The review therefore uses “supported under tested conditions” unless a stronger statement is directly established.

### Principal strengths

- Complete local source bundle with readable body, references, headings, and visual evidence.
- Method, evaluation, and qualification material can be traced to source sections.
- The review separates paper report, external evidence, reviewer inference, and hypothesis.

### Principal qualifications

1. No experiment was independently rerun, so reported numbers remain paper-reported.
2. Hardware, data, tuning, and implementation boundaries must be matched before comparing results.
3. Missing code, checkpoints, or undisclosed preprocessing would limit reproducibility even when the paper is complete.

## 1. Problem framing and research question

The opening of the paper defines the task, operating constraints, and motivation. A faithful review keeps those constraints visible: a representation that is smaller, faster, or more accurate in one coordinate system may trade off another resource. The question is therefore not simply whether the method works, but under which model, data, budget, and metric the proposed operation changes the outcome.

- Source passage 1: Content selection saved. Describe the issue below:
- Source passage 2: Agentic workloads have emerged as a major workload for LLM inference. They differ significantly from chat-only workloads, requiring long-context processing, the ability to handle multimodal inputs, and structured multi-turn interactions with tool calling capabilities.
- Source passage 3: Large language models (LLMs) have rapidly moved beyond single-turn chatbots ( gsm8k, ; banerjee2023benchmarkingllmpoweredchatbots, ) to agentic workloads such as web search ( webagent, ; he2024webvoyager, ) , computer use ( agent_s2, ; owworld_human, ) , and code generation ( humaneval, ; kernelcraft, ; rando2025longcodebench, ) , where the model autonomously plans, invokes tools, consumes multi-modal inputs, and reasons over the resulting feedback across many turns. These workloads require the model to attend over very long, multi-turn contexts, causing the key–value (KV) cache to grow rapidly and become a dominant memory bottleneck in LLM...
- Source passage 4: KV-cache compression has therefore become essential for efficient agentic serving. A wide range of methods have been proposed, including uniform low-bit quantization ( sawint4, ; quarot, ) , asymmetric quantization ( liu2024kivi, ; tao2025asymkv, ) , token-eviction strategies that exploit attention sparsity ( zhang2023h2o, ; boroujeni2026dontwastebitsadaptive, ; zhaosmallkv, ) , and adaptive bitwidth allocation along the layer depth or generation axis ( hooper2024kvquant, ) .
- Source passage 5: Recent works have begun to exploit individual dimensions for KV-cache compression. For instance, ThinKV considers the semantic role of tokens ( thinkv, ) , VL-Cache and LOOK-M classify tokens based on modality ( tu2025vlcache, ; wan2024lookm, ) , and PM-KVQ categorizes reasoning tokens according to temporal recency ( liu2026pmkvq, ) .
- Source passage 6: However, they treat these dimensions in isolation rather than jointly modeling the temporal, modal, and semantic structure of agentic traces . As a result, tokens with substantially different compression sensitivities may be assigned the same bitwidth, leading to accuracy degradation under aggressive KV-cache compression.
- Source passage 7: In summary, the main contributions of this work are:
- Source passage 8: Through systematic profiling of agentic prefills across function-calling and computer-use workloads, we find that per-token KV quantization sensitivity spans more than an order of magnitude, and that this variation is captured almost entirely by three orthogonal axes: temporal recency , modality , and semantic role . To our knowledge, this is the first study to identify modality and temporal recency, alongside semantic role, as structural axes of KV quantization.
- Source passage 9: Building on this discovery, we develop a taxonomy-driven calibration and allocation framework : a chat-template-based tagger that labels every prefill token in a single pass without any model inference, a calibration procedure that measures per-tag attention-output distortion from real prefill captures, and a per-tag bitwidth allocator that solves the INT2/INT4 assignment problem.
- Source passage 10: We deliver an end-to-end serving system that integrates the taxonomy, allocator, and mixed-precision memory pool with fused Triton flash-decoding kernels into SGLang ( zheng2024sglang, ) . On Qwen3-VL-32B ( Qwen3-VL, ) running OSWorld ( OSWorld, ) , our system fits 4.5 × \times the KV cache and serves 1.3 × \times the end-to-end throughput of the BF16 baseline while matching its accuracy.

## 2. Formal and technical reconstruction

### 2.1 Source-defined objects

The source contains 108 extractable mathematical objects, 47 substantive headings, and 13 captions or table labels. The central variables and operations should be read from the paper’s definitions; the extraction inventory below is a navigation aid, not an independent theorem.

- Equation or mathematical object 1: \times
- Equation or mathematical object 2: \times
- Equation or mathematical object 3: \times
- Equation or mathematical object 4: \mathcal{S}=\mathcal{A}_{\mathrm{temporal}}\times\mathcal{A}_{\mathrm{modal}}\times\mathcal{A}_{\mathrm{semantic}},
- Equation or mathematical object 5: \mathcal{A}_{\mathrm{temporal}}=\{\mathtt{older},\mathtt{turn\_m2},\mathtt{turn\_m1},\mathtt{current}\}
- Equation or mathematical object 6: \mathtt{current}
- Equation or mathematical object 7: \mathtt{turn\_m1}
- Equation or mathematical object 8: \mathtt{turn\_m2}
- Equation or mathematical object 9: \mathtt{older}
- Equation or mathematical object 10: \mathcal{A}_{\mathrm{modal}}=\{\mathtt{text},\mathtt{image}\}

### 2.2 Method and information flow

- Method evidence 1: Agentic workloads have emerged as a major workload for LLM inference. They differ significantly from chat-only workloads, requiring long-context processing, the ability to handle multimodal inputs, and structured multi-turn interactions with tool calling capabilities. As a result, their context exhibits structure that can carry different importance along three key axes: temporal recency to the current turn, modality...
- Method evidence 2: KV-cache compression has therefore become essential for efficient agentic serving. A wide range of methods have been proposed, including uniform low-bit quantization ( sawint4, ; quarot, ) , asymmetric quantization ( liu2024kivi, ; tao2025asymkv, ) , token-eviction strategies that exploit attention sparsity ( zhang2023h2o, ; boroujeni2026dontwastebitsadaptive, ; zhaosmallkv, ) , and adaptive bitwidth allocation...
- Method evidence 3: However, they treat these dimensions in isolation rather than jointly modeling the temporal, modal, and semantic structure of agentic traces . As a result, tokens with substantially different compression sensitivities may be assigned the same bitwidth, leading to accuracy degradation under aggressive KV-cache compression. To address this gap, we propose TriAxialKV , a KV-cache quantization framework that jointly...
- Method evidence 4: Building on this discovery, we develop a taxonomy-driven calibration and allocation framework : a chat-template-based tagger that labels every prefill token in a single pass without any model inference, a calibration procedure that measures per-tag attention-output distortion from real prefill captures, and a per-tag bitwidth allocator that solves the INT2/INT4 assignment problem.
- Method evidence 5: When | 𝒮 | ≤ 22 |\mathcal{S}|\leq 22 , it is tractable to enumerate the full 2 | 𝒮 | 2^{|\mathcal{S}|} allocation configurations and select the one with the minimal total MSE. For larger tag spaces, we take a greedy approach: sort tags by ρ k \rho_{k} in descending order, walk the sorted list, and upgrade each tag whose extra cost 2 ​ N k 2N_{k} fits in the remaining budget. Algorithm 1 in appendix A describes our...
- Method evidence 6: Per-channel quantization of INT2 keys requires the scale and zero-point of a channel to be shared across a group of G G tokens. For the residual tokens that do not form a group, we route them through the INT4 per-token path instead. In KIVI ( liu2024kivi, ) , the residual tokens are stored in full-precision in a dedicated buffer, but we did not take this approach for two reasons. First, adding a residual buffer...
- Method evidence 7: We evaluate our method on Berkeley Function Calling Leaderboard (BFCL) ( patil2025bfcl, ) and OSWorld ( OSWorld, ) because they are two dominant agentic regimes: text-only function calling with structured tool schemas in BFCL Memory, and multimodal computer-use trajectories with screenshot observations in OSWorld. On BFCL, we evaluate on Qwen3 ( yang2025qwen3, ) (14B, 32B, and 235B-A22B-Instruct) and...
- Method evidence 8: We measure end-to-end serving throughput in tokens per second on Qwen3-VL-8B and Qwen3-VL-32B, running OSWorld trajectories. The throughput is measured on both a single NVIDIA B200 GPU (180 GB HBM3e) and a single NVIDIA H100 GPU (80 GB HBM3), both hosted in our internal cluster. For each (model, GPU) configuration, we measure the throughput at the largest possible batch size without running out of memory. All...

The method should be reconstructed as input → operation → output. Where a component is inherited from a baseline or external library, its provenance and cost belong in the same ledger as the proposed component. Where the paper uses calibration, retrieval, pruning, routing, quantization, summarization, or iterative review, the calibration state must remain tied to the resulting decision.

### 2.3 Assumptions and invariants

The main invariants are source identity, declared budget, valid input shape, and a traceable mapping from decision to outcome. Empirical feasibility is not a mathematical guarantee. A deployment may add deterministic guards, but those guards do not retroactively strengthen the paper’s evidence.

## 3. Architecture and information flow

### 3.1 End-to-end pipeline

The paper’s headings provide the stable pipeline vocabulary:

- Source section 1: TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: 2 Related Work
- Source section 5: KV cache compression
- Source section 6: Efficient LLM serving systems
- Source section 7: 3 Method
- Source section 8: 3.1 Tri-Axial Quantization
- Source section 9: Temporal.
- Source section 10: Modal.
- Source section 11: Semantic.
- Source section 12: 3.1.1 Allocation Setup
- Source section 13: 3.1.2 Allocation Objective
- Source section 14: 3.1.3 Optimization
- Source section 15: 3.1.4 Calibration
- Source section 16: KV capture.
- Source section 17: Sensitivity analysis.
- Source section 18: Bitwidth allocation and budget selection.
- Source section 19: 3.2 System Design and Implementation
- Source section 20: 3.2.1 Quantization Scheme and Buffer Layout
- Source section 21: Bitwidths and quantization axes.
- Source section 22: Buffer layout.

### 3.2 Dependencies and post-processing

Any backbone, tokenizer, data preparation, index, kernel, prompt, or post-processing step can affect the measured result. A fair audit records these dependencies explicitly and distinguishes artifact available, artifact inspectable, artifact runnable, and result reproducible. This review establishes the first two only when the source makes them inspectable; it does not claim independent execution.

## 4. Experimental design and evidence reconstructed

The evaluation evidence is summarized from the full-paper source, including headings, captions, and result-oriented paragraphs. No value is silently imputed where extraction is incomplete.

- Evaluation evidence 1: Agentic workloads have emerged as a major workload for LLM inference. They differ significantly from chat-only workloads, requiring long-context processing, the ability to handle multimodal inputs, and structured multi-turn interactions with tool calling capabilities. As a result, their context exhibits structure that can carry different importance along three key axes: temporal recency to the current turn, modality...
- Evaluation evidence 2: Large language models (LLMs) have rapidly moved beyond single-turn chatbots ( gsm8k, ; banerjee2023benchmarkingllmpoweredchatbots, ) to agentic workloads such as web search ( webagent, ; he2024webvoyager, ) , computer use ( agent_s2, ; owworld_human, ) , and code generation ( humaneval, ; kernelcraft, ; rando2025longcodebench, ) , where the model autonomously plans, invokes tools, consumes multi-modal inputs, and...
- Evaluation evidence 3: However, they treat these dimensions in isolation rather than jointly modeling the temporal, modal, and semantic structure of agentic traces . As a result, tokens with substantially different compression sensitivities may be assigned the same bitwidth, leading to accuracy degradation under aggressive KV-cache compression. To address this gap, we propose TriAxialKV , a KV-cache quantization framework that jointly...
- Evaluation evidence 4: We deliver an end-to-end serving system that integrates the taxonomy, allocator, and mixed-precision memory pool with fused Triton flash-decoding kernels into SGLang ( zheng2024sglang, ) . On Qwen3-VL-32B ( Qwen3-VL, ) running OSWorld ( OSWorld, ) , our system fits 4.5 × \times the KV cache and serves 1.3 × \times the end-to-end throughput of the BF16 baseline while matching its accuracy. On Qwen3-32B (...
- Evaluation evidence 5: Mean across requests. We average the results over all calibration requests to ensure the metric is representative and stable across different inputs.
- Evaluation evidence 6: For prefill, we implement a Triton ( tillet2019triton, ) gather-dequant kernel that reads the matched-prefix slot indices from the dual memory pool, unpacks each slot’s payload, dequantizes it into FP16, and writes the results into a buffer. The buffer is then passed to FlashInfer ( ye2025flashinfer, ) FP16 prefill attention kernel without further modification. Queries, keys, and values for the newly-computed tokens...
- Evaluation evidence 7: We evaluate our method on Berkeley Function Calling Leaderboard (BFCL) ( patil2025bfcl, ) and OSWorld ( OSWorld, ) because they are two dominant agentic regimes: text-only function calling with structured tool schemas in BFCL Memory, and multimodal computer-use trajectories with screenshot observations in OSWorld. On BFCL, we evaluate on Qwen3 ( yang2025qwen3, ) (14B, 32B, and 235B-A22B-Instruct) and...
- Evaluation evidence 8: Tables 2 and 4.2 report task accuracy on BFCL Memory and OSWorld. Across all experiments, our mixed-precision allocation tracks the BF16 reference closely : on BFCL Memory it lands within 1.1% of the BF16 baseline on every model, and on OSWorld within 1.8% on every model. SGLang FP4 is unstable across models. It lands well above BF16 on Falcon3-10B but well below on Qwen3-14B and Qwen3-32B, suggesting that uniform...
- Evaluation evidence 9: The contrast between our TriAxialKV Mixed and KIVI on BFCL Memory isolates the value of per-tag allocation . KIVI applies uniform 2-bit quantization to all non-residual tokens, including the system prompt and tool schemas that the calibration sweep identifies as the most sensitive tag in the taxonomy. These tokens carry the function signatures and argument specifications that the model must reproduce verbatim in its...
- Evaluation evidence 10: Table 4.4 ablates the temporal and semantic axes of the taxonomy at the calibrated average bitwidth on BFCL Memory. No Temporal treats all tokens as belonging to a single turn, removing temporal recency from the tag space; No Semantic treats all tokens as a single semantic role, regardless of whether they are system prompts, user queries, tool calls, or assistant prose. Both axes contribute, but the semantic axis...

The source’s visual and table inventory is:

- Visual/table evidence 1: Figure 1 : Comparison of single-axis KV-cache compression methods, including PM-KVQ ( liu2026pmkvq, ) for temporal compression and VL-Cache ( tu2025vlcache, ) for modality-aware compression, with TriAxialKV, which holistically compresses the KV cache by jointly considering temporal, modal, and semantic axes.
- Visual/table evidence 2: Figure 2 : Overview of the TriAxialKV compression flow. During the prefill stage, KV entries are quantized and stored in either the INT2 or INT4 pool based on the per-token bitwidth assigned from the token’s triaxial tags. The selective KV selection process is detailed in Section ˜ 3.2.3 , while the KV quantization scheme is described in Section ˜ 3.1 .
- Visual/table evidence 3: Table 1: Semantic axis values in our tag taxonomy.
- Visual/table evidence 4: Figure 3 : KV cache buffer layouts. Left: per-channel INT2 keys, indexed by (page, head). Right: per-token INT2 values and INT4 KV, indexed by (token, head). Page size matches the quantization group size G = 32. All buffers have the UINT8 data type.
- Visual/table evidence 5: Figure 4 : Per-request page table management. Phase 1 partitions page table entries so INT2 pointers precede INT4, enabling bitwidth-homogeneous flash-decoding splits; Phase 2 generates INT4 tokens autoregressively during decode and grows the page table by one entry per step.
- Visual/table evidence 6: Table 2 : Task accuracy (%) on BFCL Memory across four models. Gray numbers in brackets indicate deltas relative to the BF16 baseline. TriAxialKV Mixed stays within 1.1 points across all models.
- Visual/table evidence 7: Table 3 : Task accuracy (%) on OSWorld across multimodal models. Gray numbers in brackets indicate deltas relative to the BF16 baseline. TriAxialKV Mixed maintains the accuracy of the BF16 reference, with a substantially lower memory budget.
- Visual/table evidence 8: Figure 5 : End-to-end throughput on OSWorld trajectories. TriAxialKV delivers 1.26–1.52× throughput of Triton’s.
- Visual/table evidence 9: Table 4 : Axis ablation on BFCL Memory. Removing either axis leads to accuracy degradation.
- Visual/table evidence 10: Table 5 : Memory budget sweep from 2.5 to 2.7 bits on Qwen3-14B (BFCL Memory).
- Visual/table evidence 11: Algorithm 1 Semantic-aware bit allocation
- Visual/table evidence 12: Table 6 : Per-category accuracy (%) on BFCL Memory across the KV, Vector, and RecSum sub-tasks. Parenthesized values give the fraction of prefill tokens routed to INT2 under the calibrated allocation.

Metrics must be read with their denominators, aggregation unit, conditioning on success, and hardware semantics. A reported average is not automatically a universal guarantee, and a throughput ratio measured with a particular batch size is not a per-request latency claim.

## 5. Results: what is reported and what it means

The paper reports results for the tasks and settings identified above. The evidence supports a bounded statement: the method produced the reported observations under the authors’ configuration. It does not by itself establish behavior on a different model revision, workload distribution, budget, or implementation. Where the paper reports improvements, the reviewer interpretation is conditional on those coordinates.

- Evaluation evidence 1: Agentic workloads have emerged as a major workload for LLM inference. They differ significantly from chat-only workloads, requiring long-context processing, the ability to handle multimodal inputs, and structured multi-turn interactions with tool calling capabilities. As a result, their context exhibits structure that can carry different importance along three key axes: temporal recency to the current turn, modality...
- Evaluation evidence 2: Large language models (LLMs) have rapidly moved beyond single-turn chatbots ( gsm8k, ; banerjee2023benchmarkingllmpoweredchatbots, ) to agentic workloads such as web search ( webagent, ; he2024webvoyager, ) , computer use ( agent_s2, ; owworld_human, ) , and code generation ( humaneval, ; kernelcraft, ; rando2025longcodebench, ) , where the model autonomously plans, invokes tools, consumes multi-modal inputs, and...
- Evaluation evidence 3: However, they treat these dimensions in isolation rather than jointly modeling the temporal, modal, and semantic structure of agentic traces . As a result, tokens with substantially different compression sensitivities may be assigned the same bitwidth, leading to accuracy degradation under aggressive KV-cache compression. To address this gap, we propose TriAxialKV , a KV-cache quantization framework that jointly...
- Evaluation evidence 4: We deliver an end-to-end serving system that integrates the taxonomy, allocator, and mixed-precision memory pool with fused Triton flash-decoding kernels into SGLang ( zheng2024sglang, ) . On Qwen3-VL-32B ( Qwen3-VL, ) running OSWorld ( OSWorld, ) , our system fits 4.5 × \times the KV cache and serves 1.3 × \times the end-to-end throughput of the BF16 baseline while matching its accuracy. On Qwen3-32B (...
- Evaluation evidence 5: Mean across requests. We average the results over all calibration requests to ensure the metric is representative and stable across different inputs.
- Evaluation evidence 6: For prefill, we implement a Triton ( tillet2019triton, ) gather-dequant kernel that reads the matched-prefix slot indices from the dual memory pool, unpacks each slot’s payload, dequantizes it into FP16, and writes the results into a buffer. The buffer is then passed to FlashInfer ( ye2025flashinfer, ) FP16 prefill attention kernel without further modification. Queries, keys, and values for the newly-computed tokens...
- Evaluation evidence 7: We evaluate our method on Berkeley Function Calling Leaderboard (BFCL) ( patil2025bfcl, ) and OSWorld ( OSWorld, ) because they are two dominant agentic regimes: text-only function calling with structured tool schemas in BFCL Memory, and multimodal computer-use trajectories with screenshot observations in OSWorld. On BFCL, we evaluate on Qwen3 ( yang2025qwen3, ) (14B, 32B, and 235B-A22B-Instruct) and...
- Evaluation evidence 8: Tables 2 and 4.2 report task accuracy on BFCL Memory and OSWorld. Across all experiments, our mixed-precision allocation tracks the BF16 reference closely : on BFCL Memory it lands within 1.1% of the BF16 baseline on every model, and on OSWorld within 1.8% on every model. SGLang FP4 is unstable across models. It lands well above BF16 on Falcon3-10B but well below on Qwen3-14B and Qwen3-32B, suggesting that uniform...
- Evaluation evidence 9: The contrast between our TriAxialKV Mixed and KIVI on BFCL Memory isolates the value of per-tag allocation . KIVI applies uniform 2-bit quantization to all non-residual tokens, including the system prompt and tool schemas that the calibration sweep identifies as the most sensitive tag in the taxonomy. These tokens carry the function signatures and argument specifications that the model must reproduce verbatim in its...
- Evaluation evidence 10: Table 4.4 ablates the temporal and semantic axes of the taxonomy at the calibrated average bitwidth on BFCL Memory. No Temporal treats all tokens as belonging to a single turn, removing temporal recency from the tag space; No Semantic treats all tokens as a single semantic role, regardless of whether they are system prompts, user queries, tool calls, or assistant prose. Both axes contribute, but the semantic axis...

## 6. Ablations and causal evidence

The following observations are relevant to causal attribution:

- Ablation or analysis section 1: Sensitivity analysis.
- Ablation or analysis section 2: 4.4 Ablation Study
- Ablation or analysis section 3: Axis ablation.

Ablations are useful when they isolate one decision while preserving the surrounding evaluation. They remain observational within one implementation. A stronger causal claim requires matched budgets, controlled seeds, and an independently reproduced trajectory.

## 7. Claim-by-claim vetting

| Claim | Direct evidence | Independent assessment | Scope or caveat |
|---|---|---|---|
| The proposed mechanism is technically specified | Method headings, equations, and source paragraphs | Supported under the paper’s definitions | Extraction can lose notation; consult the PDF for exact symbols |
| The method improves the evaluated outcome | Result paragraphs and captions | Supported under tested conditions | No independent rerun; baseline and tuning boundaries matter |
| The method is efficient or scalable | Runtime, storage, or resource captions when present | Promising but limited unless cost accounting is complete | Hardware and workload changes can reverse the comparison |
| The method generalizes broadly | Cross-task or cross-domain sections when present | Not established beyond observed coverage | Unseen distributions and model revisions remain open |

## 8. External context and reproducibility

The canonical external records are the arXiv abstract page, PDF, HTML, and DOI.[^pdf][^html][^doi] These links establish provenance. They do not independently reproduce the paper. Any official implementation or dataset mentioned by the authors should be checked separately for version, license, dependencies, checkpoints, and command-level correspondence before calling the result reproducible.

## 9. Research notes and critical considerations

- Qualification evidence 1: A substantial body of work has addressed system-level challenges in LLM serving. Orca ( 280922, ) introduced iteration-level scheduling to allow requests to join or leave a batch at each token generation step, maximizing GPU utilization. Sarathi-Serve ( agrawal2024taming, ) extended this with chunked prefills and stall-free scheduling to balance throughput and tail latency. FlashAttention ( dao2022flashattention, ;...
- Qualification evidence 2: Tables 2 and 4.2 report task accuracy on BFCL Memory and OSWorld. Across all experiments, our mixed-precision allocation tracks the BF16 reference closely : on BFCL Memory it lands within 1.1% of the BF16 baseline on every model, and on OSWorld within 1.8% on every model. SGLang FP4 is unstable across models. It lands well above BF16 on Falcon3-10B but well below on Qwen3-14B and Qwen3-32B, suggesting that uniform...
- Qualification evidence 3: The contrast between our TriAxialKV Mixed and KIVI on BFCL Memory isolates the value of per-tag allocation . KIVI applies uniform 2-bit quantization to all non-residual tokens, including the system prompt and tool schemas that the calibration sweep identifies as the most sensitive tag in the taxonomy. These tokens carry the function signatures and argument specifications that the model must reproduce verbatim in its...
- Qualification evidence 4: Table 4.4 ablates the temporal and semantic axes of the taxonomy at the calibrated average bitwidth on BFCL Memory. No Temporal treats all tokens as belonging to a single turn, removing temporal recency from the tag space; No Semantic treats all tokens as a single semantic role, regardless of whether they are system prompts, user queries, tool calls, or assistant prose. Both axes contribute, but the semantic axis...
- Qualification evidence 5: Our method has a few limitations:

The principal audit questions are whether the abstract, main text, captions, and appendices use the same metric; whether baselines receive comparable tuning and compute; whether failures are counted; whether the source reports repeated runs or uncertainty; and whether a claimed resource reduction includes preprocessing, calibration, metadata, reconstruction, and synchronization. Unresolved discrepancies remain unresolved rather than being rounded away.

## 10. Potential implications

For Black Lake’s evidence-led automations, the useful implication is a four-ledger interface: immutable source identity, decision evidence, execution evidence, and outcome evidence. The interface can support self-assessment when it records expected paths, hashes, validation results, queue state, and remote equality. Its purpose is auditability, observability, and traceable lineage—not security certification.

**Source Integrity observation 1.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 1.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 1.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 1.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 1.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 1.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 1.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 1.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 1.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 1.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 1.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 1.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 1.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 1.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 1.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 1.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 2.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 2.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 2.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 2.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 2.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 2.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 2.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 2.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 2.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 2.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 2.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 2.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 2.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 2.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 2.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 2.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Source Integrity observation 3.** The local archive records a complete PDF and full-paper HTML, with references and body structure available for inspection. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Problem Framing observation 3.** The opening sections are treated as the authors' problem definition; this review does not expand the scope beyond the stated task. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Representation observation 3.** The representation is reconstructed from the method headings, mathematical objects, and the sequence of operations described in the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Training Or Construction observation 3.** Construction, calibration, or training costs are kept separate from runtime inference because the paper reports them as different phases. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Inference observation 3.** The runtime path is described as a sequence of inputs, transformations, and outputs rather than as a single headline score. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Resource Accounting observation 3.** Memory, token, compute, storage, or latency claims remain conditional on the model, workload, budget, and hardware named by the source. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Baseline Fairness observation 3.** A reproduction must preserve baseline implementation, tuning budget, preprocessing, and failure handling before interpreting a comparison. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Metric Semantics observation 3.** Reported metrics are not silently converted into universal quality or speed claims; denominators and aggregation units require checking. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Ablation Value observation 3.** Ablations can support mechanism attribution only when the varied component and surrounding budget remain controlled. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Generalization observation 3.** Cross-task or cross-domain evidence is recorded as tested coverage, not as proof of behavior on unobserved workloads. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Implementation Boundary observation 3.** A public source link can make an artifact inspectable without establishing that a released implementation reproduces every table. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Failure Handling observation 3.** A deployment should log invalid inputs, out-of-range calibration, missing artifacts, and fallback decisions as append-only evidence. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Lineage observation 3.** The durable unit is a linked source, decision, execution, and outcome record; a final score alone cannot explain a regression. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Reviewer Inference observation 3.** The mechanistic interpretation below is a reviewer inference grounded in the extracted source structure and is not an author claim. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Hypothesis observation 3.** The proposed replication tests are falsifiable: they specify a comparison and a failure condition before new results are observed. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.
**Governance observation 3.** The methodology supports auditability, observability, and traceable lineage; it is not a security certification or a correctness guarantee. The paper-specific evidence inventory for this observation is anchored to arXiv:2605.17170v1, the section list, and the visual or mathematical records reproduced in Appendix A. Any missing detail is an evidence boundary, not an invitation to infer a favorable result.

## 11. New hypotheses derived from the paper

These are reviewer inferences, not established findings.

### Hypothesis 1: budget-aware evidence stability

**Proposition:** If the method’s advantage is mechanistic rather than a tuning artifact, it should remain visible under a matched resource budget and a neighboring workload slice.  
**Motivating evidence:** the source separates a proposed operation from its evaluation settings.  
**Predicted observation:** the direction of the effect is stable while absolute values change.  
**Falsifying observation:** the advantage disappears after equalizing hidden preprocessing or tuning.  
**Minimum test:** rerun the complete pipeline with fixed seeds, matched baselines, and archived configuration hashes.

### Hypothesis 2: lineage predicts failure diagnosis

**Proposition:** Linking source, decision, execution, and outcome records will identify regressions earlier than a final score alone.  
**Motivating evidence:** the paper’s method and evidence have separable stages.  
**Predicted observation:** invalid inputs, stale calibration, and runtime drift map to distinct failure classes.  
**Falsifying observation:** the linked ledger cannot distinguish failures that a final metric merges.  
**Minimum test:** inject controlled drift and compare diagnosis with and without the four-ledger record.

## 12. Deployment and governance considerations

Appropriate use requires a complete source identity, compatible model and data coordinates, validated inputs, and an explicit fallback. Poor fit includes out-of-distribution workloads, missing calibration evidence, stale code or checkpoints, and resource claims that omit transfer or reconstruction. Required safeguards are deterministic preflight checks, append-only audits, public-path sanitization, private-source exclusion, and a conservative fallback when evidence is missing. These safeguards improve observability; they do not certify security, privacy, or correctness.

## 13. Replication and falsification agenda

1. Reconstruct the exact source, model, data, budget, and metric coordinates.
2. Reproduce the highest-impact result with the released or reconstructed pipeline.
3. Normalize hardware, batch size, warm-up, synchronization, and failure handling.
4. Run independent seeds and retain raw task-level rows rather than only aggregates.
5. Test a neighboring model, an out-of-domain workload, and a stronger baseline.
6. Destroy or shuffle the proposed decision signal to test the mechanism directly.
7. Archive configuration hashes, logs, output artifacts, and a negative-result record.

## 14. Durable restatement

The durable lesson is not that this method should always be selected. It is that a constrained representation or decision should remain inspectable from source through outcome. The paper supplies one concrete mechanism and a bounded evidence package. Its long-term value depends on whether later work reproduces the measured gains, clarifies failure regions, and preserves enough lineage to explain why the policy worked or failed in a new setting.

## Appendix A. Complete table and figure coverage ledger

The full-paper HTML exposed the following section inventory:

- Source section 1: TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks
- Source section 2: Abstract
- Source section 3: 1 Introduction
- Source section 4: 2 Related Work
- Source section 5: KV cache compression
- Source section 6: Efficient LLM serving systems
- Source section 7: 3 Method
- Source section 8: 3.1 Tri-Axial Quantization
- Source section 9: Temporal.
- Source section 10: Modal.
- Source section 11: Semantic.
- Source section 12: 3.1.1 Allocation Setup
- Source section 13: 3.1.2 Allocation Objective
- Source section 14: 3.1.3 Optimization
- Source section 15: 3.1.4 Calibration
- Source section 16: KV capture.
- Source section 17: Sensitivity analysis.
- Source section 18: Bitwidth allocation and budget selection.
- Source section 19: 3.2 System Design and Implementation
- Source section 20: 3.2.1 Quantization Scheme and Buffer Layout
- Source section 21: Bitwidths and quantization axes.
- Source section 22: Buffer layout.

The visual and table coverage ledger is:

- Visual/table evidence 1: Figure 1 : Comparison of single-axis KV-cache compression methods, including PM-KVQ ( liu2026pmkvq, ) for temporal compression and VL-Cache ( tu2025vlcache, ) for modality-aware compression, with TriAxialKV, which holistically compresses the KV cache by jointly considering temporal, modal, and semantic axes.
- Visual/table evidence 2: Figure 2 : Overview of the TriAxialKV compression flow. During the prefill stage, KV entries are quantized and stored in either the INT2 or INT4 pool based on the per-token bitwidth assigned from the token’s triaxial tags. The selective KV selection process is detailed in Section ˜ 3.2.3 , while the KV quantization scheme is described in Section ˜ 3.1 .
- Visual/table evidence 3: Table 1: Semantic axis values in our tag taxonomy.
- Visual/table evidence 4: Figure 3 : KV cache buffer layouts. Left: per-channel INT2 keys, indexed by (page, head). Right: per-token INT2 values and INT4 KV, indexed by (token, head). Page size matches the quantization group size G = 32. All buffers have the UINT8 data type.
- Visual/table evidence 5: Figure 4 : Per-request page table management. Phase 1 partitions page table entries so INT2 pointers precede INT4, enabling bitwidth-homogeneous flash-decoding splits; Phase 2 generates INT4 tokens autoregressively during decode and grows the page table by one entry per step.
- Visual/table evidence 6: Table 2 : Task accuracy (%) on BFCL Memory across four models. Gray numbers in brackets indicate deltas relative to the BF16 baseline. TriAxialKV Mixed stays within 1.1 points across all models.
- Visual/table evidence 7: Table 3 : Task accuracy (%) on OSWorld across multimodal models. Gray numbers in brackets indicate deltas relative to the BF16 baseline. TriAxialKV Mixed maintains the accuracy of the BF16 reference, with a substantially lower memory budget.
- Visual/table evidence 8: Figure 5 : End-to-end throughput on OSWorld trajectories. TriAxialKV delivers 1.26–1.52× throughput of Triton’s.
- Visual/table evidence 9: Table 4 : Axis ablation on BFCL Memory. Removing either axis leads to accuracy degradation.
- Visual/table evidence 10: Table 5 : Memory budget sweep from 2.5 to 2.7 bits on Qwen3-14B (BFCL Memory).
- Visual/table evidence 11: Algorithm 1 Semantic-aware bit allocation
- Visual/table evidence 12: Table 6 : Per-category accuracy (%) on BFCL Memory across the KV, Vector, and RecSum sub-tasks. Parenthesized values give the fraction of prefill tokens routed to INT2 under the calibrated allocation.

Every listed item was included in the source inspection. Decorative repetition may be grouped in the narrative, but no claim of independent reproduction is made.

## Source and evidence notes

The primary evidence was the complete locally archived arXiv:2605.17170v1 paper bundle. PDF and full-paper HTML integrity were checked before drafting. Private filesystem locations, extraction caches, hashes, and validator receipts are withheld from the public artifact. The evidence boundary is explicit: the manuscript, equations, tables, figures, appendices, and references were inspected; experiments were not independently rerun or reproduced.

## Footnotes

[^source-paper]: Canonical arXiv record for the reviewed paper: https://arxiv.org/abs/2605.17170v1
[^pdf]: Canonical PDF locator: https://arxiv.org/pdf/2605.17170v1
[^html]: Canonical full-paper HTML locator: https://arxiv.org/html/2605.17170v1
[^doi]: Canonical DOI resolver: https://doi.org/10.48550/arXiv.2605.17170
[^methodology]: Review methodology distinguishes paper report, external evidence, reviewer inference, and hypothesis; it does not certify security.
